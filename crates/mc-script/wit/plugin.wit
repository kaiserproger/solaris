/// The one world a plugin component implements.
///
/// The guest exports the phases and the event callback; the host owns every
/// effect. There is no export that lets a guest choose its own plugin id, name a
/// generation, or reach an admission boundary directly.
package solaris:plugin@0.7.0;

world plugin {
    import host;
    export lifecycle;
    export events;
}
