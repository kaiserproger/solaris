# Phase 4 entity / AI evidence matrix — 2026-08-21

This file tracks Phase-4 item 3 independently from the broader common-play matrix.
A row is not promoted to complete merely because lower owner/unit tests exist; the
strongest executable production-path evidence is named explicitly.

| Domain | Strongest current executable evidence | Current disposition |
| --- | --- | --- |
| Villagers / schedules | `mc-test-harness --test villager_schedule_presence`: bound server + production ticker, seeded persisted adult brain/POIs, `time set 2000`, visible movement toward the job site; generated-village worldgen gate also covers stable UUID/save/restart | Production schedule caller is proven. The side-by-side vanilla parity harness still cannot construct equivalent persisted brain/POI state on both servers, so its `PassiveAiSchedule` row remains explicitly degraded for fixture parity only. |
| Village defence | `mc-test-harness --test village_defense_presence`: three recently slept adult villagers + ravager under the real ticker produce an iron-golem `AddEntity`, followed by tracking-scoped attack event `4`; lower owner suite covers spawn memory and restart dedupe | Production TCP path proven. Broader raids/player-created anger/repair/flowers remain outside this bounded defence slice. |
| Guardians | `mc-test-harness --test guardian_presence`: guardian target metadata index `17` -> event `21` -> player damage -> target reset. `mc-net guardian` also fences the common target planner and goal/physics CAS interaction | Production TCP lifecycle proven. Squid/axolotl targeting, true LOS, thorns/mining-fatigue and runtime difficulty selection remain open. |
| Friendly/hostile population | Existing natural-spawn TCP/real-client manifests plus deterministic natural-spawn restart identity/persistence evidence | Scheduler/population authority is present; the separately named 20-minute visible natural-spawn acceptance session remains a release/Phase-4 item-1/6 gate. |
| Navigation / movement authority | Entity goal/pathing owner tests, regional owner scaling gates, collision/physics TCP coverage, and the new villager/guardian production-ticker tests | Generic navigation is active in production. Species-specific locomotion quirks are not implied by this row. |
| Common hostile combat | Existing zombie raw-TCP damage/drop gate; skeleton arrow and pillager crossbow owner/wire suites; guardian, blaze, warden, shulker, evoker, ghast, breeze, witch, wither, Ender Dragon and iron-golem TCP gates | Generic melee, skeleton-family arrow, creeper fuse, pillager crossbow, guardian beam, blaze small-fireball, warden sonic boom, shulker bullet, evoker fangs, ghast large-fireball, breeze wind-charge, witch splash-potion, wither-skull, bounded Ender Dragon air combat and golem combat are implemented. No species remains on the `UnsupportedSpecial` combat fallback. |
| Blaze / small fireball | `mc-test-harness --test blaze_presence`: real TCP client observes blaze charged metadata, `small_fireball` spawn, 5-damage player health publication, projectile removal and charged reset. `mc-net blaze_` proves exact retained 60-tick charge / 6-tick three-shot burst / 100-tick cooldown plus metadata encoding; `mc-net small_fireball` covers entity damage, accepted-hit 5-second fire timer and lethal XP rewards; `mc-entity hurting_projectile` covers owner/block ordering and stale-fact commit fences; the server physics regression is pinned to the local 26.1.2 `AbstractHurtingProjectile.tick()` inertia-before-raycast ordering. | Production AI -> generic hurting-projectile -> wire -> player-damage path is proven. Accepted entity/player fireball damage retains the vanilla 5-second ignition timer transactionally. Periodic generic burn damage and SmallFireball block-fire placement under `mobGriefing` remain explicit residuals. |
| Warden / sonic boom | `mc-test-harness --test warden_presence`: bound server + real protocol client observes Warden event `62` and the resulting 10-damage health publication. `mc-net warden_sonic_boom_charges_damages_and_cools_down` pins the local 26.1.2 34-tick charge, 60-tick behavior duration and 40-tick cooldown with CAS-guarded retained state. | Production charge/event/damage lifecycle proven. The 26.1.2 sonic-specific particle trail, sounds and strong resistance-scaled knockback remain explicit presentation/physics residuals rather than being claimed by the damage gate. |
| Shulker / shulker bullet | `mc-test-harness --test shulker_presence`: bound server + real protocol client observes `shulker_bullet` spawn, production-ticker homing, 4-damage health publication, Levitation id `24` for `200` ticks and bullet removal. `mc-net shulker_` covers retained attack cadence, owner/target binding and accepted entity-hit Levitation state. | Production AI -> homing projectile -> collision -> damage/effect -> discard path is proven. Exact vanilla axis/obstacle rerouting and server-side non-player Levitation movement remain explicit residuals. |
| Evoker / fangs | `mc-test-harness --test evoker_presence`: bound server + real protocol client observes `evoker_fangs` spawn, spike event `4`, 6-damage health publication and fang removal. `mc-net evoker_` pins 20-tick warmup, 40-tick casting window, 100-tick interval, 13-fang close / 16-fang line patterns and fang lifecycle. | Production primary combat spell is proven. Summon Vex, Wololo, casting presentation and exact vanilla sturdy-ground downward placement remain explicit residuals. `evoker_fangs` uses immobile physics rather than generic living gravity. |
| Ghast / large fireball | `mc-test-harness --test ghast_presence`: bound server + real protocol client observes `fireball` spawn, direct player damage, `ClientboundExplode` with radius `1`, and projectile removal. Focused `mc-net` tests pin 20-tick charge, reset to `-40`, owned generic hurting-projectile state, direct 6 damage and pending `MOB` explosion power `1`. | Production primary ranged combat path is proven through the shared projectile/explosion authority. Charging metadata, level events/sounds, reflected-fireball 1000-damage special-case and exact LOS/presentation remain explicit residuals. |
| Breeze / wind charge | `mc-test-harness --test breeze_presence`: bound server + real protocol client observes `breeze_wind_charge` spawn, direct 1-damage health publication, `ClientboundExplode` radius `3` with `block_count=0`, and projectile removal. `mc-net breeze_` pins 15-tick charge, 4-tick recovery, 10-tick cooldown, speed `0.7`, acceleration `0`, inertia `1`, and `TRIGGER` explosion power `3` without block/explosion damage. | Production primary ranged combat path is proven. Difficulty-dependent projectile uncertainty, shooting pose/sounds, exact block-hit face-normal explosion offset, wind-charge block-trigger details and player knockback publication remain explicit residuals. |
| Witch / splash potion | `mc-test-harness --test witch_presence`: bound server + protocol client observes `splash_potion` spawn, production throwable flight/impact, vanilla-radius status splash with duration falloff, effect packet and projectile removal. `mc-net witch_` pins the 60-tick `RangedAttackGoal` cadence, owner binding, Slowness selection at the fixed distant target, throwable CAS path and 6-damage Harming direct hit. | Production primary player-target ranged path is proven. Runtime movement may legitimately change Slowness to Poison before the throw. The vanilla 25% close-range Weakness RNG branch, Raider healing/regeneration targets, self-drinking potion AI, full non-player splash AOE and potion presentation remain explicit residuals. |
| Wither / wither skull | `mc-test-harness --test wither_presence`: bound server + protocol client observes `wither_skull` spawn, 8 direct damage, `MOB` explosion radius `1` and projectile removal. `mc-net wither_` proves 20-block central-head target selection, 40-tick ranged cadence, owner binding and shared hurting-projectile/explosion authority. | Production primary ranged combat path is proven. The 220-tick spawn invulnerability/explosion, side-head independent targeting, dangerous skull RNG/resistance override, NORMAL/HARD Wither status effect, healing-on-kill, regeneration, powered projectile immunity, boss bar and block-destruction lifecycle remain explicit boss residuals. |
| Ender Dragon / D1 air combat | `mc-test-harness --test dragon_presence`: bound server + protocol client observes authoritative Dragon movement, `dragon_fireball` spawn, `area_effect_cloud` spawn, breath-cloud player damage and fireball removal. `mc-net dragon_` proves the dedicated flight owner, owner-bound fireball/cloud path, vanilla multipart id reservation (`parent+1..+8`), part-aware player damage, lethal `Dying` transition, 200-tick death removal and fight-less 500-XP schedule. `mc-entity dragon_26_1_2` pins the D1 phase/damage/contact/steering/death rules. | The command-summoned **D1 flying combat loop** is production-proven and does not use generic pursuit/melee. Its bounded divergence is explicit: D1 uses a deterministic eight-point air orbit instead of the End fight's 24-node A* ring and suppresses landing/perch transitions. End dimension/fight/crystals, landing/sitting/flame cycle, boss bar, terrain griefing/`inWall`, exact wing push/per-victim contact timestamp, full non-player breath-cloud AOE, phase/sound/particle presentation and restart-mid-flight phase identity remain boss/rare residuals. Multipart attack routing is owner-tested against reserved vanilla `parent+1..+8` ids; the current raw-TCP Dragon gate does not independently drive a vanilla-client attack at a part id. |
| Despawn | Item despawn deadline raw-TCP save/restart gate; dying-entity lifecycle tests and entity visibility removal coverage | Common lifecycle/despawn authority is proven for current implemented entities. |
| Drops / pickups | Zombie raw-TCP rotten-flesh drop, item merge, partial pickup/overflow, death inventory/XP conservation and restart gates | Common drop/pickup authority is proven. Species-specific loot completeness remains data/parity work. |
| Restart identity | Generated-village villager restart UUID gate, natural-spawn deterministic identity checkpoint/replay fence, item/entity persistence suites, village-defence restart dedupe | Current deterministic/runtime identities survive the exercised restart paths without duplicate materialization. |

## Current code blockers

No species in `mc-data::mob_behavior_26_1_2` remains on
`MobCombatPolicy::UnsupportedSpecial`. All previously enumerated special-combat
species now have an explicit production authority path rather than falling through
to generic melee. The remaining gaps in this matrix are deliberate species/boss
residuals documented per row, not an unowned common-combat fallback.

## Fresh evidence from this checkpoint

- `cargo test -p mc-test-harness --test warden_presence -- --nocapture` — PASS (`1/1`, ~2.40 s).
- `cargo test -p mc-net warden_sonic_boom_charges_damages_and_cools_down -- --nocapture` — PASS (`1/1`).
- `cargo test -p mc-test-harness --test blaze_presence -- --nocapture` — PASS (`1/1`, ~4.6 s in this checkpoint).
- `cargo test -p mc-net small_fireball --quiet` — PASS (`3/3`).
- `cargo test -p mc-test-harness --test natural_mob_despawn -- --nocapture` — PASS (`1/1`, ~0.96 s).
- `cargo test -p mc-test-harness --test shulker_presence -- --nocapture` — PASS (`1/1`, ~3.38 s runtime after build).
- `cargo test -p mc-net shulker_ -- --nocapture` — PASS (`2/2` focused attack/collision semantics; homing query and authoritative revision are fenced together).
- `cargo test -p mc-test-harness --test evoker_presence -- --nocapture` — PASS (`1/1`, ~2.80 s runtime after build).
- `cargo test -p mc-net evoker_ -- --nocapture` — PASS (`1/1`, focused spell/fang lifecycle).
- `cargo test -p mc-test-harness --test ghast_presence -- --nocapture` — PASS (`1/1`, ~2.11 s runtime after build).
- `cargo test -p mc-net ghast_ -- --nocapture` — PASS (`1/1`, focused 20-tick charge and owned large-fireball spawn).
- `cargo test -p mc-net large_fireball_hit_deals_six_damage_and_arms_mob_explosion -- --nocapture` — PASS (`1/1`).
- `cargo test -p mc-test-harness --test breeze_presence -- --nocapture` — PASS (`1/1`, ~2.04 s runtime after build; player knockback publication remains a stated residual).
- `cargo test -p mc-net breeze_ -- --nocapture` — PASS (`2/2`, focused shoot state and trigger-explosion semantics).
- `cargo test -p mc-test-harness --test wither_presence -- --nocapture` — PASS (`1/1`, ~3.16 s runtime after build).
- `cargo test -p mc-net wither_ -- --nocapture` — PASS (`2/2`, central-head spawn plus 8-damage/explosion semantics).
- `cargo test -p mc-test-harness --test witch_presence -- --nocapture` — PASS (`1/1`, ~4.03 s runtime after build).
- `cargo test -p mc-net witch_ -- --nocapture` — PASS (`2/2`, 60-tick selection/spawn plus throwable Harming collision semantics).
- `cargo test -p mc-entity dragon_26_1_2 -- --nocapture` — PASS (`8/8`, bounded D1 phase, steering, part geometry/damage/contact and fight-less death schedule).
- `cargo test -p mc-net dragon_ -- --nocapture` — PASS (`5/5`, multipart-id reservation, dedicated air owner/fireball, fireball→cloud, cloud lifecycle and part-aware Dying/XP path).
- `cargo test -p mc-test-harness --test dragon_presence -- --nocapture` — PASS (`1/1`, ~2.26 s runtime; Dragon movement → `dragon_fireball` → `area_effect_cloud` → player damage → fireball removal over bound-server TCP).
- `cargo test -p mc-net ordinary_hostile_melee_tick_never_waits_for_session_registry -- --nocapture` — PASS (`1/1`, Dragon/cloud authority does not regress ordinary hostile lock independence).
- `cargo test -p mc-data mob_behavior_26_1_2 --quiet` — PASS (`2/2`; Dragon uses `DragonBoss`, no special species is left on `UnsupportedSpecial`).
- `cargo clippy --workspace --all-targets -- -D warnings` — PASS on the current Dragon tree with no warnings.
- `cargo run -p xtask -- code-health` — PASS (`0 fail / KEEP`) on the current Dragon tree.
- `cargo test -p mc-net --lib --quiet` — PASS (`1960 passed / 0 failed / 5 ignored`).
- `cargo test --workspace --quiet` — PASS across the current workspace with zero failures; ignored sidecar/manual gates remain ignored by their existing explicit contracts.
- `cargo test -p mc-test-harness --test guardian_presence -- --nocapture` — PASS (`1/1`, ~5.2 s in the preceding checkpoint).
- `cargo test -p mc-test-harness --test village_defense_presence -- --nocapture` — PASS (`1/1`, ~6.4 s after fixture tightening).
- `cargo test -p mc-test-harness --test villager_schedule_presence -- --nocapture` — PASS (`1/1`, ~1.62 s).
- `cargo test -p mc-test-harness --test entity_parity_26_1_2 solaris_entity_scenarios_emit_normalized_or_explicitly_degraded_evidence -- --nocapture` — PASS (`1/1`).
