# Solaris code-review remediation plan

Date: 2026-07-31

Quality label: `stabilization`

Source bundle: `tmp/SOLARIS_CODE_REVIEW_BUNDLE/`

Source snapshot SHA-256: `1770a1bca05f435e9c242a7d2276e9b9842db7c95b1c5fde6ef28c7d57983185`

## Scope and evidence boundary

The Pro review is a static analysis of a Repomix subset. It did not include every ignored/generated/data file, root workspace manifests, dependency lock state, CI policy, runtime configuration, live clients, profilers, fault injection, or cargo validation. Its findings are therefore a remediation input, not executable evidence by themselves.

Every finding must pass current-tree triage before code changes:

1. reproduce or confirm the stated control/data flow in the current checkout;
2. check whether later local commits already fixed or narrowed it;
3. add a focused failing regression when practical;
4. repair the shared primitive rather than one call site;
5. run the affected package gates and the validation tier required by `AGENTS.md`;
6. record remaining deployment, oracle, client, performance, concurrency, and recovery gaps honestly.

The bundle contains 42 findings: 11 High, 26 Medium, and 5 Low. The review marks 27 as release blockers. Public exposure and write-format migration remain stabilization-only until the confirmed blocker set is closed or explicitly accepted by the owner.

## Main conclusions

The strongest parts of Solaris remain its explicit ownership direction, bounded queues in several subsystems, lack of `unsafe`, deterministic evidence culture, and focused runtime tests. The review identifies a repeated cross-cutting weakness: important invariants are enforced locally instead of at shared boundaries.

The remediation program therefore prioritizes common primitives:

- exact packet decode before liveness credit;
- absolute state-machine deadlines and bounded unauthenticated admission;
- parser allocation/byte/node budgets;
- validated resource paths and canonical identifiers;
- storage coordinate/size invariants before mutation;
- authority-side movement validation;
- bounded actor/plugin delivery and typed fail-stop supervision.

## Ordered work waves

### Wave A — Canonical data and parser safety

Close small, reusable invariant primitives first because they reduce the blast radius of later storage and loader work.

- `SOL-023`: canonical `Identifier` serde; strict legacy object migration.
- `SOL-024`: separate validated resource path from logical identifier; prevent traversal and symlink escape.
- `SOL-016`: journal semantic image/frame/file budgets before allocation.
- `SOL-035`: bounded count-prefixed vector decoding based on remaining wire bytes.
- `SOL-009`: aggregate NBT byte/node/string/entry budgets and writer rollback.
- `SOL-021`: bounded playerdata/structure decompression with exact consumption.
- `SOL-032`: iterative bounded data-sidecar traversal with explicit symlink policy.

Exit condition: malformed tiny inputs cannot trigger disproportionate allocation, panic, or path escape in the covered primitives.

### Wave B — World persistence integrity

- `SOL-013`: bounded Anvil region/chunk decompression and aggregate memory.
- `SOL-014`: reject invalid coordinates, duplicate slots, and chunks over 255 sectors without modifying output.
- `SOL-015`: validate reserved sectors, extents, overlap, and codec checksums.
- `SOL-020`: enforce `requested/cache key == decoded chunk.pos == serialized expected key`.
- `SOL-017`: acquire an OS-level exclusive lease for a writable world root.
- `SOL-027`: enforce resident/dirty byte budgets and backpressure when save is unhealthy.
- `SOL-040`: centralize cross-platform atomic replace and durability policy.

Exit condition: corrupt/imported files fail closed or quarantine, failed writes leave prior files intact, and two writable processes cannot share a world root.

### Wave C — Network liveness and admission

- `SOL-001`: only exact, recognized decoded packets count as inbound liveness.
- `SOL-004`: exact Configuration packet bodies, including empty ACKs.
- `SOL-003`: absolute handshake/Configuration/Loader deadlines and bounded unauthenticated permits.
- `SOL-028`: weighted per-session packet/byte rate budgets.
- `SOL-029`: deadlines and shutdown/owner-health selection for simulation queue and replies.
- `SOL-007`: bounded Mojang response reads and correct upstream-status classification.
- `SOL-008`: canonical compression framing, strict stream completion, and CPU budgeting.
- `SOL-030`: canonical login-name validation and identity collision policy.

Exit condition: slow or malformed clients cannot retain slots indefinitely, monopolize permits, or block the connection loop behind an unhealthy owner.

### Wave D — Runtime ownership and fail-stop behavior

- `SOL-002`: close-safe async extension boundary; no lost close wakeups.
- `SOL-012`: bounded committed-event bridge with explicit required/best-effort policy.
- `SOL-031`: typed owner fatal state and coordinated fail-stop instead of panic propagation.
- `SOL-041`: classify poisoned locks; reset benign telemetry only, fail-stop authoritative state.
- `SOL-037`: remove stale admission tombstones with RAII cancellation.
- `SOL-039`: strict production plugin deployment mode and expected plugin set.
- `SOL-038`: aggregate plugin wall-clock/fairness budget.

Exit condition: owner/plugin failure completes pending work with typed errors, stops new mutation, and enters a controlled drain/shutdown path without indefinite waiters.

### Wave E — Authority and gameplay correctness

- `SOL-019`: authority-side displacement budget, swept collision, loaded destination, and teleport exceptions.
- `SOL-025`: preserve valid partial horizontal travel and report collision separately.

Exit condition: a client cannot choose arbitrary movement, while legitimate lag/knockback/swim/vehicle/teleport paths remain explicitly modeled.

### Wave F — Loader, data correctness, and architecture debt

- `SOL-005`: bind Loader artifact digest to immutable/opened bytes.
- `SOL-006`: validate ACK state IDs and carrier family against the negotiated registry snapshot.
- `SOL-018`: either implement dependency/load-order graph validation or remove unsupported contract fields.
- `SOL-022`: validate resource-heavy configuration maxima and checked derived budgets.
- `SOL-010`, `SOL-011`, `SOL-026`, `SOL-033`, `SOL-034`, `SOL-036`: harden public codec/physics/recipe/tag/item-stack contracts.
- `SOL-042`: extract typed dispatch/deadline/transaction middleware after characterization tests; do not mechanically split files without ownership boundaries.

## Initial status

| Finding | State | Evidence / next action |
| --- | --- | --- |
| `SOL-023` | complete | Derived serde was replaced by canonical string serialization. The legacy `{full,colon}` object is accepted only when both fields exactly match the value re-parsed through `Identifier::parse`; impossible indices, invalid characters, non-canonical bare paths, and extra fields fail closed. `mc-data`, `mc-protocol`, `mc-world`, `mc-script`, and `mc-worldgen` package tests plus workspace Clippy pass. |
| `SOL-024` | complete | Added a shared `ResourcePath` boundary that rejects empty/dot/absolute/platform-ambiguous segments. Registry Network-NBT, entity-loot references, configured worldgen features, and ore features now read from an already-opened descriptor whose OS identity is compared with the post-open canonical target under the trusted root. The independent reviewer initially blocked the canonicalize-then-reopen TOCTOU; that path was removed and a deterministic symlink-swap regression now fails closed through `same-file` identity checks. Primitive plus real loader traversal/symlink tests pass; the only remaining direct `join(identifier.path())` occurrence is a trusted test fixture writer. No second reviewer was run, per `AGENTS.md`. |
| `SOL-016` | complete | World-journal recovery validates image count against a 512-image semantic ceiling and remaining payload before allocation, caps each image NBT at 16 MiB, frame payloads at 64 MiB, the journal file at 256 MiB, and pending decisions at 65,536. Encoding preflights aggregate append/replacement size and uses fallible reservations. Recovery reads an exact bounded file length and repairs an incomplete tail through the same open handle. The writer owns an exclusive cross-process `fd-lock` lease, so size/header mutation is serialized; a reserved append that cannot complete enters fail-stop and wakes later waiters. The single reviewer initially returned `BLOCKED` for waiter deadlock, file-growth races, `MAX+1` recovery allocation, pathname reopen during repair, and aggregate buffer growth. All findings received direct regressions; no second reviewer was run per `AGENTS.md`. Focused journal tests pass 29/29 and full `mc-net` passes 1,863 tests plus 3 additional tests, with 5 ignored. Workspace Clippy, formatter, code-health, and diff-check pass. The final `cargo test --workspace` attempt was externally SIGTERM after 58 seconds without a reported failed test and is not counted as a complete workspace gate. |
| `SOL-035` | complete | Added shared `read_bounded_count`, `read_bounded_vec`, and bounded initial-capacity primitives for VarInt-counted collections. Counts are checked for negativity, semantic maxima, and `count × conservative minimum wire size` feasibility before the first collection allocation; multiplication and `needed` arithmetic saturate safely, and initial capacity is capped at 1,024 while valid larger collections grow after successful element decoding. Login profile properties, Play dimension/player/entity/command/chunk/light/recipe collections, Configuration registries/tags/known packs, merchant offers, and entity-attribute collections were migrated. The residual allocation inventory contains no eager `with_capacity`/`reserve` from wire counts outside the helper. Production malformed-frame regressions cover Login properties, dimension names, one-million chunk block entities, and one-million tag entries; helper regressions cover negative/over-max/tiny-body/overflow/bounded-capacity/valid-large cases. `mc-protocol` passes 314 tests, `mc-net` passes 1,863 plus 3 additional tests with 5 ignored, all `mc-server` suites pass, and the full `mc-test-harness` suite passes. Workspace Clippy, formatter, code-health, and diff-check pass. The final `cargo test --workspace` attempt was externally SIGTERM after 61 seconds without a reported failed test and is not counted as a complete workspace gate. The single bounded reviewer attempt was externally terminated before producing a new verdict; its output file remained the stale prior `SOL-016` review, so no second reviewer was started per `AGENTS.md`. |
| `SOL-009` | complete | Named and Network NBT now use one non-recursive explicit-stack reader, validator, and writer traversal. Existing 16 MiB per-collection and 512-level depth limits remain, with new aggregate ceilings of 64 MiB wire bytes, 1,048,576 tag nodes, 65,536 entries per compound, 1,048,576 compound entries per root, 16 MiB Modified UTF-8 payload, and 64 MiB decoded allocation. Reader allocation accounting covers requested and allocator-returned capacities for frame stacks, lists, compound growth, arrays, string input/code units, and decoded strings; count feasibility runs before list allocation. Modified UTF-8 size arithmetic is checked, duplicate compound names remain ordered and preserved, and public writers preflight the complete tree before the first caller-buffer write. The single saved reviewer session `019fb785-cc73-73f2-991e-5942147d12ea` returned `BLOCKED` for uncharged capacity/frame growth, unchecked string-length accumulation, and missing depth/list/allocation boundaries. All three findings were fixed with direct regressions; no second reviewer was run per `AGENTS.md`. `mc-nbt` passes 36/36. Core data/protocol/world/worldgen/entity suites, `mc-net` (1,863 plus 3 additional tests, 5 ignored), all `mc-server` suites, and the full `mc-test-harness` suite pass. Workspace Clippy, formatter, code-health, and diff-check pass. The final `cargo test --workspace` attempt was externally SIGTERM after 73 seconds without a reported failed test and is not counted as a complete workspace gate. |
| `SOL-021` | complete | Player/entity/world persistence gzip and raw/gzip structure templates now cap compressed or raw input and decompressed NBT at 64 MiB. Both loaders use a fixed 8 KiB counting pass followed by one fallible exact allocation and a second read/decode pass, avoiding `read_to_end` capacity amplification. Persistence files are counted, rewound, and reread through the same opened descriptor; shrink fails `read_exact`, growth is rejected by an EOF probe, and later pathname replacement cannot swap the pinned file object. Gzip decoding uses `bufread::GzDecoder<Cursor<&[u8]>>`; the consumed cursor position must equal the full compressed slice on both passes, enforcing exactly one member without relying on decoder buffering. Named NBT must consume the entire decompressed or raw payload. Exact limit, limit+1, truncated gzip, trailing decompressed bytes, and concatenated-member regressions pass in both crates, including a 128 KiB incompressible first member through the public structure loader. Corrupt playerdata remains a typed per-connection `InvalidData` failure rather than a process-wide panic. Saved reviewer session `019fb7b9-9804-7500-969f-96070d5279a0` returned `BLOCKED` for ambiguous trailing-member detection, `read_to_end` peak allocation, and unclear file replacement/growth semantics; all three were addressed without a second reviewer per `AGENTS.md`. Full `mc-net` passes 1,866 tests plus 3 additional tests with 5 ignored; `mc-worldgen` passes 109 tests plus its binary/integration suites with 5 ignored; all `mc-server` and `mc-test-harness` suites pass. Workspace Clippy, formatter, code-health, and diff-check pass. The final `cargo test --workspace` attempt was externally SIGTERM after 56 seconds without a reported failed test and is not counted as a complete workspace gate. |
| `SOL-032` | complete | Added one shared sidecar boundary for recursive registry/tag/recipe/biome/loot/item-component/damage traversal, flat worldgen/report discovery, top-level JSON reports, captured registry payloads, and worldgen inventory. Directory walking is iterative and lexicographically deterministic, rejects symlinks and unsupported filesystem entries, validates every directory component below the trusted sidecar root, and enforces 32 levels, 131,072 visited entries, 65,536 matching files, 16 MiB per file, and 128 MiB aggregate matching bytes before visitor work. A single directory cannot be collected past the entry ceiling, and directory-entry plus file-buffer reservations are fallible. Direct reads reject symlinks, compare the opened descriptor with the current pathname, allocate the exact metadata length fallibly, fail on shrink/partial read, and reject growth with an EOF probe. Focused regressions cover deterministic order; exact/over depth, entry, file-count, per-file, and aggregate-byte limits; file symlink escape; directory symlink loops; symlinked ancestors under the trusted root; exact-size reads; and injected partial-read failure. Public registry and worldgen-inventory loaders reject symlinked entries, ancestors, and optional areas. Full `mc-data` passes 218 library tests plus 11 integration tests, with 25 ignored; opt-in real-sidecar registry and worldgen-inventory tests pass against the local 26.1.2 extraction (7,842 files, about 15.1 MiB total, largest file about 6.24 MiB). Package and workspace Clippy, formatter, and code-health pass. The sole independent reviewer handoff was written, but the local handoff runner remained in `unknown` state and produced no verdict; no second reviewer was started per `AGENTS.md`. The final `cargo test --workspace` attempt was externally SIGTERM after 71 seconds without a reported failed test and is not counted as a complete workspace gate. |
| `SOL-013` | complete | The Anvil reader now opens one descriptor, reads only the 8 KiB header, seeks to bounded chunk allocations, and rejects files above the maximum location-table sector geometry before body reads. Compressed payload allocation is bounded by the slot's `u8` sector count. Gzip and zlib use fixed-buffer bounded counting followed by one fallible exact allocation; zlib additionally requires explicit `StreamEnd`, full input consumption, and exact output length. Raw and compressed LZ4 validate every block header and aggregate declared output before one exact allocation, then decode directly into the bounded destination. Each chunk is capped at 64 MiB and full-region decoded work/retention at 256 MiB. Public `visit_region` permits streaming consumption, while the read-view disk path uses a single-slot reader and no longer decodes unrelated chunks; a corrupt unrelated slot does not block a valid target slot, while full-region reads remain fail-closed. Focused tests cover all four codecs at exact/over limits, declared LZ4 bombs, aggregate multi-chunk limits before the second allocation, oversized region metadata, truncated/trailing streams, truncated slot extents, visitor ordering, invalid local coordinates, and isolated target reads. Full `mc-world` passes 227 tests with 15 ignored; real local vanilla region read and byte-for-byte round-trip gates pass. Package and workspace Clippy, formatter, code-health, and diff-check pass. The sole independent reviewer handoff was written, but the local handoff runner remained in `unknown` state and produced no verdict; no second reviewer was started per `AGENTS.md`. The final `cargo test --workspace` attempt was externally SIGTERM after 61 seconds without a reported failed test and is not counted as a complete workspace gate. |
| `SOL-014` | complete | The Anvil writer now preflights every chunk before compression or any output create/truncate: local coordinates must be within `0..32`, slots must be unique, and uncompressed per-chunk/aggregate bytes must fit the same 64 MiB/256 MiB budgets enforced by the reader. Zlib output is measured with checked layout arithmetic; 255 sectors is accepted exactly, while 256 or 257 sectors returns typed `ChunkTooLarge` instead of truncating the location-table count through `& 0xFF`. Region offsets and image sizes are checked, body/image reservations are fallible, and the location count is written losslessly. All validation and encoding complete in memory before `File::create` or `create_new`, so invalid coordinates, duplicate slots, and oversized incompressible payloads leave an existing output byte-for-byte unchanged; validation failure on `write_region_create_new` creates no file. Focused Anvil tests pass 16/16 with 4 ignored, including exact 255/256/257 boundaries, public incompressible payloads, reader-budget symmetry, and unchanged-output assertions. Full `mc-world` passes 232 tests with 15 ignored, including dirty-flush; real local vanilla region read and byte-for-byte round-trip gates pass. Package and workspace Clippy, formatter, code-health, and diff-check pass. The sole independent reviewer handoff was written, but the local handoff runner remained in `unknown` state and produced no verdict; no second reviewer was started per `AGENTS.md`. The final `cargo test --workspace` attempt was externally SIGTERM after 66 seconds without a reported failed test and is not counted as a complete workspace gate. |
| `SOL-015` | complete | `RegionReader::open` now validates the complete 1,024-entry location table before any chunk decode or visitor callback. Non-empty locations must have a nonzero count, begin after the two reserved header sectors, have a checked full allocated extent within the opened file, and not overlap any earlier slot; structural errors include the region path and both affected local slots. The validated fixed-size location map is then reused by single-slot and full-region reads, so unrelated payload corruption remains isolated for `read_chunk`, while ambiguous structural corruption fails the whole region before partial acceptance. LZ4Block headers now parse and verify their little-endian xxHash32 checksum over each decoded block using the format seed `0x9747B28C`; raw and compressed blocks plus the zero-checksum end marker fail closed on mismatch. Focused tests cover reserved sectors, zero counts, truncated extents, overlap with zero visitor calls, 512 deterministic fuzzed location tables against an interval oracle, raw/compressed checksum corruption, end-marker corruption, and standard xxHash32 vectors. Focused Anvil tests pass 21/21 with 4 ignored; full `mc-world` passes 237 tests with 15 ignored; real local vanilla region read and byte-for-byte round-trip pass. The opt-in real vanilla LZ4 oracle is absent in this checkout and could not run. Package and workspace Clippy, formatter, code-health, and diff-check pass. The sole independent reviewer handoff was written, but the local handoff runner remained in `unknown` state and produced no verdict; no second reviewer was started per `AGENTS.md`. The final `cargo test --workspace` attempt was externally SIGTERM after 68 seconds without a reported failed test and is not counted as a complete workspace gate. |
| `SOL-020` | complete | Production chunk loading now preserves the absolute requested key through both synchronous `WorldStorage` and background `ChunkDiskLoadPlan` paths, parses exactly one complete NBT root, and rejects embedded `xPos`/`zPos` that differ from the selected region slot before cache publication. Generated and committed chunks pass a final `cache key == Chunk.pos` barrier; resident insertion/replacement retain debug assertions at the lower ownership boundary. Dirty-flush planning and writing independently verify each resident key/snapshot pair, pass the expected absolute position to the serializer, and derive the destination local slot from the planned key rather than internal chunk state. A forged mismatched plan fails before temp creation and leaves the existing region byte-for-byte unchanged. The explicit offline `repair_chunk_nbt_position` primitive canonicalizes missing, wrongly typed, duplicated, or mismatched coordinate fields but is never invoked by runtime load. Regressions cover same-region and cross-region coordinate substitution, sync and read-view loads, exact NBT consumption, cache insertion/commit, expected-key serialization, repair idempotence, and dirty-flush no-write behavior. Full `mc-world` passes 246 tests with 15 ignored; read-view passes 15/15 and dirty-flush 24/24; the real local vanilla world opens and serves block queries through the strict path. Package and workspace Clippy, formatter, code-health, and diff-check pass. The sole independent reviewer handoff was written, but the local handoff runner remained in `unknown` state and produced no verdict; no second reviewer was started per `AGENTS.md`. The final `cargo test --workspace` attempt was externally SIGTERM after 86 seconds without a reported failed test and is not counted as a complete workspace gate. |
| `SOL-017` | complete | Writable `WorldStorage::open*` now canonicalizes the world root and acquires one process-reentrant OS exclusive lease on `.solaris-world.lock` before region data access. The guard is retained inside every storage handle for the full runtime lifetime; same-process opens share it through a `Weak` registry, while another process fails fast with `WorldLocked`. The holder writes bounded diagnostic metadata (`pid`, `started_unix_ms`, `instance`) only after acquiring the lease, preserves it for contenders, and overwrites stale metadata after crash recovery. `open_read_only` deliberately skips the writable lease, while every dirty-flush plan rejects read-only storage before snapshot or file work. One event-driven subprocess regression covers competing writers, holder diagnostics, distinct world roots, read-only tooling, forced process termination, stale metadata, and immediate reacquisition without sleeps. Full `mc-world` passes 248 tests with 15 ignored; all `mc-server` suites pass. Package and workspace Clippy, formatter, code-health, and diff-check pass. The sole independent reviewer handoff was written, but the local handoff runner remained in `unknown` state and produced no verdict; no second reviewer was started per `AGENTS.md`. The final `cargo test --workspace` attempt was externally SIGTERM after 67 seconds without a reported failed test and is not counted as a complete workspace gate. |
| `SOL-027` | complete | Chunk admission now enforces both a hard resident-count ceiling and conservative heap-byte ceilings. `Chunk::estimated_heap_bytes` accounts for section palettes/packed arrays, biome identifiers, heightmaps, block entities, typed inventories, scheduled ticks, baked light, extras, and mutation maps. Default resident budgets reserve headroom for flush work; dirty bytes have a lower ceiling. Every new disk/generated/journal chunk passes the same admission gate, which evicts clean chunks when possible and otherwise returns typed `ChunkCachePressure` instead of growing an all-dirty cache. Synchronous save failures mark the pipeline unhealthy and block further admission while dirty state remains; a successful flush clears the health fence. Stats expose resident/dirty bytes, budgets, and health. Regressions cover all-dirty hard cap, oversized resident/dirty payloads, unhealthy-save fail-fast and recovery, clean eviction, and no implicit flush under insertion. Full `mc-world` passes 252 tests with 15 ignored; package Clippy and formatter pass. |
| `SOL-040` | complete | Added one cross-platform `mc_world::atomic_file` boundary for same-directory replacement, temp cleanup, and parent durability. Unix uses atomic rename plus directory `sync_all`; Windows uses `MoveFileExW` with `MOVEFILE_REPLACE_EXISTING | MOVEFILE_WRITE_THROUGH`, so repeated saves replace existing targets instead of returning the old unsupported error. Region dirty-flush retains stale-version rechecks and no-overwrite hard-link installation for new regions, while existing-region replacement uses the shared boundary. Playerdata, regional entity journal compaction, world-journal checkpoint replacement, and persisted world-contract writes now use the same replacement primitive. Focused tests cover missing/existing targets and temp cleanup on failure; dirty-flush 24/24, world-journal 29/29, persistence 87/87 with one ignored, and world-contract 5/5 pass. Package Clippy for `mc-world`, `mc-net`, and `mc-server` passes. The Windows target is not installed in this checkout, so native Windows compilation could not be run locally. |
| `SOL-001` | complete | Every recognized serverbound Play packet is now decoded against an immutable body clone and must consume the entire body before it can refresh inbound liveness. Unknown IDs remain ignored but do not extend keepalive deadlines. Intentionally ignored movement packets before `PlayerLoaded` or while a teleport is pending still pass full syntactic validation before the dispatch branch may continue. A mismatched keepalive ID is syntactically valid activity but does not clear the pending request. Focused regressions cover unknown IDs, trailing keepalive bytes, truncated ignored movement, valid non-keepalive activity, exact keepalive bodies, and overdue timeout behavior. Full `mc-net` passes 1,871 tests plus 3 integration tests with 5 ignored; package Clippy passes. |
| `SOL-004` | complete | Configuration now uses one exact packet decoder for every recognized serverbound packet body. Known Packs, Client Information, and Resource Pack status must consume the entire frame in all three handshake phases. Acknowledge Finish Configuration is an explicit empty-body marker and rejects any byte. Brand custom payloads must consume their complete inner payload; loader and extension payloads retain their existing bounded exact codecs or intentionally own the remaining bytes. Unknown Configuration packet IDs keep the existing bounded skip policy. Focused tests cover trailing bytes for Known Packs, Client Information, Resource Pack, brand, and Finish ACK plus the valid empty ACK. Configuration unit tests pass 11/11, the full Configuration integration suite passes 14/14, and package Clippy passes. |
| `SOL-003` | complete | A single 45-second absolute deadline now covers Handshake, Login/Transfer, Mojang verification, Configuration, and Solaris Loader; the existing 10-second read timeout remains a progress deadline, and Loader no longer receives a separate 120-second allowance. One shared pre-Play budget caps complete inbound frames at 96 packets and 512 KiB across the whole state chain. The accept loop uses a separate smaller pre-auth semaphore plus a four-connection per-IP cap; its RAII permit is released immediately after successful Configuration, before entering Play, and is recovered on timeout, rejection, disconnect, or task cancellation. Typed errors distinguish absolute deadline, packet-budget, and byte-budget failures. Fake-time tests prove intermediate progress cannot extend the absolute deadline; exact/max+1 packet and byte tests, global/per-IP exhaustion, permit recovery, limit scaling, full `mc-net` (1,881 passed plus 3 integration tests, 5 ignored), and all `mc-server` suites pass. Oversized Play custom payloads retain their bounded-ignore policy without refreshing liveness. Package and workspace Clippy, formatter, code-health, and diff-check pass. The sole independent reviewer handoff was written, but the local handoff runner remained in `unknown` state and produced no verdict; no second reviewer was started per `AGENTS.md`. The final `cargo test --workspace` attempt was externally SIGTERM after 68 seconds without a reported failed test and is not counted as a complete workspace gate. |
| `SOL-028` | complete | Play now has one per-session ingress limiter before liveness and handler dispatch. Integer token buckets enforce total packet rate, total body bytes, weighted work, and dedicated chat, command, suggestion, and custom-payload budgets with explicit burst allowances and monotonic refill. Movement and keepalive remain cheap; command suggestions and custom payload bytes receive higher independent costs. A violation is structurally logged with packet ID, reason, streak, and per-class count; the first two packets are dropped without refreshing liveness, while the third violation within ten seconds returns typed `PlayRateLimitExceeded`. The streak resets after a quiet interval. Deterministic injected-time tests cover normal movement, allowed bursts, sustained packet flood, expensive suggestions, custom payload bytes, refill, reset, and cheap keepalive. Full `mc-net` passes 1,887 tests plus 3 integration tests with 5 ignored; Play integration passes 16/16 and package Clippy passes. |
| `SOL-029` | complete | Every simulation request now crosses one bounded owner boundary. Queue admission uses a 250 ms deadline instead of unbounded `sender.reserve().await`; timeout returns `QueueAdmissionTimeout` and increments dedicated metrics. Every response receiver carries a five-second deadline from enqueue time, observes atomic owner health plus a wake notification, and returns typed `ResponseTimeout`, `ShuttingDown`, or `OwnerStopped`; connection/task cancellation drops the receiver so the owner skips cancelled work. Owner shutdown/drop transitions health before closing and draining the queue, and new requests after owner loss preserve the owner-state error instead of collapsing to generic `Closed`. The raw oneshot receiver exists only inside the deadline-aware wrapper. The old synchronous receiver compatibility was removed: 58 simulation tests were converted to async, iterator closures were rewritten as explicit async loops, and blocking Tokio locks became awaited locks. Regressions cover full-queue admission timeout, response timeout, valid response, owner shutdown, owner drop, pending cancellation, typed metrics, and prior regional/container/block-drop invariants. Full `mc-net` passes 1,892 tests plus 3 integration tests with 5 ignored; Play integration passes 16/16 and package Clippy passes. |
| `SOL-007` | complete | Mojang `hasJoined` responses are classified by HTTP status before body collection: 5xx and unexpected non-success states are `Unavailable`, while 204 and 4xx remain authentication rejection. Successful bodies are streamed through a 64 KiB ceiling with both `Content-Length` preflight and checked chunk accumulation, so missing length cannot bypass the cap. Successful JSON must contain a parseable UUID and returned profile name matching the requested username case-insensitively; the returned canonical casing becomes the LoginSuccess/session name. Profile properties are limited to 16 entries with 64-byte names and 16 KiB value/signature ceilings. Malformed 2xx, oversized bodies, invalid UUID/name shape, and excessive properties are treated as upstream failure, while a mismatched identity is `Unverified`. Regressions cover 204/4xx, 503 HTML, malformed 2xx, exact/over body limits, no-length streaming overflow, name mismatch/casing, null properties, count limits, and oversized values/signatures. Session-auth tests pass 14/14, full `mc-net` passes 1,896 tests plus 3 integration tests with 5 ignored, encrypted Login integration passes, and package Clippy passes. |
| `SOL-008` | complete | Compression framing now separates lightweight outer-frame parsing from owned compressed decode plans. A raw `data_length = 0` frame is accepted only when the complete uncompressed packet is strictly below the negotiated threshold; raw-at-threshold returns `UncompressedAboveThreshold`, while positive compressed lengths below threshold remain rejected. Zlib decoding uses an exact `claimed + 1` output buffer and `flate2::Decompress`: output length must match, status must be `StreamEnd`, and every compressed input byte must be consumed, so truncated streams and trailing bytes fail closed. The public encoded-size helper now returns `Result` and uses checked arithmetic/conversion instead of wrapping `usize as i32`. Production `mc-net::read_frame` consumes the parse plan and runs compressed decode through a `spawn_blocking` boundary guarded by at most eight CPU-derived semaphore permits; cancellation while waiting does not enqueue work, and completed blocking tasks release their permit. Regressions cover raw exact/below threshold, compressed below threshold, exact 8 MiB and 8 MiB+1 output, mismatched length, truncated/trailing zlib, checked size overflow, production compressed reads, and execution off the Tokio runtime thread. `mc-protocol` passes 320/320, full `mc-net` passes 1,898 tests plus 3 integration tests with 5 ignored, Login and Configuration integrations pass, and strict package Clippy passes. |
| `SOL-030` | complete | Login now applies one canonical username policy immediately after exact `LoginStart` decode and before RSA authentication, offline UUID derivation, access lists, or any log/plugin/session publication. Names must contain 3–16 ASCII bytes from `[A-Za-z0-9_]`; empty/short/long, whitespace, controls, punctuation, and Unicode are rejected with an uncompressed `LoginDisconnect`. Invalid-name diagnostics contain only typed reason, byte count, index, and numeric byte value, never the raw untrusted string. Online verifier results are independently revalidated at the Login boundary: returned names must satisfy the same syntax and match the requested name case-insensitively before their canonical casing may become LoginSuccess/session identity. The existing active-session fence was confirmed and pinned with different-case names carrying different offline UUIDs, preventing case-collision identities until unregister. Unit tests cover exact 3/16 boundaries, valid alphabet, controls/newlines, spaces, punctuation, Unicode, and online canonical casing; real Login integration rejects seven hostile names before compression; case-insensitive duplicate registration passes its focused regression. Full `mc-net` passes 1,900 tests plus 3 integration tests with 5 ignored, Login integration passes 11/11, and package Clippy passes. |
| Other findings | queued for current-tree triage | Confirm against current code before implementation; static bundle severity is not automatically treated as current runtime truth. |

## Validation policy

Each checkpoint is committed separately. The ZIP, extracted review bundle, `repomix-output.xml`, local worlds, `.analysis/`, and other owner artifacts are never staged. A finding is not marked complete from helper-only tests: the touched boundary must have focused malformed/sad-path coverage, and storage/network/runtime findings require the relevant concurrency, persistence, or liveness evidence.
