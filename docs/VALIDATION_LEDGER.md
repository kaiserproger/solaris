# M100 Validation Ledger

**Status:** M77 frozen denominator. Quality label: `stabilization`.

This is the canonical scope and evidence ledger for the M100 core MVP. It
tracks client-observable vanilla 26.1.2 overworld-survival mechanics and
the non-gameplay evidence gates needed to claim replacement readiness.

Rows marked in scope are the M100 coverage denominator. Later removals
require owner approval and must remain visible in the denominator appendix.
Rows with only unit tests, Solaris-only harness tests, wire-probe-only or
protocol-metadata-only evidence, negated evidence, or implementation existence
do not count toward the 80% coverage numerator until they also have linked
vanilla oracle or real-client evidence.

## Status Vocabulary

| Status | Meaning |
|---|---|
| `ready` | Normal-path runtime tests plus linked vanilla oracle or real-client evidence exist for this exact row. |
| `partial` | Usable implementation and some tests/evidence exist, but the row is narrower than vanilla or lacks required oracle/client evidence. |
| `draft debt` | Implementation or scaffolding exists, but evidence is too thin for stabilization claims. |
| `accepted divergence` | Solaris intentionally differs from vanilla for the scoped MVP. Excluded from the coverage numerator, but visible to operators. |
| `non-goal` | Outside the scoped M100 MVP unless the owner changes scope. |
| `blocked` | A known failed/missing required gate prevents this row from counting. |
| `unknown` | Not enough current evidence to classify beyond future target work. |

## Preflight At M77

| Item | Result |
|---|---|
| Repository/toolchain | `pwd`, repo root, Rust 1.94.1, Cargo 1.94.1, rustfmt, Java 25.0.3, and `javap` are present. |
| Local artifacts | `.analysis/server.jar`, `data/vanilla/reports`, and `.analysis/test-world` are present. The test world was regenerated by Solaris from an empty `example.toml` world path, not by `tools/generate-test-world.sh`. |
| Degraded items | Current branch is `main`, not a `dev/M77-*` branch. Worktree already has unrelated modified/untracked files, including local-only `.serena/`, `.analysis/`, `YOLO_MODE.md`, `log.log`, and `opencode.json`; these must not be staged. Optional harness evidence also remains degraded where `data/vanilla/reports/block_light.json`, ignored vanilla-oracle tests, or `M43_VANILLA_ADDR` are absent. |

## Post-M95 Local Automated Validation Update

This local stabilization pass generated the gitignored vanilla sidecar
`data/vanilla/reports/block_light.json` from the bundled sidecar tooling and
re-ran the automated gates below. The quality label remains `stabilization`:
these results improve local oracle/load evidence, but do not provide a real
vanilla client/PrismLauncher run and do not move any row into the conservative
`ready` numerator by themselves.

| Gate | Result | Notes |
|---|---|---|
| Workspace tests | Passed | `cargo test --workspace` |
| Lint | Passed | `cargo clippy --workspace --all-targets -- -D warnings` |
| Formatting | Passed | `cargo fmt --all -- --check` |
| Code health | Not present | The `xtask code-health` gate had not been added yet for this historical pass. Current and future stabilization passes must record `cargo run -p xtask -- code-health` after final changes. |
| Vanilla oracle smoke suite | Not rerun | Ignored local-oracle scenarios were not run in this validation pass. |
| M96 load replay scaffold | Not rerun | Ignored local-sidecar load scenarios were not run in this validation pass. |
| M97 chunk-stream guard | Not rerun | The ignored view-distance-8/light-path guard was not run in this validation pass. |
| Approved real-client runner contract | Partial scaffold check | `bash -n tools/run-real-client-regression.sh`; `--prepare` accepted an absolute `SOLARIS_REAL_CLIENT_SERVER_CONFIG`, wrote a redacted client command plus SHA-256 fingerprint, and produced local artifacts under `.analysis/real-client-runs`. The full `--check`/`--validate-run` matrix was not rerun. |
| Real-client/manual | Not run | Still requires executing the approved runner with a real vanilla/PrismLauncher client and filling run observations/artifacts. |

## 2026-06-13 Static Review Update

The 2026-06-13 review was static-only. It did not run cargo, a real
vanilla/PrismLauncher client, profiler/load scenarios, or soak tests, and
therefore does not move any row into the conservative `ready` numerator.

Canonical outcome: Solaris is a strong stabilization-alpha/private vanilla-near
base, not a release-ready vanilla replacement. The frozen M100 denominator
remains 46 rows with 0 countable `ready` rows and 0.00% conservative coverage
under the runtime-test plus separate vanilla oracle/real-client evidence rule.

The review confirms these positive project properties without treating them as
coverage: honest readiness docs, modular crates, data sidecar discipline,
protocol discipline, real vertical slices, dense entity store direction, lock
metrics, and bounded extension/script contracts. It also keeps these blockers
front and center: missing real-client/oracle evidence; generated-world
chunk/light performance (`289` chunks functional, 17.3s full window,
`light_compute_ms=50489`, tick/`chunk_prepare`/`save_all_flush` warnings);
global `WorldStorage` mutex and `SessionRegistry` lock responsibilities; draft
entity AI/pathfinding; player water/swim and movement; stale block edit/CAS
transaction safety; public auth/offline-mode; persistence/crash/soak/autoscale;
and plugin API as a non-goal beyond boundary primitives.

## 2026-06-14 Stabilization Gate Update

This local stabilization pass added the fail-only `xtask code-health` gate to
the standard post-change baseline. It is an architecture-policy tripwire, not
gameplay, vanilla-oracle, real-client, performance, or soak evidence; it does
not move any M100 row into the conservative `ready` numerator.

| Gate | Result | Notes |
|---|---|---|
| Code health | Passed | `cargo run -p xtask -- code-health` reported `summary: 0 fail` and `verdict: KEEP` after the final code-health/tooling changes. |
| Focused code-health tests | Passed | `cargo test -p xtask` reported `3 passed`. |
| Focused chunk-stream cleanup tests | Passed | `cargo test -p mc-net chunk_stream --lib` reported `15 passed, 295 filtered out`. |
| Focused W2 storage/Anvil tests | Passed | `cargo test -p mc-world --lib anvil::region::tests` reported `7 passed`; `anvil::chunk_nbt::tests::real_test_world_carries_dropped_root_fields_in_extras`, `anvil::chunk_nbt::tests::round_trip_modified_chunk_through_disk`, and `storage::tests::unknown_root_extras_survive_world_storage_edit_flush_reopen` each passed. |
| Focused L2 item persistence tests | Passed | `cargo test -p mc-net save_all_writes_entities_and_world_metadata_to_real_storage --lib`, `save_all_then_bind_restores_world_time_and_item_entities`, `item_entity_lifecycle_fields_round_trip`, and `restored_item_respects_remaining_pickup_delay` each passed. |
| Focused P3 offline admission tests | Passed | `cargo test -p mc-server --test login login_toml_whitelist_allows_normalized_offline_profile` and the full `mc-server` login integration suite passed. |
| Workspace tests | Passed | `cargo test --workspace --all-targets` reported `944 passed, 11 ignored`. |
| Lint | Passed | `cargo clippy --workspace --all-targets -- -D warnings`. |
| Formatting | Passed | `cargo fmt --all -- --check`. |
| Vanilla oracle | Not run | W2, L2, and P3 behavior was strengthened with synthetic/unit/storage/TCP integration evidence only; no fresh vanilla capture was run. |
| Real-client/manual | Not run | No real-client/manual run was performed; persisted light/extras, dropped-item persistence, and offline admission changes remain automated test evidence only. |
| Performance/concurrency | Not measured | The W2/L2/P3 slices add tests and docs around existing paths; no performance, storage-concurrency, auth-concurrency, or soak gate was run. |

## 2026-06-22 Generated-World Baked-Light/Warm-Cache Probe

This local O1/O2 slice changed empty-world startup to materialize the
view-distance square plus the one-chunk light border, bake view-square light
arrays into generated chunks before the first stream, reuse persisted section
light arrays during chunk encoding, and warm the spawn-window cache when opening
an existing generated world. The quality label remains `stabilization`: the join
stream is much faster, but startup prep now carries the light-bake cost, first
missing-light backfill is also startup work, earlier restart probes emitted
`chunk_prepare` lock warnings and shutdown-hang evidence, and no real-client or
vanilla-oracle performance gate was run.

| Gate | Result | Notes |
|---|---|---|
| Focused baked-light tests | Passed | `cargo test -p mc-world light --lib`, `cargo test -p mc-world set_block_and_update --lib`, `cargo test -p mc-net chunk_stream --lib`, and `cargo test -p mc-server --bin mc-server spawn_window` passed after the baked-light and warm-cache changes. The follow-up runtime-light slice passed `cargo test -p mc-net light_inert_block_edits_do_not_request_full_relight --lib -- --nocapture`, `cargo test -p mc-net ignore_ticketed_chunks_until_loaded --lib -- --nocapture`, `cargo test -p mc-net random_tick --lib -- --nocapture`, and `cargo test -p mc-net fluid_tick --lib -- --nocapture`. Individual tests covered rebuilding `ChunkLight` from section arrays, invalidating stale baked light on block mutation, preserving baked light for light-inert edits, persisting recomputed relight, encoding a baked-light chunk without recompute, baking startup view-square light, and warming existing chunks without dirtying them. |
| Existing-world dirty-border regression | Passed | The 2026-07-05 `cargo test -p mc-server --bin mc-server existing_world_startup -- --nocapture` slice covers a disk-backed existing world whose view-square chunks already have baked light but whose one-chunk light border is absent. The first run failed before the helper existed; the fixed startup path now prepares existing worlds through the helper and leaves generated warm-cache border chunks dirty/resident for save-all (`flushed=0`, `dirty_count=16`) even when `baked == 0`. |
| Fresh empty-world debug probe | Partial improvement | Temporary `example.toml` copy with a fresh empty world and `random_tick_speed = 0`; debug `cargo run --bin mc-server -- --config /tmp/.../example-random0.toml` plus `timeout 120s cargo run -p mc-test-harness --bin wire-probe -- --addr 127.0.0.1:25565 --play-seconds 30`. Startup generated and flushed 361 chunks in `2398` ms, then baked 289 view-square chunks with total pregen-plus-bake elapsed `17273` ms. The stream emitted all 289 view-distance-8 chunks with `staged=361`, `fetch_ms=109`, `slow_fetch_chunks=0`, `light_compute_ms=0`, `slow_light_compute_chunks=0`, `pressure_flush_runs=0`, and first/ring1/ring2/full markers `8`/`16`/`31`/`327` ms. After Ctrl-C, the debug server completed clean save with `dirty_before=0`/`dirty_after=0` but still had to be killed after it did not exit on its own. |
| Restart diagnostic before warm-cache fix | Failed path captured | Reopening the same persisted world after baked-light persistence removed light recompute (`light_compute_ms=0`), but disk fetch remained too slow before the warm-cache fix: `fetch_ms=524275`, `slow_fetch_chunks=289`, `elapsed_ms=8888`, and first/ring1/ring2 markers `795`/`955`/`1180` ms. |
| Restart after warm-cache fix | Partial improvement | Startup warmed 361 spawn-window chunks from the existing world before listening in about `1.06` s. The next `wire-probe` stream emitted all 289 view-distance-8 chunks with `staged=361`, `fetch_ms=108`, `max_fetch_ms=10`, `slow_fetch_chunks=0`, `light_compute_ms=0`, `slow_light_compute_chunks=0`, `pressure_flush_runs=0`, and first/ring1/ring2/full markers `9`/`20`/`30`/`338` ms. The same run still logged two `chunk_prepare` wait warnings around `10.8` ms and reproduced the clean-save-then-hang shutdown issue. |
| Existing-world missing-light and runtime-light follow-up | Partial improvement | `.analysis/perf-runs/20260622T163447Z-generated-world-full-window` captured the remaining failed path after loaded-chunk simulation was fixed: all 289 chunks emitted, but `light_compute_ms=47773`, `slow_light_compute_chunks=289`, and full-window `elapsed_ms=16326`. `.analysis/perf-runs/20260622T163821Z-generated-world-rebaked-full-window` then backfilled missing existing-world spawn light at startup (`baked=289`, `flushed=289`) and streamed the same 289 chunks with `light_compute_ms=0`, `slow_light_compute_chunks=0`, first/ring1/ring2/full markers `10`/`19`/`32`/`332` ms, and no runtime tick budget warnings. Follow-up probes exposed and then fixed a runtime persistence gap where light-inert edits cleared baked arrays: `.analysis/perf-runs/20260622T164812Z-generated-world-preserved-light-client` kept startup `baked=0`/`flushed=0`, streamed with `fetch_ms=5`, `light_compute_ms=0`, and full-window `elapsed_ms=323`, then saved one dirty block-data chunk; `.analysis/perf-runs/20260622T164835Z-generated-world-preserved-light-startup-check` restarted immediately after that save with `baked=0`, `flushed=0`, and shutdown `dirty_before=0`. |
| Remaining blocker as of 2026-06-22 | Blocked | Generated-world join streaming no longer spent the first stream generating the light border or recomputing light for every emitted chunk, and light-inert runtime edits no longer forced a later startup rebake. The work was still not a green performance/concurrency gate. Fresh empty-world startup and first existing-world missing-light backfill were still startup-bound in that snapshot, earlier warmed streams still logged lock-wait warnings, and low/balanced/high profiles, tick p95/p99, slow-client/load, real-client performance, vanilla-oracle, crash-recovery, and soak gates were not run. The 2026-07-04 and 2026-07-05 disk-backed startup gates below supersede the fresh binary startup budget and first existing-world missing-light startup/stream path for one local debug profile only. |
| Real-client/manual | Not run | This was a protocol-client `wire-probe` performance/concurrency probe, not a real vanilla/PrismLauncher client run. |

## 2026-07-04 Disk-Backed Generated-World Startup Gate

This M100 slice added an ignored binary-level generated-world startup gate and
then fixed the measured fresh-startup blocker by using available CPU parallelism
for one-time spawn-window prep and deferring the fresh startup disk flush to the
existing save-all/stop path. The quality label remains `stabilization`: this is
one local debug-build, sidecar-dependent gate, not a low/balanced/high profile
matrix, slow-client test, real-client performance run, crash-recovery run, or
soak.

| Gate | Result | Notes |
|---|---|---|
| Red startup budget gate | Failed before fix | `cargo test -p mc-server --test generated_world_startup disk_backed_generated_world_startup_stream_budget -- --ignored --exact --nocapture` first failed with fresh `startup_to_listener = 19641` ms against a 10 s budget while the first stream was already healthy: 289 chunks emitted in `332` ms, first/ring1/ring2 markers `34`/`41`/`54` ms, restart startup `1642` ms, restart full stream `350` ms, and summary assertions for `emitted=289`, `absent=0`, `pressure_flush_runs=0`, `degraded_delivery=false`, `light_compute_ms=0`, `slow_fetch_chunks=0`, and `slow_light_compute_chunks=0` passed before the budget panic. |
| Disk-backed startup/stream gate | Passed | The same ignored gate passed after startup worker saturation plus deferred fresh disk flush: latest run logged `first_startup_ms=9295`, `first_first_chunk_ms=33`, `first_ring1_ms=Some(41)`, `first_ring2_ms=Some(54)`, `first_full_ms=350`, `second_startup_ms=1644`, `second_first_chunk_ms=33`, `second_ring1_ms=Some(42)`, `second_ring2_ms=Some(56)`, and `second_full_ms=345`. |
| Deferred flush durability path | Passed | The gate asserts fresh startup leaves the generated spawn window resident and dirty before listener (`chunks=361`, `dirty=361`, `region_files=0`, `disk flush deferred to save-all`) and that `/stop` save-all flushes the same 361 chunks before restart (`flushed=361`, `planned=361`, `dirty_before=361`, `dirty_after=0`). |
| Evidence limit | Partial | The gate exercises the real `mc-server` binary, local `data/vanilla` sidecars, temp disk world, protocol harness client, stop command, restart, and 289-chunk stream. It is ignored by default and does not prove real-client visual behavior, vanilla oracle parity, crash-before-save durability, broad hardware profiles, long tick p95/p99, multi-client load, slow-reader pressure, or soak. |

## 2026-07-05 Existing-World Missing-Light Startup Gate

This M100 slice added an ignored binary-level gate for a disk-backed existing
world whose spawn window exists on disk but lacks section light arrays. The fix
keeps the 289 rebaked view-distance chunks resident and dirty until the existing
save-all/stop path instead of synchronously flushing them before the listener.
The quality label remains `stabilization`: this is one local debug-build,
sidecar-dependent gate, not a broad performance or durability pass.

| Gate | Result | Notes |
|---|---|---|
| Red existing-world flush gate | Failed before fix | `cargo test -p mc-server --test generated_world_startup disk_backed_existing_world_missing_light_startup_stream_budget -- --ignored --exact --nocapture` first failed because the existing-world warm-cache summary logged the 289 rebaked chunks as `flushed=289` before the listener instead of the expected `flushed=0`. |
| Existing-world startup/stream gate | Passed | The same ignored gate passed after deferring the startup flush: latest run logged `startup_ms=8768`, `full_ms=347`, `first_chunk_ms=33`, `ring1_ms=Some(44)`, and `ring2_ms=Some(56)`. It asserts the startup summary has `chunks=361`, `baked=289`, `flushed=0`, and `dirty=289`; the stream summary has `emitted=289`, `absent=0`, `pressure_flush_runs=0`, `degraded_delivery=false`, `light_compute_ms=0`, `slow_fetch_chunks=0`, and `slow_light_compute_chunks=0`. |
| Stop save-all durability path | Passed | The gate sends `/stop` and asserts save-all flushes the 289 dirty rebaked chunks (`flushed=289`, `planned=289`, `dirty_before=289`, `dirty_after=0`). |
| Evidence limit | Partial | The fixture materializes the unbaked spawn window through `mc_worldgen` and `WorldStorage` instead of binary-generating a world and stripping light arrays. The gate is ignored by default and depends on local vanilla sidecars. It does not prove crash-before-save durability, real-client visual behavior, vanilla oracle parity, broad low/balanced/high hardware profiles, long tick p95/p99, multi-client load, slow-reader pressure, or soak. |

## 2026-07-05 Slow-Reader Outbound Pressure Gate

This local S2/O1/O2 slice bounds reliable retry task growth for one
backpressured outbound recipient and strengthens the ignored load scenarios so
healthy protocol clients must continue receiving entity broadcasts while a peer
reader is paused. The quality label remains `stabilization`: this is
sidecar-dependent protocol-harness evidence, not real-client load, slow-client
shedding, profile-matrix, or soak evidence.

| Gate | Result | Notes |
|---|---|---|
| Red bounded-retry unit | Failed before fix | `cargo test -p mc-net reliable_visibility_retries_are_bounded_per_slow_recipient --lib -- --nocapture` first failed with `left: 16` and `right: 1`, proving one full recipient channel could spawn 16 pending reliable retry tasks. A follow-up RED compile check added `max_reliable_command_retries_in_flight` to the pressure snapshot before the metric existed. |
| Bounded-retry unit | Passed | `cargo test -p mc-net reliable_visibility --lib -- --nocapture` passed both retry tests after per-recipient retry latching and max-in-flight pressure tracking. |
| Healthy-observer slow-reader gate | Passed | `cargo test -p mc-test-harness --test load_scenarios paused_reader_pressure_does_not_delay_healthy_observers -- --ignored --exact --nocapture` passed with two healthy observers receiving `128/128` spawn packets each while a paused peer produced outbound pressure: `visibility_command_drops=4407`, `reliable_command_retries=1`, `reliable_command_retries_in_flight=0`, and `max_reliable_command_retries_in_flight=1`. |
| Existing slow-reader gate | Passed | `cargo test -p mc-test-harness --test load_scenarios paused_reader_does_not_stall_active_entity_broadcasts -- --ignored --exact --nocapture` passed with `active_spawns=55`, `summons=256`, `visibility_command_drops=19069`, `reliable_command_retries=10`, and `max_reliable_command_retries_in_flight=1`. |
| Bounded replay gate | Passed | `cargo test -p mc-test-harness --test load_scenarios bounded_multiplayer_survival_replay_covers_sequential_contention_and_slow_reader -- --ignored --exact --nocapture` passed with `clients=5`, `sequential_same_block_updates=2`, `spawns=9`, `elapsed_ms=10632`, `visibility_command_drops=1175`, `reliable_command_retries=1`, and `max_reliable_command_retries_in_flight=1`. |
| Evidence limit | Partial | These gates use local `data/vanilla` sidecars and protocol clients. They prove bounded retry-task growth and healthy observer delivery for one paused-reader entity-broadcast shape, but they do not prove real-client behavior, broad two-client gameplay, 20-player view-distance-8 load, low/balanced/high profile budgets, slow-client disconnect/shedding policy, long soak, or autoscale control. Reliable commands beyond the one queued retry are currently dropped and counted as pressure for the slow recipient rather than recovered for that client. |

## 2026-07-05 Dirty-Flush Lock-Narrowing Guards

This local O2 slice narrows the save-all/dirty-pressure path by making
`plan_dirty_flush` clone dirty chunk snapshots without NBT encoding and by making
post-plan mutable chunk aliases invalidate the planned dirty generation instead
of relying on payload re-encode comparison under `SaveAllFlush`. The quality
label remains `stabilization`: this proves focused storage invariants, not broad
lock-budget, crash-recovery, or soak behavior.

| Gate | Result | Notes |
|---|---|---|
| Red mutable-alias unit | Failed before fix | `cargo test -p mc-world dirty_flush_mutable_alias_after_plan_invalidates_planned_generation --lib -- --nocapture` failed because `WorldStorage::get_chunk_mut` after `plan_dirty_flush` left `dirty_generation` unchanged, so the old plan could only be rejected by a later payload comparison. |
| Red plan-encoding unit | Failed before fix | `cargo test -p mc-world dirty_flush_plan_clones_snapshots_without_encoding_payloads --lib -- --nocapture` failed with `left: 1` and `right: 0`, proving `plan_dirty_flush` encoded dirty chunk NBT while callers still held the world mutex. |
| Dirty-flush focused suite | Passed | `cargo test -p mc-world dirty_flush_ --lib -- --nocapture` passed 17 focused dirty-flush tests after routing mutable cached chunk access through a helper that bumps `dirty_generation` when a dirty chunk has shared snapshots and moving dirty chunk NBT encoding to `DirtyFlushPlan::write`. The unchanged fast path still cleans without copying when the planned snapshot is still the live snapshot, while defensive/legacy payload fallback coverage remains for non-normal shapes. |
| Evidence limit | Partial | This is unit-level storage evidence. It proves plan-time dirty flush work no longer encodes payloads under the storage lock, but it does not measure save-all hold time, chunk-pressure flush latency, shutdown behavior under live mutation, crash-before-save durability, broad generated-world stream pressure, or low/balanced/high profile budgets. |

## 2026-06-22 Real-Client Agent Run

This local stabilization pass drove a real vanilla 26.1.2 client through the
approved Java-agent runner path against Solaris. It proves six focused M94
scenarios, not the full M94 pack, and does not make a broad M100 readiness
claim.

| Gate | Result | Notes |
|---|---|---|
| Real-client runner | Passed | `bash tools/run-real-client-regression.sh --run` with `SOLARIS_REAL_CLIENT_KIND=vanilla-client` produced and `--validate-run` accepted `.analysis/real-client-runs/20260622T135534Z-m94-regression-pack`, `.analysis/real-client-runs/20260622T142127Z-m94-regression-pack`, `.analysis/real-client-runs/20260622T144628Z-m94-regression-pack`, `.analysis/real-client-runs/20260622T152126Z-m94-regression-pack`, `.analysis/real-client-runs/20260622T153556Z-m94-regression-pack`, and `.analysis/real-client-runs/20260622T161221Z-m94-regression-pack`. |
| Rejected placement evidence | Passed | `m94-02b-rejected-block-resync` reached Play in `minecraft:overworld`, ran against a real client at player position `(0.5, 81.0, 0.5)`, passed occupied solid placement rejection, survival out-of-reach solid placement rejection, and occupied water-bucket fallback/resync observations, then wrote `screenshots/m94-02b-rejected-block-resync.png`. |
| Solid break/drop/place evidence | Passed | `m94-02a-solid-place-break-drop` reached Play in `minecraft:overworld`, used a real-client item-use placement path and named vanilla destroy packets from inside the real client process for the server-timed break/drop path, observed target air after break, observed a visible dirt drop, restored the held dirt stack by pickup, then placed dirt into a loaded adjacent air cell and wrote `screenshots/m94-02a-solid-place-break-drop.png`. Door/trapdoor, crop/bonemeal, sugar cane, and broad fluid paths were explicitly degraded rather than counted under this focused scenario. |
| Join/rejoin movement evidence | Passed | `m94-01-join-rejoin-chunks-movement` reached Play, moved a real vanilla client forward for `750` ms with `horizontal_delta=3.673`, observed the Solaris server-side session release marker, rejoined Play in `minecraft:overworld`, and wrote `screenshots/m94-01-join-rejoin-chunks-movement-after-move.png` plus `screenshots/m94-01-join-rejoin-chunks-movement-after-rejoin.png`. The final run showed prompt session release at `2026-06-22T14:22:16.119Z` and rejoin at `2026-06-22T14:22:16.315Z`, without duplicate-session rejection or keepalive-timeout cleanup. |
| Water-bucket evidence | Passed | `m94-02c-water-bucket-place-pickup` reached Play in `minecraft:overworld`, used the real-client item-use path to place water at `(2,78,-11)`, observed the held item become `minecraft:bucket x1`, picked the source back up with the empty bucket, observed `source_cleared=true`, restored `minecraft:water_bucket x1`, and wrote `screenshots/m94-02c-water-bucket-place-pickup.png`. Lava, broad fluid spread, water-lava interaction, and swim feel were explicitly degraded rather than counted under this focused scenario. |
| Sign evidence | Passed | `m94-04a-regular-sign-place-text` reached Play in `minecraft:overworld`, used the real-client item-use path to place an oak sign at `(2,78,-11)`, observed the held sign stack decrement to empty, observed the real `SignEditScreen`, sent `Solaris|M94|real-client|sign` from inside the client process, observed the client-side sign block entity converge to those lines, and wrote `screenshots/m94-04a-regular-sign-place-text.png`. The later broad `m94-04` rerun below additionally closed the editor and rechecked the same text after close. Hanging signs, waxed signs, styled/filtered/clickable text, bed sleep/respawn, campfires, restart persistence, and broad visual parity assertions remain degraded rather than counted under this focused scenario. |
| Inventory recipe evidence | Passed | `m94-03a-inventory-oak-log-to-planks` reached Play in `minecraft:overworld` after the driver waited out an existing `ConnectScreen` instead of issuing a duplicate connect, loaded `1146` configured sidecar recipes, gave the client one oak log, sent the vanilla `ServerboundPlaceRecipePacket` for inventory container `0` and sidecar `minecraft:oak_planks` recipe display id `697`, observed `oak_log_expected_count=0`/`oak_log_count_matched=true`, observed oak planks increase from `8` to `12` with `oak_planks_count_matched=true`, and wrote `screenshots/m94-03a-inventory-oak-log-to-planks.png`. Crafting-table UI, cursor recovery, recipe-book discovery UI, containers, stations, malformed clicks, and broad recipe execution were explicitly degraded rather than counted under this focused scenario. |
| Remaining M94 coverage | Blocked | The broad `m94-02-blocks-fluids-farming-drops`, `m94-03-inventory-crafting-containers-stations`, and `m94-04-signs-beds-campfires-and-block-entities` rows now have separate blocked evidence recorded below; three other broad M94 manifest scenarios remain `manual-pending`/unrun. These focused runs are real-client evidence for the exact join/rejoin/movement, rejected-placement/water-bucket fallback, focused solid break/drop/place, accepted water-bucket place/pickup, regular sign placement/plain-text, and focused inventory oak-log-to-planks recipe slices only; they are not full manual/client coverage, vanilla oracle coverage, performance evidence, or soak evidence. |

## 2026-06-22 Real-Client Random-Tick Performance Follow-Up

This local O1/O2 follow-up used the approved PrismLauncher/Java-agent runner
against a real vanilla 26.1.2 client after random-tick stabilization work. The
quality label remains `stabilization`: the focused client scenario no longer
reproduces random-tick runtime stalls, but this is not a low/balanced/high
profile, multi-client, slow-client, soak, or vanilla-oracle performance gate.

| Gate | Result | Notes |
|---|---|---|
| Focused random/light tests | Passed | `cargo test -p mc-net farmland_random_tick_does_not_materialize_neighbour_chunks -- --nocapture`, `cargo test -p mc-net light_inert_block_edits_do_not_request_full_relight -- --nocapture`, and `cargo test -p mc-net random_tick -- --nocapture`. These cover resident-only farmland random-tick neighbor reads and background incremental relight from saved baked light instead of the old full relight helper. |
| Real-client water-bucket rerun | Passed | `SOLARIS_REAL_CLIENT_KIND=prism-launcher ... bash tools/run-real-client-regression.sh --run` produced `.analysis/real-client-runs/20260622T174026Z-m94-regression-pack`, and `bash tools/run-real-client-regression.sh --validate-run .analysis/real-client-runs/20260622T174026Z-m94-regression-pack` accepted it. `m94-02c-water-bucket-place-pickup` passed with the same focused observations as the earlier water run: water placed at `(2,78,-11)`, held stack changed to `minecraft:bucket x1`, pickup cleared the source, and held stack returned to `minecraft:water_bucket x1`. |
| Runtime tick budget | Partial improvement | The previous PrismLauncher rerun `.analysis/real-client-runs/20260622T173420Z-m94-regression-pack` reproduced repeated `runtime tick exceeded performance budget` warnings dominated by `random_tick_us=149225..525492` when random ticks applied light-changing edits. After resident-only random neighbor reads plus background incremental relight, `.analysis/real-client-runs/20260622T174026Z-m94-regression-pack/server.log` had no `runtime tick exceeded performance budget` lines and no `background relight skipped` lines during the focused scenario. |
| Remaining performance debt | Blocked | The same run still reports chunk-stream cost rather than a full performance pass: `view-distance window flushed` with `fetch_ms=8431`, `light_compute_ms=3100`, `slow_fetch_chunks=17`, `slow_light_compute_chunks=17`, and full-window `elapsed_ms=1879`. Broad generated-world startup/backfill, low/balanced/high profiles, tick p95/p99, multi-client load, slow-client pressure, shutdown health, and soak remain unrun. |

## 2026-06-22 Real-Client Broad Inventory/Chest Probe

This local Q2 follow-up extended the Java-agent `m94-03` broad inventory row
from an unsupported-scenario blocker into a partial real-client probe. The row
remains `blocked`: recipe execution and simple chest open/close now have real
client evidence, but cursor transfer, chest/barrel clicks, furnace-family UI,
common stations, malformed clicks, and recovery paths still need dedicated
in-client primitives and server evidence.

| Gate | Result | Notes |
|---|---|---|
| Agent scenario tests | Passed | Red-first Java-agent coverage was added for `m94-03-inventory-crafting-containers-stations`; `client-mod/solaris-client-agent/gradlew -p client-mod/solaris-client-agent :java-agent:test --tests dev.solaris.agent.javaagent.M94InventoryCraftingScenarioTest --no-daemon` passed after the scenario ran the recipe probe, placed/opened a chest, observed `ContainerScreen`, and closed the screen. |
| Agent jar | Passed | `client-mod/solaris-client-agent/gradlew -p client-mod/solaris-client-agent :java-agent:jar --no-daemon` rebuilt `client-mod/solaris-client-agent/java-agent/build/libs/java-agent-0.1.0.jar` before the client rerun. |
| Real-client broad `m94-03` probe | Blocked | `SOLARIS_REAL_CLIENT_KIND=prism-launcher ... SOLARIS_REAL_CLIENT_AGENT_SCENARIO=m94-03-inventory-crafting-containers-stations bash tools/run-real-client-regression.sh --run` produced `.analysis/real-client-runs/20260622T180005Z-m94-regression-pack`, and `bash tools/run-real-client-regression.sh --validate-run .analysis/real-client-runs/20260622T180005Z-m94-regression-pack` accepted it. The real client reached Play in `minecraft:overworld`, consumed one oak log, increased oak planks from `20` to `24`, placed a chest, opened `net.minecraft.client.gui.screens.inventory.ContainerScreen`, closed back to `current_screen=none`, and wrote `screenshots/m94-03-inventory-crafting-containers-stations.png`. The scenario result is intentionally `blocked`, not `passed`, because cursor transfer, chest/barrel clicks, furnace-family UI, common stations, malformed clicks, and recovery paths remain unimplemented in the in-client agent. |
| Runtime warnings | Passed for this focused run | The final artifact server log had no `runtime tick exceeded performance budget` lines and no `background relight skipped` lines. The client log still contains PrismLauncher/OpenAL/AppImage shutdown noise from the runner killing the client after observation; that is not counted as a gameplay pass or failure. |
| Remaining M94 coverage | Blocked | Six of twelve M94 scenarios have passed real-client evidence, and broad `m94-03` has blocked partial real-client evidence at this checkpoint. Later broad block/fluid and signs slices below supersede this interim remaining-coverage snapshot. |

## 2026-06-22 Real-Client Broad Block/Fluid Probe

This local Q2 follow-up extended the Java-agent `m94-02` broad block/fluid row
from an unsupported-scenario blocker into a composed real-client probe. The row
remains `blocked`: focused solid break/drop/place and water-bucket place/pickup
now have same-run real-client evidence, but door/trapdoor, crop/bonemeal, sugar
cane support/cascade/drop, broad fluid spread, water-lava interaction, and swim
feel still need dedicated in-client primitives and server evidence.

| Gate | Result | Notes |
|---|---|---|
| Agent scenario tests | Passed | Red-first Java-agent coverage was added for `m94-02-blocks-fluids-farming-drops`; `client-mod/solaris-client-agent/gradlew -p client-mod/solaris-client-agent :java-agent:test --tests dev.solaris.agent.javaagent.M94BlocksFluidsFarmingDropsScenarioTest --no-daemon` passed after the scenario composed the focused solid and water subprobes while preserving blocked broad subrows. |
| Agent jar | Passed | `client-mod/solaris-client-agent/gradlew -p client-mod/solaris-client-agent :java-agent:jar --no-daemon` rebuilt `client-mod/solaris-client-agent/java-agent/build/libs/java-agent-0.1.0.jar` before the client rerun. |
| Real-client broad `m94-02` probe | Blocked | `SOLARIS_REAL_CLIENT_KIND=prism-launcher ... SOLARIS_REAL_CLIENT_AGENT_SCENARIO=m94-02-blocks-fluids-farming-drops bash tools/run-real-client-regression.sh --run` produced `.analysis/real-client-runs/20260622T180920Z-m94-regression-pack`, and `bash tools/run-real-client-regression.sh --validate-run .analysis/real-client-runs/20260622T180920Z-m94-regression-pack` accepted it. The real client reached Play in `minecraft:overworld`, broke a `minecraft:podzol` target into air, observed a visible dirt drop and pickup convergence, placed dirt back into loaded air, placed a water source, observed the held item become `minecraft:bucket x1`, picked the source back up, observed `source_cleared=true`, restored `minecraft:water_bucket x1`, and wrote `screenshots/m94-02-blocks-fluids-farming-drops.png`. The scenario result is intentionally `blocked`, not `passed`, because door/trapdoor, crop/bonemeal, sugar cane support/cascade/drop, broad fluid spread, water-lava interaction, and swim feel remain unimplemented in the in-client agent. |
| Runtime warnings | Passed for this focused run | The final artifact server log had no `runtime tick exceeded performance budget` lines and no `background relight skipped` lines. The client log still contains PrismLauncher/OpenAL/AppImage shutdown noise from the runner killing the client after observation; that is not counted as a gameplay pass or failure. |
| Remaining M94 coverage | Blocked | Six of twelve M94 scenarios have passed real-client evidence, and broad `m94-02` plus broad `m94-03` have blocked partial real-client evidence at this checkpoint. The later signs slice below supersedes this interim remaining-coverage snapshot. |

## 2026-06-22 Real-Client Broad Signs/Beds/Campfires Probe

This local Q2 follow-up extended the Java-agent `m94-04` broad block-entity row
from an unsupported-scenario blocker into a composed real-client sign probe. The
row remains `blocked`: regular sign placement, editor open, plain-text update,
editor close, and after-close text visibility now have same-run real-client
evidence, but beds, campfires, restart persistence, hanging signs, waxed signs,
styled/filtered/clickable text, sounds/statistics/events, and broader visual
parity still need dedicated in-client primitives and server evidence.

| Gate | Result | Notes |
|---|---|---|
| Agent scenario tests | Passed | Red-first Java-agent coverage was added for `m94-04-signs-beds-campfires-and-block-entities`; `client-mod/solaris-client-agent/gradlew -p client-mod/solaris-client-agent :java-agent:test --tests dev.solaris.agent.javaagent.M94SignsBedsCampfiresScenarioTest --no-daemon` passed after the scenario composed the regular-sign subprobe while preserving blocked broad subrows. The focused sign test was tightened so `M94SignScenario` closes the real sign editor and checks text again after close. |
| Agent jar | Passed | `client-mod/solaris-client-agent/gradlew -p client-mod/solaris-client-agent :java-agent:test --no-daemon && client-mod/solaris-client-agent/gradlew -p client-mod/solaris-client-agent :java-agent:jar --no-daemon` passed before the client rerun. |
| Real-client broad `m94-04` probe | Blocked | `SOLARIS_REAL_CLIENT_KIND=prism-launcher ... SOLARIS_REAL_CLIENT_AGENT_SCENARIO=m94-04-signs-beds-campfires-and-block-entities bash tools/run-real-client-regression.sh --run` produced `.analysis/real-client-runs/20260622T182159Z-m94-regression-pack`, and `bash tools/run-real-client-regression.sh --validate-run .analysis/real-client-runs/20260622T182159Z-m94-regression-pack` accepted it. The real client reached Play in `minecraft:overworld`, placed an oak sign at `(2,78,-11)`, observed `SignEditScreen`, sent `Solaris|M94|real-client|sign`, observed text convergence, closed the editor, rechecked the same text after close, ended with `current_screen=none`, and wrote `screenshots/m94-04-signs-beds-campfires-and-block-entities.png`. The scenario result is intentionally `blocked`, not `passed`, because beds, campfires, restart persistence, hanging/waxed/styled signs, sounds/statistics/events, and broad visual parity remain unimplemented in the in-client agent. |
| Runtime warnings | Passed for this focused run | The final artifact server log had no `runtime tick exceeded performance budget` lines and no `background relight skipped` lines. The client log still contains PrismLauncher/OpenAL/AppImage shutdown noise from the runner killing the client after observation; that is not counted as a gameplay pass or failure. |
| Remaining M94 coverage | Blocked | Six of twelve M94 scenarios have passed real-client evidence, broad `m94-02`, broad `m94-03`, and broad `m94-04` have blocked partial real-client evidence, and three broad scenarios remain unrun/manual-pending at this checkpoint. The later entities/combat/death slice below supersedes this interim count. |

## 2026-06-22 Real-Client Broad Entities/Combat/Death/Respawn Probe

This local Q2 follow-up extended the Java-agent `m94-05` broad entities/combat
row from an unsupported-scenario blocker into a composed real-client probe. The
row remains `blocked`: visible passive entity spawn/sync and death/respawn now
have same-run real-client evidence, but hostile combat, melee damage/knockback,
mob drops, XP pickup, projectiles, shield timing, vehicles, and broad AI/pathing
still need dedicated in-client primitives and server evidence.

| Gate | Result | Notes |
|---|---|---|
| Agent scenario tests | Passed | Red-first Java-agent coverage was added for `m94-05-entities-combat-death-respawn`; `client-mod/solaris-client-agent/gradlew -p client-mod/solaris-client-agent :java-agent:test --no-daemon` passed after the scenario summoned a nearby cow, required the observation to be near the summon target, observed `DeathScreen`, and performed a vanilla respawn client command. |
| Server respawn regression | Passed | The packet harness `cargo test -p mc-test-harness --test block_edit dead_survival_player_can_respawn_and_act_again -- --nocapture` was tightened to drain the complete initial spawn view, then require `ClientboundRespawn`, `GameEvent::EVENT_START_WAITING_FOR_CHUNKS`, `SetCenterChunk`, a replayed `LevelChunkWithLight`, and full health after `PERFORM_RESPAWN`. It failed before the server fix with `chunk=false`, then failed again with `load_start=false`, and passed after respawn replay used `ChunkScheduler::replay_view` plus the vanilla-pinned load-start game event. The unit guard `cargo test -p mc-net chunk_pipeline::tests::scheduler_replay_view_requeues_finished_chunks --lib` also passed. |
| Agent jar | Passed | `client-mod/solaris-client-agent/gradlew -p client-mod/solaris-client-agent :java-agent:test :java-agent:jar --no-daemon` passed before the final client rerun. |
| Real-client broad `m94-05` probe | Blocked | `SOLARIS_REAL_CLIENT_KIND=prism-launcher ... SOLARIS_REAL_CLIENT_AGENT_SCENARIO=m94-05-entities-combat-death-respawn bash tools/run-real-client-regression.sh --run` produced `.analysis/real-client-runs/20260622T190401Z-m94-regression-pack`, and `bash tools/run-real-client-regression.sh --validate-run .analysis/real-client-runs/20260622T190401Z-m94-regression-pack` accepted it. The real client reached Play in `minecraft:overworld`, observed a visible `minecraft:cow` at `(0.5,81.0,4.5)` with `distance_squared=16.0`, showed the death screen after survival damage, performed respawn, ended with `current_screen=none`, and wrote `screenshots/m94-05-entities-combat-death-respawn.png`. The scenario result is intentionally `blocked`, not `passed`, because hostile combat, melee damage/knockback, mob drops, XP pickup, projectiles, shield timing, vehicles, and broad AI/pathing remain unimplemented in the in-client agent. |
| Prior failed artifacts | Captured bug evidence | `.analysis/real-client-runs/20260622T183542Z-m94-regression-pack` exposed an invalid entity observation that matched an old persisted cow far from the summon target; the Java-agent probe now filters by target distance. `.analysis/real-client-runs/20260622T183924Z-m94-regression-pack` and `.analysis/real-client-runs/20260622T185755Z-m94-regression-pack` reproduced a real respawn loading-screen failure; the final good artifact shows the same path leaving the loading screen after the respawn replay/load-start fix. |
| Runtime warnings | Passed for this focused run | The final artifact server log had no `runtime tick exceeded performance budget` lines and no `background relight skipped` lines. The client log still contains PrismLauncher/AppImage shutdown noise from the runner killing the client after observation; that is not counted as a gameplay pass or failure. |
| Remaining M94 coverage | Blocked | Six of twelve M94 scenarios have passed real-client evidence; broad `m94-02`, `m94-03`, `m94-04`, and `m94-05` have blocked partial real-client evidence; two broad scenarios remain unrun/manual-pending at this checkpoint: `m94-06-save-restart-two-client-visibility` and `m94-07-m40-m41-route-with-metrics`. The later M40/M41 route slice below supersedes this interim count. |

## 2026-06-22 Real-Client M40/M41 Route Probe

This local Q2/O1/O2 follow-up extended the Java-agent
`m94-07-m40-m41-route-with-metrics` row from an unsupported-scenario blocker
into a composed real-client probe. The row remains `blocked`: same-run water
placement/pickup, visible drop/pickup, solid placement, and passive entity
visibility now have real-client evidence, but swim feel, sugar cane
support/cascade/drop, the owner M40/M41 frozen-world route, and full TPS/lock
performance evidence still need dedicated gates.

| Gate | Result | Notes |
|---|---|---|
| Agent scenario tests | Passed | Red-first Java-agent coverage was added for `m94-07-m40-m41-route-with-metrics`; from `client-mod/solaris-client-agent`, `./gradlew :java-agent:test --tests dev.solaris.agent.javaagent.M94M40M41RouteScenarioTest --no-daemon` passed after the scenario composed water, solid/drop, and visible-entity subprobes while preserving blocked manual/performance subrows. `./gradlew :java-agent:test :java-agent:jar --no-daemon` then rebuilt the agent jar. |
| Real-client broad `m94-07` probe | Blocked | `SOLARIS_REAL_CLIENT_KIND=prism-launcher ... SOLARIS_REAL_CLIENT_AGENT_SCENARIO=m94-07-m40-m41-route-with-metrics bash tools/run-real-client-regression.sh --run` produced `.analysis/real-client-runs/20260622T191650Z-m94-regression-pack`, and `bash tools/run-real-client-regression.sh --validate-run .analysis/real-client-runs/20260622T191650Z-m94-regression-pack` accepted it. The real client reached Play in `minecraft:overworld`, placed and picked up water at `(-1,81,0)`, broke a `minecraft:podzol` target into air, observed a visible dirt drop and pickup convergence, placed dirt back into loaded air, observed a visible `minecraft:cow` at `(0.5,81.0,4.5)` with `distance_squared=16.0`, and wrote `screenshots/m94-07-m40-m41-route-with-metrics.png`. The scenario result is intentionally `blocked`, not `passed`, because swim feel, sugar cane support/cascade/drop, TPS/lock log analysis, and the owner M40/M41 frozen-world route remain unimplemented in the in-client agent. |
| Runtime/log evidence | Partial | The artifact server log had no `runtime tick exceeded performance budget` lines and no `background relight skipped` lines. Its spawn stream emitted the 289-chunk view-distance-8 window with `fetch_ms=27`, `light_compute_ms=0`, `slow_fetch_chunks=0`, `slow_light_compute_chunks=0`, first/ring1/ring2/full markers `12`/`18`/`38`/`412` ms, and `pressure_flush_runs=0`. This is useful focused evidence only; it is not a low/balanced/high profile, slow-client, multi-client, or soak performance gate. |
| Remaining M94 coverage | Blocked | Six of twelve M94 scenarios have passed focused real-client evidence; broad `m94-02`, `m94-03`, `m94-04`, `m94-05`, and `m94-07` have blocked partial real-client evidence, and `m94-06-save-restart-two-client-visibility` remains unrun/manual-pending at this checkpoint. The later save/restart slice below supersedes this interim count. |

## 2026-06-22 Real-Client Save/Restart Probe

This local Q2/S1/S2 follow-up extended the Java-agent
`m94-06-save-restart-two-client-visibility` row from an unsupported/manual
blocker into a runner-managed real-client probe with save/restart and optional
second-client phases. The row remains `blocked`: one real client now has
save/restart marker-persistence evidence and a second real client observed one
live block edit, one shared item drop, and one shared item pickup/removal, but
shared container convergence, edit contention, and broader two-client join/move
coverage still need evidence.

| Gate | Result | Notes |
|---|---|---|
| Agent scenario and runner tests | Passed | Red-first Java-agent coverage was added for the `m94-06-save-restart-before` and `m94-06-save-restart-after` phases, for `m94-06-two-client-place`/`m94-06-two-client-observe`, for `m94-06-two-client-drop-break`/`m94-06-two-client-drop-observe`, and for `m94-06-two-client-pickup-collect`/`m94-06-two-client-pickup-gone-observe`; from `client-mod/solaris-client-agent`, `./gradlew :java-agent:test --tests dev.solaris.agent.javaagent.M94SaveRestartVisibilityScenarioTest --no-daemon` passed after the phased scenario wrote/read restart and shared-drop markers and the secondary observer saw the primary marker/drop/pickup removal. `./gradlew :java-agent:test :java-agent:jar --no-daemon` rebuilt the agent jar. The runner and driver append/two-bridge contracts passed `cargo test -p mc-test-harness --test real_client_manifest -- --nocapture`, `cargo test -p mc-test-harness --test real_client_agent_driver -- --nocapture`, and `bash -n tools/run-real-client-regression.sh`. |
| Real-client broad `m94-06` probe | Blocked | The first validated save/restart artifact `.analysis/real-client-runs/20260622T193743Z-m94-regression-pack` proved same-client marker persistence across `save-all`, `kill -INT`, restart, and reconnect. The latest two-client run `SOLARIS_REAL_CLIENT_KIND=prism-launcher ... SOLARIS_REAL_CLIENT_SECOND_COMMAND='... --dir /home/kaiserroman/solaris/.analysis/prism-second ...' SOLARIS_REAL_CLIENT_AGENT_SCENARIO=m94-06-save-restart-two-client-visibility bash tools/run-real-client-regression.sh --run` produced `.analysis/real-client-runs/20260622T210703Z-m94-regression-pack`, and `bash tools/run-real-client-regression.sh --validate-run .analysis/real-client-runs/20260622T210703Z-m94-regression-pack` accepted it. The before phase placed a dirt marker at `(-2,82,0)` through the real-client item-use path, sent `save-all`, wrote `screenshots/m94-06-save-restart-before.png`, and the runner stopped Solaris with `kill -INT`; the after phase restarted Solaris, reconnected the same real client, observed that marker persisted, and wrote `screenshots/m94-06-save-restart-after.png`. The appended `m94-06-two-client-live-visibility` phase launched `SolarisObserver` from a separate Prism root, had the primary bridge place a fresh marker at `(-1,82,1)`, had the secondary bridge observe `two-client block visibility: passed`, and wrote primary/secondary screenshots. The appended `m94-06-two-client-shared-drop` phase had the primary bridge break a dirt block at `(-1,82,0)`, observe `two-client shared drop break: passed started=true became_air=true saw_drop=true`, had the secondary bridge observe `two-client shared drop visibility: passed`, and wrote primary/secondary screenshots. The appended `m94-06-two-client-shared-pickup` phase had both clients observe a fresh shared dirt drop, had the primary bridge collect it through the real client with `pickup_restored=true held=minecraft:dirt x1`, had the secondary bridge observe `two-client shared pickup removal: passed`, and wrote primary/secondary screenshots. The overall scenario result remains `blocked`, not `passed`, because shared container convergence, edit contention, and broader two-client join/move coverage remain uncovered. |
| Runtime/log evidence | Partial | The two-client artifact server log had no `WARN`/`ERROR`, `runtime tick exceeded performance budget`, or `background relight skipped` lines. It recorded `SolarisAgent` and `SolarisObserver` offline logins, plus eight view-distance-8 streams with `light_compute_ms=0`, `slow_fetch_chunks=0`, `slow_light_compute_chunks=0`, `degraded_delivery=false`, and full-window markers `394`, `379`, `372`, `148`, `360`, `62`, `330`, and `71` ms. This is useful focused restart/live-block/drop/pickup-removal evidence only; it is not a low/balanced/high profile, slow-client, shared-container, contention, crash-recovery, or soak gate. |
| Remaining M94 coverage | Blocked | Six of twelve M94 scenarios had passed focused real-client evidence at this checkpoint; the six broad rows `m94-02`, `m94-03`, `m94-04`, `m94-05`, `m94-06`, and `m94-07` had blocked partial real-client evidence. The later shared-chest probes below add two focused passing routes, and the full pack is still non-green because broad subrows still lack door/trapdoor, crops, sugar cane, lava, broad fluids, swim feel, hanging/waxed/styled signs, beds, campfires, full containers/stations, hostile combat, drops/XP, vehicles, broad AI/pathing, shared container/edit contention, performance profiles, vanilla oracle, and soak evidence. |

## 2026-06-22 Real-Client Shared Chest Probe

This local Q2/S2/K1 follow-up added and then drove a focused two-client
shared-container route, `m94-03b-two-client-shared-chest`, through real vanilla
26.1.2 clients. It counts only for the exact marked chest slot deposit/observe
path; broad containers, stations, malformed clicks, cursor recovery, and
contention remain open.

| Gate | Result | Notes |
|---|---|---|
| Agent, driver, and manifest coverage | Passed | Red-first Java-agent coverage was added for `m94-03b-two-client-shared-chest-deposit` and `m94-03b-two-client-shared-chest-observe`; the fake client proves the deposit phase writes a marker and moves selected dirt into chest slot `0`, and the observe phase opens the marked chest and waits for dirt in slot `0`. The two-bridge driver test proves the aggregate route calls the primary bridge with the deposit phase and the secondary bridge with the observe phase. The manifest test proves the public `m94-03b-two-client-shared-chest` id is listed in the M94 regression pack. |
| Focused local validation | Passed | `cargo test -p mc-net debug_give_zero_count_clears_hotbar_slot_before_item_lookup -- --nocapture`, `cargo test -p mc-net chest_quick_move_places_player_stack_in_first_empty_storage_slot -- --nocapture`, `./gradlew :java-agent:test --tests dev.solaris.agent.javaagent.M94InventoryCraftingScenarioTest --no-daemon`, `cargo test -p mc-test-harness --test real_client_agent_driver agent_driver_coordinates_two_real_client_bridges_for_m94_03b_shared_chest -- --nocapture`, `cargo test -p mc-test-harness --test real_client_manifest m94_real_client_manifest_covers_required_regression_rows -- --nocapture`, `python3 -m py_compile tools/real-client-agent-driver.py`, `bash -n tools/run-real-client-regression.sh`, and `./gradlew :java-agent:jar --no-daemon` passed after implementation. |
| Real-client `m94-03b` probe | Passed | Initial shape-validated attempts `.analysis/real-client-runs/20260622T213302Z-m94-regression-pack`, `.analysis/real-client-runs/20260622T213826Z-m94-regression-pack`, and `.analysis/real-client-runs/20260622T213933Z-m94-regression-pack` failed before the scenario reached Play. The sidecar-backed diagnostic `.analysis/real-client-runs/20260622T220041Z-m94-regression-pack` reached Play and exposed `moved_slot=false` plus an observer empty-hand setup blocker; `.analysis/real-client-runs/20260622T220850Z-m94-regression-pack` passed primary deposit and exposed the secondary non-op debug-clear blocker. The final sidecar-backed run `.analysis/real-client-runs/20260622T221125Z-m94-regression-pack` was accepted by `bash tools/run-real-client-regression.sh --validate-run`, recorded `result: passed`, had both real clients enter Play, loaded registries/tags/recipes from `source="vanilla_sidecar"`, moved dirt into chest slot `0` with `moved_slot=true` and `slot_matched=true`, had the secondary open the same marked chest with `visible=true` and `slot_matched=true`, and wrote primary plus secondary screenshots. |
| Remaining M94 coverage | Blocked | At that checkpoint the manifest had thirteen scenarios: seven focused rows had passed real-client evidence, and the six broad rows remained blocked partial evidence. Broad shared-container convergence, edit contention, malformed menu clicks, broad stations, performance profiles, vanilla oracle, and soak evidence remained missing. |

## 2026-06-23 Shared Chest Live-Update Real-Client Route

This local Q2/K1/S2 slice adds and validates a focused two-client real-client
route for a still-open shared chest screen receiving a peer mutation. It counts
only for that exact shared chest slot convergence path.

| Gate | Result | Notes |
|---|---|---|
| Red agent regressions | Passed after implementation | `client-mod/solaris-client-agent/gradlew -p client-mod/solaris-client-agent :java-agent:test --tests dev.solaris.agent.javaagent.M94InventoryCraftingScenarioTest --no-daemon` first failed the three new `m94-03c` phase tests because the scenario ids were unsupported, then passed after the Java agent added primary open-with-dirt, secondary withdraw, and primary observe-empty phases. A later red trace failed until the primary setup phase teleported the operator-controlled primary back near the secondary spawn before selecting a chest target. |
| Red driver regressions | Passed after implementation | `cargo test -p mc-test-harness --test real_client_agent_driver agent_driver_coordinates_two_real_client_bridges_for_m94_03c_shared_chest_live_update -- --nocapture` first failed because the driver sent only the aggregate id to the primary bridge, then passed after the driver orchestrated primary `open-with-dirt`, secondary `withdraw`, and primary `observe-empty`. `agent_driver_waits_for_interactive_play_screen_before_scenario` first failed because `in_play=true` with `LevelLoadingScreen` was treated as scenario-ready, then passed after the driver waited for an interactive `current_screen=none` sample. |
| Manifest/runner contract | Passed | `cargo test -p mc-test-harness --test real_client_manifest -- --nocapture` passed after the manifest listed `m94-03c-two-client-shared-chest-live-update` and the runner required the second bridge for it. |
| Post-change automated validation | Passed | After the final driver, Java-agent, and docs changes: `python3 -m py_compile tools/real-client-agent-driver.py`, `bash -n tools/run-real-client-regression.sh`, `bash tools/run-real-client-regression.sh --validate-run .analysis/real-client-runs/20260622T232643Z-m94-regression-pack`, `cargo test -p mc-test-harness --test real_client_agent_driver -- --nocapture`, `cargo test -p mc-test-harness --test real_client_manifest -- --nocapture`, `client-mod/solaris-client-agent/gradlew -p client-mod/solaris-client-agent :java-agent:test :java-agent:jar --no-daemon`, `cargo fmt --all -- --check`, `cargo run -p xtask -- code-health`, `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, and `git diff --check` all passed. |
| Real-client `m94-03c` probe | Passed | Diagnostic runs `.analysis/real-client-runs/20260622T231502Z-m94-regression-pack`, `.analysis/real-client-runs/20260622T231956Z-m94-regression-pack`, and `.analysis/real-client-runs/20260622T232141Z-m94-regression-pack` exposed, respectively, premature `LevelLoadingScreen` readiness, seeded X11 adapter interference, and an out-of-reach secondary caused by persisted primary position. The final direct-Xvfb run `.analysis/real-client-runs/20260622T232643Z-m94-regression-pack` passed `bash tools/run-real-client-regression.sh --validate-run`, recorded `result: passed`, and passed all three phases: primary open/deposit with `primary_screen_left_open=true`, secondary same-chest withdraw with `moved_to_inventory=true` and `slot_empty=true`, and primary still-open slot observation with `slot_empty=true` and `closed=true`. |
| Evidence limit | Partial | This is focused real-client evidence for one two-client shared chest live slot update. It does not inject deliberate stale container clicks, cover furnace/barrel/station UIs, prove broad cursor recovery or malformed click recovery, run a vanilla oracle, measure performance, or provide soak/slow-reader contention evidence. |
| Remaining M94 coverage | Blocked | The manifest now has fourteen scenarios: eight focused rows have passed real-client evidence (`m94-02b`, `m94-01`, `m94-02a`, `m94-02c`, `m94-04a`, `m94-03a`, `m94-03b`, and `m94-03c`), and the six broad rows remain blocked partial evidence. Broad shared-container convergence, deliberate stale-click injection, edit contention, malformed menu clicks, broad stations, performance profiles, vanilla oracle, and soak evidence remain missing. |

## 2026-06-23 Shared Chest Stale-Click Race Slice

This local K1/S2 stabilization slice fixes a race exposed by the shared-chest
path above: one viewer could mutate a chest while another viewer's outbound
slot broadcast was still queued, then the second viewer could submit an old
`state_id` click that still matched its stale local `ChestWindow`.

| Gate | Result | Notes |
|---|---|---|
| Red regression | Passed after fix | `cargo test -p mc-net stale_chest_click_after_peer_mutation_resyncs_without_mutating_storage -- --nocapture` first failed at `assertion failed: state.carried_item.is_empty()`, proving the stale click picked up the peer-mutated stack. |
| Focused fix evidence | Passed | The fixed path keeps a shared chest state id in `SessionRegistry`, opens new chest windows at the current shared state, carries the exact state id in `ChestSlots` dispatches, and makes `handle_chest_container_click` resync instead of mutating when the packet's state id is stale relative to the shared counter. |
| Evidence limit | Partial | This is a unit-level real storage/packet-path regression for one shared chest stale-click race. It is not a real-client concurrent-click run, vanilla oracle capture, broad container state-machine proof, slow-reader contention gate, or soak evidence. |

## 2026-07-04 Shared Chest Protocol Stale-Click Harness Slice

This local K1/S2 stabilization slice lifts the existing stale-chest guard into
a real TCP/session harness. One protocol client opens a placed chest, a second
protocol client inserts dirt into that shared chest and advances the shared
state id, then the first client sends a stale click with its old state id.

| Gate | Result | Notes |
|---|---|---|
| Focused harness evidence | Passed | `cargo test -p mc-test-harness --test block_edit two_clients_stale_chest_click_after_peer_update_resyncs -- --nocapture` passed on current production code. The harness observed the peer `ContainerSetSlot` state id advance beyond the initial open state, then required the stale click to produce a full `ContainerSetContent` resync with dirt still in chest slot `0` and an empty carried item. |
| Evidence limit | Partial | This is protocol-client evidence for one two-viewer stale chest click after a live peer update. It is not a real-client concurrent-click run, vanilla oracle capture, broad container state-machine proof, slow-reader contention gate, performance evidence, or soak evidence. |

## 2026-07-04 Unsupported And Malformed Container Click Fail-Closed Slice

This local Q3/K1 stabilization slice adds a narrow valid-wire but unsupported
menu-click matrix. Protocol clients open a chest containing dirt and a furnace
containing raw iron, then send `ServerboundContainerClick` with
`ContainerInput::QuickCraft`, `Clone`, and `PickupAll`, lying `changed_slots`,
and a lying carried item. It also adds a valid `Pickup` click against the
furnace with a matching state id but an impossible client-reported carried
item. The server must not trust the client delta.

| Gate | Result | Notes |
|---|---|---|
| Focused harness evidence | Passed | `cargo test -p mc-test-harness --test block_edit unsupported_chest_click_modes_resync_without_trusting_client_slots -- --nocapture` passed on current production code. `cargo test -p mc-test-harness --test block_edit malformed_furnace_clicks_resync_without_trusting_client_slots -- --nocapture` first failed with the malformed pickup advancing furnace state from `1` to `2`, then passed after post-click carried-item rollback was added for conflicting non-empty client predictions. The harnesses require `ContainerSetContent` resyncs with authoritative slot `0` unchanged, an authoritative server carried item, and direct backing block-entity storage unchanged. |
| Evidence limit | Partial | This is protocol-client fail-closed coverage for two container-family storage paths with a three-mode unsupported click matrix plus one malformed carried-item pickup route. It is not a broad malformed-click state-machine proof, invalid-wire decoder/fuzzer corpus, replay manifest, real-client behavior, vanilla oracle capture, slow-reader contention gate, or soak evidence. |

## 2026-07-04 Malformed Inventory Click Fail-Closed Slice

This local Q3/I1 stabilization slice covers one valid-wire malformed
window-0 inventory click. A protocol client gives dirt into hotbar slot `36`,
then sends `ServerboundContainerClick` with matching inventory state id,
`ContainerInput::Pickup`, lying `changed_slots`, and an impossible post-click
carried-item prediction. The server must roll back the mutation and resync the
authoritative slot/cursor state without treating a rejected click as a state
advance.

| Gate | Result | Notes |
|---|---|---|
| Red regression | Passed after fix | `cargo test -p mc-test-harness --test block_edit malformed_inventory_click_resyncs_without_advancing_state -- --nocapture` first failed with `left: 3 right: 2` on the resync state id assertion, proving the rollback resync advanced the authoritative inventory state id. |
| Focused harness evidence | Passed | The same focused test passed after inventory content writes were split into mutation-advancing writes and resync-preserving writes. The harness requires hotbar slot `36` to still contain dirt x10, the server cursor to stay empty, no dirt to migrate into other inventory slots, and the resync state id to equal the pre-click authoritative state id. |
| Evidence limit | Partial | This is protocol-client fail-closed coverage for one malformed window-0 carried-item prediction with lying changed slots. It is not broad click-state-machine coverage, invalid-wire decoder/fuzzer corpus, replay manifest, real-client behavior, vanilla oracle capture, recipe-book/crafting-table cursor recovery, slow-reader contention, or soak evidence. |

## 2026-07-04 Invalid Carried-Item Slot Fail-Closed Slice

This local Q3/B1/I1 stabilization slice covers one valid-wire malformed
hotbar-selection route. A protocol client holds dirt in hotbar slot `0`, gives
stone into slot `8`, sends `ServerboundSetCarriedItem { slot: 99 }`, then uses
`ServerboundUseItemOn`. The server must ignore the invalid carried slot rather
than clamping it into a valid hotbar index.

| Gate | Result | Notes |
|---|---|---|
| Red regression | Passed after fix | `cargo test -p mc-test-harness --test block_edit invalid_carried_item_slot_does_not_change_survival_placement_slot -- --nocapture` first failed because the placement `BlockUpdate` used stone state id `1` instead of dirt state id `10`, proving the invalid slot had been clamped to slot `8`. |
| Focused harness evidence | Passed | The same focused test passed after the `ServerboundSetCarriedItem` handler accepted only `0..=8` and ignored invalid slots instead of clamping them. The harness now requires dirt placement at the target cell, matching `BlockChangedAck`, hotbar slot `0` consumption, and no slot-`8` consumption. |
| Evidence limit | Partial | This is protocol-client fail-closed coverage for one malformed carried-item slot before a survival placement. It is not negative-slot coverage, active pending break/use preservation, broad invalid-action coverage, invalid-wire decoder/fuzzer corpus, replay manifest, real-client behavior, vanilla oracle capture, anti-cheat evidence, slow-reader contention, or soak evidence. |

## 2026-07-04 World-Border UseItemOn Fail-Closed Slice

This local Q3/B1 stabilization slice covers a narrow valid-wire `UseItemOn`
rejection route. A protocol client sends `ServerboundUseItemOn` with
`world_border_hit = true` against a loaded dirt support and adjacent air target;
a follow-up route sends the same flag against far, unloaded cells while holding
a water bucket. The server must treat the action as a no-op and must not use
the world-border flag to force far block loads or held-bucket resync.

| Gate | Result | Notes |
|---|---|---|
| Red regression | Passed after fix | `cargo test -p mc-test-harness --test block_edit rejected_world_border_use_item_on_resyncs_without_placing -- --nocapture` first failed because the target-cell `BlockUpdate` used dirt state id `10` instead of air state id `0`, proving Solaris ignored `world_border_hit` and placed the held dirt. |
| Red regression | Passed after fix | `cargo test -p mc-test-harness --test block_edit far_world_border_use_item_on_does_not_load_blocks_or_resync_bucket -- --nocapture` first failed with `far world-border UseItemOn must not load and resync clicked/target cells`, proving the first fix used authoritative lookup on far cells. |
| Focused harness evidence | Passed | The focused loaded-cell test passed after the `UseItemOn` preflight path returned a `WorldBorderHit` no-op and routed it through loaded-only clicked/target resync before ack. The far-cell bucket test passed after the route switched to `LOADED_ONLY_BLOCKS`, requiring no clicked/target `BlockUpdate` and no held water-bucket `ContainerSetSlot` before ack. |
| Evidence limit | Partial | This is protocol-client fail-closed coverage for two client-reported world-border-hit shapes: loaded dirt placement and far unloaded water-bucket use. It is not world-border geometry or boundary enforcement, broad invalid-action coverage, invalid-wire decoder/fuzzer corpus, replay manifest, real-client behavior, vanilla oracle capture, anti-cheat evidence, slow-reader contention, or soak evidence. |

## 2026-07-04 Out-Of-Reach Survival Break Resync Slice

This local Q3/B1 stabilization slice covers two valid-wire
`StartDestroyBlock` rejection shapes in survival mode. A loaded target outside
the 5-block reach must be resynced before ack; a far unloaded target must not
be loaded or generated just to produce the rejection.

| Gate | Result | Notes |
|---|---|---|
| Red regression | Passed after fix | `cargo test -p mc-test-harness --test block_edit out_of_reach_survival_break_resyncs_target_before_ack -- --nocapture` first failed with `rejected break ack arrived before target-cell BlockUpdate`, proving the out-of-reach survival destroy path acked without resyncing the loaded target cell. |
| Red regression | Passed after fix | `cargo test -p mc-test-harness --test block_edit far_out_of_reach_survival_break_does_not_load_target_before_ack -- --nocapture` first failed because the rejection path emitted a far target `BlockUpdate`, proving the first fix used authoritative lookup that could load/generate the target cell. |
| Focused harness evidence | Passed | `cargo test -p mc-test-harness --test block_edit out_of_reach_survival_break -- --nocapture` passed after the rejection path switched to cached loaded-cell resync before ack. The loaded case requires the seeded stone target `BlockUpdate` before `BlockChangedAck`; the far case requires the matching ack without a target-cell `BlockUpdate`. |
| Evidence limit | Partial | This is protocol-client coverage for loaded-target resync and far no-load/no-resync out-of-reach `StartDestroyBlock` rejection. It is not broad destroy-action coverage, stop/abort variants, real-client behavior, vanilla oracle capture, anti-cheat evidence, invalid-wire decoder/fuzzer corpus, slow-reader contention, or soak evidence. |

## 2026-06-23 Shared Furnace Stale-Click Race Slice

This local K1/S2 stabilization slice fixes the same stale-window race for
furnace-family containers. A furnace slot mutation could be queued for peer
viewers while another viewer still had a stale local `FurnaceWindow` state id;
that viewer could then submit an old click that matched the stale local window
and mutate the peer-updated furnace slots.

| Gate | Result | Notes |
|---|---|---|
| Red regression | Passed after fix | `cargo test -p mc-net stale_furnace_click_after_peer_mutation_resyncs_without_mutating_storage --lib -- --nocapture` first failed at `assertion failed: state.carried_item.is_empty()`, proving the stale click picked up the peer-mutated furnace input stack. |
| Focused fix evidence | Passed | The fixed path keeps a shared furnace state id in `SessionRegistry`, opens furnace windows at the current shared state, carries the exact state id in `FurnaceSlots` dispatches, makes `handle_furnace_container_click` resync stale packets, and uses the shared state id for smelting-tick slot changes before owner/viewer slot packets are written. |
| Evidence limit | Partial | This is unit-level real storage/packet-path coverage for one shared furnace stale-click race plus registry state-id dispatch behavior. It is not a real-client concurrent-click run, vanilla oracle capture, broad furnace state-machine proof, slow-reader contention gate, or soak evidence. |

## 2026-07-04 Shared Furnace Protocol Stale-Click Harness Slice

This local K1/S2 stabilization slice lifts the existing stale-furnace guard
into a real TCP/session harness. One protocol client opens a placed furnace,
a second protocol client inserts raw iron into that shared furnace and advances
the shared state id, then the first client sends a stale click with its old
state id.

| Gate | Result | Notes |
|---|---|---|
| Setup regression | Failed before cleanup | `cargo test -p mc-test-harness --test block_edit two_clients_stale_furnace_click_after_peer_update_resyncs -- --nocapture` first failed in login because the test usernames exceeded vanilla's 16-character limit. |
| Focused harness evidence | Passed | After shortening the usernames, the same command passed. The harness observed the peer `ContainerSetSlot` state id advance beyond the initial open state, then required the stale click to produce a full `ContainerSetContent` resync with raw iron still in furnace input slot `0` and an empty carried item. |
| Evidence limit | Partial | This is protocol-client evidence for one two-viewer stale furnace click after a live peer update. It is not a real-client concurrent-click run, vanilla oracle capture, broad furnace state-machine proof, slow-reader contention gate, performance evidence, or soak evidence. |

## 2026-06-22 Scheduled Fluid Save/Restart Slice

This local B3/S1/O2 stabilization slice fixed dirty-flush planning for live
scheduled ticks. The Anvil codec already models vanilla `block_ticks.t` and
`fluid_ticks.t` as remaining delay fields, but the dirty-flush path encoded
chunks with a zero tick basis. A pending fluid tick scheduled at simulation tick
`100` for trigger tick `112` therefore reloaded after restart as trigger tick
`112` in the fresh runtime instead of `12` ticks remaining.

| Gate | Result | Notes |
|---|---|---|
| Red regression | Passed after fix | `cargo test -p mc-net server::tests::save_all_persists_scheduled_fluid_ticks_as_remaining_restart_delay -- --nocapture` first failed with `left: 112` / `right: 12`, then passed after `save_all` and chunk-pressure dirty flushes planned chunks with `SessionRegistry::simulation_tick()`. |
| Focused storage/flush tests | Passed | `cargo test -p mc-world scheduled_tick -- --nocapture`, `cargo test -p mc-world dirty_flush -- --nocapture`, `cargo test -p mc-net dirty_pressure_defers_generated_stream_chunk_instead_of_absent -- --nocapture`, and `cargo test -p mc-net stream_recovers_deferred_chunk_after_dirty_pressure_clears -- --nocapture`. |
| Evidence limit | Partial | This proves a server save-all/Anvil/reopen bug for scheduled fluid tick delay accounting. It is not a real-client restart run, crash-recovery gate, vanilla oracle capture, broad fluid parity test, or soak/performance gate. |

## 2026-07-04 Generated-World Chunk-Stream Lock-Pressure Harness Slice

This local C1/O2 stabilization slice extends the ignored, sidecar-dependent
view-distance-8 chunk-stream harness with lock-pressure delta assertions around
the full 289-chunk stream.

| Gate | Result | Notes |
|---|---|---|
| Focused generated-world harness | Passed | `cargo test -p mc-test-harness --test chunk_stream vanilla_client_receives_spawn_view_distance_window -- --ignored --nocapture` passed with the new assertions. The logged deltas showed `chunk_prepare` wait/hold counts advancing from the pre-stream snapshot, while `save_all_flush` and `player_persistence` stayed unchanged. |
| Evidence limit | Partial | This proves the ignored in-memory generated-world stream exercises the narrowed `ChunkPrepare` metric path without entering save-all or player-persistence lock paths. It is not a disk-backed generated-world latency gate, real-client performance run, low/balanced/high hardware profile, slow-reader contention test, broad lock-narrowing review, shutdown/flush-health gate, or soak evidence. |

## 2026-07-04 Operator Public-Bind Check Warning Slice

This local O4 stabilization slice makes `mc-server --check` surface runtime
public-bind safety blockers in its JSON output. Public binds that would fail at
serve time because Solaris is in offline mode, or because local development
operators are enabled, now produce structured `operator_warnings`.

| Gate | Result | Notes |
|---|---|---|
| Red CLI regression | Passed after implementation | `cargo test -p mc-server --test cli check_reports_public_bind_security_warnings -- --nocapture` first failed because `--check` printed no `operator_warnings` field for `bind_address = "8.8.8.8"` with `allow_local_dev_operators = true`, then passed after the check JSON included `public_bind_offline_mode` and `public_bind_local_dev_operators`. |
| Evidence limit | Partial | This is operator check-output evidence for two public-bind warning codes. It does not implement online-mode authentication, change runtime public-bind rejection, prove real deployment safety, cover backups/restore, audit dependencies/licenses, or validate data/protocol drift. |

## 2026-07-04 Online Mode Check Warning Slice

This local O4 stabilization slice makes `mc-server --check` surface
`auth.online_mode = true` even on loopback binds. Runtime login already rejects
online-mode because Mojang authentication is not implemented; the check path now
warns as `online_mode_unsupported` before operators discover that at connect
time.

| Gate | Result | Notes |
|---|---|---|
| Red CLI regression | Passed after implementation | `cargo test -p mc-server --test cli check_reports_online_mode_warning_on_loopback -- --nocapture` first failed because a loopback config with `[auth] online_mode = true` rendered only `missing_world_dir`, then passed after `--check` included `online_mode_unsupported` without `public_bind_online_mode`. |
| Evidence limit | Partial | This is operator check-output evidence for one unsupported-auth configuration. It does not implement Mojang online-mode authentication, validate session servers, prove public deployment safety, cover login UX beyond existing fail-closed behavior, or replace real-client/manual auth testing. |

## 2026-07-04 Invalid Bind Address Check Failure Slice

This local O4 stabilization slice makes `mc-server --check` fail loudly when
`network.bind_address` is not a valid IP address. Previously the check path
rendered JSON successfully for a config that would fail later when translated
for serve.

| Gate | Result | Notes |
|---|---|---|
| Red CLI regression | Passed after implementation | `cargo test -p mc-server --test cli check_invalid_bind_address_exits_nonzero_with_clear_error -- --nocapture` first failed because a config with `bind_address = "not-an-ip"` exited 0 and rendered JSON, then passed after `--check` validated `network.bind_address` before rendering and stderr included both `network.bind_address` and `not-an-ip`. |
| Evidence limit | Partial | This is CLI config-validation evidence for bind-address syntax only. It does not test TCP bind, port availability, firewall/NAT behavior, public deployment safety, runtime listener startup, or multi-address bind policy. |

## 2026-07-04 Zero Save-Interval Check Warning Slice

This local O4 stabilization slice makes `mc-server --check` surface a
configuration that runtime already clamps: `[simulation].save_interval_ticks =
0`. A zero configured value becomes a one-tick save interval in the network
policy, which can create continuous save pressure.

| Gate | Result | Notes |
|---|---|---|
| Red CLI regression | Passed after implementation | `cargo test -p mc-server --test cli check_reports_zero_save_interval_warning -- --nocapture` first failed because a config with `save_interval_ticks = 0` and a valid temp world dir rendered `operator_warnings: []`, then passed after `--check` included `simulation_save_interval_ticks_zero` without `missing_world_dir`. |
| CLI surface | Passed | `cargo test -p mc-server --test cli -- --nocapture` passed 33/33 after the new warning. |
| Evidence limit | Partial | This is operator check-output evidence for one clamped simulation save interval. It does not prove disk-backed save durability, backup/restore, runtime save scheduling performance, shutdown flush behavior, filesystem permissions, real-client operation, or soak evidence. |

## 2026-07-04 Blank Admin Operator Check Warning Slice

This local O4 stabilization slice makes `mc-server --check` surface empty or
whitespace-only `[admin].operators` entries. Runtime lowercases configured
operator names, but blank entries never match players or grant command
permissions.

| Gate | Result | Notes |
|---|---|---|
| Red CLI regression | Passed after implementation | `cargo test -p mc-server --test cli check_reports_blank_admin_operator_warning -- --nocapture` first failed because a config with `operators = ["", "  "]` and a valid temp world dir rendered `operator_warnings: []`, then passed after `--check` included `admin_operator_entry_blank` without `missing_world_dir`. |
| CLI surface | Passed | `cargo test -p mc-server --test cli -- --nocapture` passed 34/34 after the new warning. |
| Evidence limit | Partial | This is operator check-output evidence for blank operator entries only. It does not normalize/trim operator names, prove runtime command permissions, cover duplicate/case/whitespace operator policy, real-client/manual admin operation, public deployment safety, or broader auth/admin persistence. |

## 2026-07-04 Blank Auth Access Check Warning Slice

This local O4/P3 stabilization slice makes `mc-server --check` surface empty
or whitespace-only `[auth].whitelist` and `[auth].banned_players` entries.
Runtime trims and drops those entries before login admission, so they never
allow or deny a player.

| Gate | Result | Notes |
|---|---|---|
| Red CLI regression | Passed after implementation | `cargo test -p mc-server --test cli check_reports_blank_auth_access_entries_warning -- --nocapture` first failed because a config with blank whitelist and banned-player entries plus a valid temp world dir rendered `operator_warnings: []`, then passed after `--check` included `auth_whitelist_entry_blank` and `auth_banned_player_entry_blank` without `missing_world_dir`. |
| CLI surface | Passed | `cargo test -p mc-server --test cli -- --nocapture` passed 35/35 after the new warnings. |
| Evidence limit | Partial | This is operator check-output evidence for inert auth-list entries only. It does not prove runtime whitelist/banlist admission behavior, persisted auth files, duplicate/case/whitespace policy beyond blank entries, real-client/manual login behavior, online-mode auth, auth-concurrency, or public deployment safety. |

## 2026-07-04 Real-Client Screenshot Validation Slice

This local Q2 stabilization slice makes
`tools/run-real-client-regression.sh --validate-run` reject a passed observed
scenario whose manifest sets `screenshots_required: true` unless
`observations.json` references at least one existing file under `screenshots/`.

| Gate | Result | Notes |
|---|---|---|
| Red runner regression | Passed after implementation | `cargo test -p mc-test-harness --test real_client_manifest -- validate_run_rejects_missing_required_scenario_screenshots --exact --nocapture` first failed because a synthetic passed `m94-01-join-rejoin-chunks-movement` artifact with an empty scenario screenshot list printed `validated`, then passed after `--validate-run` rejected the missing `screenshots/` artifact. |
| Runner contract surface | Passed | `cargo test -p mc-test-harness --test real_client_manifest -- --nocapture` passed 3/3 after the new validation check. |
| Evidence limit | Partial | This is real-client runner validation-tooling evidence only. It does not run a real client, validate screenshot content, prove gameplay correctness, broaden M94 scenario coverage, provide vanilla oracle evidence, or cover performance/soak behavior. |

## 2026-07-04 Real-Client Scenario ID Validation Slice

This local Q2 stabilization slice makes
`tools/run-real-client-regression.sh --validate-run` reject observed scenario
ids that are absent from the copied manifest before accepting real-client
observations.

| Gate | Result | Notes |
|---|---|---|
| Red runner regression | Passed after implementation | `cargo test -p mc-test-harness --test real_client_manifest -- validate_run_rejects_unknown_observed_scenario_id --exact --nocapture` first failed because a synthetic passed `m94-unknown-scenario` artifact with an actual `screenshots/` file printed `validated`, then passed after `--validate-run` rejected the unknown scenario id. |
| Runner contract surface | Passed | `cargo test -p mc-test-harness --test real_client_manifest -- --nocapture` passed 4/4 after the new manifest-membership check. |
| Evidence limit | Partial | This is real-client runner validation-tooling evidence only. It does not run a real client, prove manifest completeness, validate scenario semantics, prove gameplay correctness, provide vanilla oracle evidence, or cover performance/soak behavior. |

## 2026-07-04 Real-Client Screenshot PNG Validation Slice

This local Q2 stabilization slice makes the in-client agent driver reject
captured screenshot artifacts that are not structurally valid PNG files before
writing passed observations.

| Gate | Result | Notes |
|---|---|---|
| Red driver regression | Passed after implementation | `cargo test -p mc-test-harness --test real_client_agent_driver -- agent_driver_rejects_invalid_screenshot_artifact --exact --nocapture` first failed because invalid fake screenshot bytes reached the post-screenshot disconnect path instead of reporting invalid PNG bytes, then passed after the driver checked PNG structure before accepting the artifact. |
| Driver contract surface | Passed | `cargo test -p mc-test-harness --test real_client_agent_driver -- --nocapture` passed 16/16 after normal fake screenshots moved to a minimal valid PNG fixture. |
| Python syntax | Passed | `python3 -m py_compile tools/real-client-agent-driver.py` passed after adding the PNG parser. |
| Evidence limit | Partial | This is real-client driver validation-tooling evidence only. It does not run a real client, prove screenshot visual content, prove gameplay correctness, provide vanilla oracle evidence, broaden M94 coverage, or cover performance/soak behavior. |

## 2026-07-04 Real-Client Runner Screenshot PNG Validation Slice

This local Q2 stabilization slice makes
`tools/run-real-client-regression.sh --validate-run` reject referenced
required-screenshot artifacts that are not structurally valid PNG files.

| Gate | Result | Notes |
|---|---|---|
| Red runner regression | Passed after implementation | `cargo test -p mc-test-harness --test real_client_manifest -- validate_run_rejects_invalid_screenshot_png --exact --nocapture` first failed because a synthetic passed `m94-01-join-rejoin-chunks-movement` artifact with an existing `screenshots/*.png` file containing `fake png bytes` printed `validated`, then passed after `--validate-run` rejected invalid PNG bytes. |
| Runner contract surface | Passed | `cargo test -p mc-test-harness --test real_client_manifest -- --nocapture` passed 6/6 after adding the invalid-PNG rejection plus a valid-PNG success fixture. |
| Shell syntax | Passed | `bash -n tools/run-real-client-regression.sh` passed after adding the inline PNG validator. |
| Evidence limit | Partial | This is real-client runner validation-tooling evidence only. It does not run a real client, prove screenshot visual content, prove gameplay correctness, provide vanilla oracle evidence, broaden M94 coverage, or cover performance/soak behavior. |

## 2026-07-04 Real-Client Broad Scenario Blocked-Only Slice

This local Q2 stabilization slice makes the in-client agent driver reject
`passed` reports for broad M94 scenario ids that are documented as
blocked-only until their missing evidence is implemented.

| Gate | Result | Notes |
|---|---|---|
| Red driver regression | Passed after implementation | `cargo test -p mc-test-harness --test real_client_agent_driver -- agent_driver_rejects_passed_broad_blocked_only_scenario --exact --nocapture` first failed because a fake bridge returned `passed` for `m94-02-blocks-fluids-farming-drops` and the driver wrote passed observations, then passed after the driver rejected the broad blocked-only pass. |
| Driver contract surface | Passed | `cargo test -p mc-test-harness --test real_client_agent_driver -- --nocapture` passed 17/17 after adding the blocked-only policy. |
| Evidence limit | Partial | This is real-client driver validation-tooling evidence only. It does not run a real client, complete the broad M94 scenarios, prove gameplay correctness, provide vanilla oracle evidence, or cover performance/soak behavior. |

## 2026-07-04 Real-Client Server Address Loopback Slice

This local Q2 stabilization slice makes the in-client agent driver reject
`--server-addr` targets outside explicit loopback hosts before issuing any
bridge RPC.

| Gate | Result | Notes |
|---|---|---|
| Red driver regression | Passed after implementation | `cargo test -p mc-test-harness --test real_client_agent_driver -- agent_driver_rejects_non_loopback_server_addr --exact --nocapture` first failed because `example.com:25565` was forwarded through the fake loopback bridge and wrote passed observations, then passed after the driver required a loopback `server_addr`, wrote failed observations, and made no bridge RPCs. |
| Driver contract surface | Passed | `cargo test -p mc-test-harness --test real_client_agent_driver -- --nocapture` passed 18/18 after adding the loopback server-address policy. |
| Python syntax | Passed | `python3 -m py_compile tools/real-client-agent-driver.py` passed after adding the parser; generated `tools/__pycache__` was removed. |
| Evidence limit | Partial | This is real-client driver validation-tooling evidence only. It does not run a real client, prove the target is Solaris beyond loopback address policy, prove gameplay correctness, provide vanilla oracle evidence, broaden M94 coverage, or cover performance/soak behavior. |

## 2026-07-04 Real-Client Exact Client Gate Validation Slice

This local Q2 stabilization slice makes
`tools/run-real-client-regression.sh --validate-run` require exact
`"client_gate": "agent-run-real-client"` instead of accepting the misspelled
`agent-runreal-client` alias.

| Gate | Result | Notes |
|---|---|---|
| Red runner regression | Passed after implementation | `cargo test -p mc-test-harness --test real_client_manifest -- validate_run_rejects_misspelled_client_gate --exact --nocapture` first failed because a synthetic passed artifact with `client_gate: agent-runreal-client` printed `validated`, then passed after `--validate-run` required the exact gate and named `client_gate` in the diagnostic. |
| Runner contract surface | Passed | `cargo test -p mc-test-harness --test real_client_manifest -- --nocapture` passed 7/7 after adding the exact-gate rejection. |
| Shell syntax | Passed | `bash -n tools/run-real-client-regression.sh` passed after tightening the gate check. |
| Evidence limit | Partial | This is real-client runner validation-tooling evidence only. It does not run a real client, prove gameplay correctness, provide vanilla oracle evidence, broaden M94 coverage, or cover performance/soak behavior. |

## 2026-07-04 Real-Client Observations Schema Validation Slice

This local Q2 stabilization slice makes
`tools/run-real-client-regression.sh --validate-run` require exact
`"schema": "solaris.real_client_observations.v1"` in `observations.json`.

| Gate | Result | Notes |
|---|---|---|
| Red runner regression | Passed after implementation | `cargo test -p mc-test-harness --test real_client_manifest -- validate_run_rejects_wrong_observations_schema --exact --nocapture` first failed because a synthetic passed artifact with `schema: solaris.real_client_observations.v0` printed `validated`, then passed after `--validate-run` rejected the wrong schema and named `schema` in the diagnostic. |
| Runner contract surface | Passed | `cargo test -p mc-test-harness --test real_client_manifest -- --nocapture` passed 8/8 after adding the schema rejection. |
| Shell syntax | Passed | `bash -n tools/run-real-client-regression.sh` passed after adding the parsed metadata check. |
| Evidence limit | Partial | This is real-client runner validation-tooling evidence only. It does not run a real client, prove gameplay correctness, provide vanilla oracle evidence, broaden M94 coverage, or cover performance/soak behavior. |

## 2026-07-04 Real-Client Quality Label Validation Slice

This local Q2 stabilization slice makes
`tools/run-real-client-regression.sh --validate-run` require exact
`"quality_label": "stabilization"` in `observations.json`.

| Gate | Result | Notes |
|---|---|---|
| Red runner regression | Passed after implementation | `cargo test -p mc-test-harness --test real_client_manifest -- validate_run_rejects_wrong_quality_label --exact --nocapture` first failed because a synthetic passed artifact with `quality_label: release-ready` printed `validated`, then passed after `--validate-run` rejected the wrong label and named `quality_label` in the diagnostic. |
| Runner contract surface | Passed | `cargo test -p mc-test-harness --test real_client_manifest -- --nocapture` passed 9/9 after adding the quality-label rejection. |
| Shell syntax | Passed | `bash -n tools/run-real-client-regression.sh` passed after adding the parsed metadata check. |
| Evidence limit | Partial | This is real-client runner validation-tooling evidence only. It does not run a real client, prove release readiness, prove gameplay correctness, provide vanilla oracle evidence, broaden M94 coverage, or cover performance/soak behavior. |

## 2026-07-04 Save-All Stop Command Path Slice

This local O4 stabilization slice adds a packet-harness command-path regression
for permitted player-issued `/save-all` and `/stop`. The harness sends
`ServerboundChatCommand`, decodes the returned `ClientboundSystemChat` text
component NBT, checks exact operator-facing feedback, and verifies `/stop`
signals the shared `ShutdownHandle`.

| Gate | Result | Notes |
|---|---|---|
| Command-path regression | Passed with mutation check | `cargo test -p mc-test-harness --test commands save_all_and_stop_commands_report_feedback_and_signal_shutdown -- --nocapture` passed for `/save-all` feedback `Saved 0 players, 0 entities, 0 chunks`, `/stop` feedback `Saved all state; stopping server`, and `shutdown.is_requested()`. A temporary mutation to the `/save-all` feedback text failed at the exact text assertion, and a temporary mutation suppressing `config.shutdown.request()` failed at the shutdown assertion; restoring production code made the test pass again. |
| Evidence limit | Partial | This is TCP packet-harness command-routing and feedback evidence on an in-memory no-world server. It does not prove disk-backed save durability, backup/restore, crash recovery, save failure feedback, `/stop` drain behavior, real-client/manual operation, public deployment safety, or health/status output. |

## 2026-07-04 Missing World Directory Check Warning Slice

This local O4 stabilization slice makes `mc-server --check` surface a degraded
operator state that the runtime already logs: configs without `[data].world_dir`
start without persistent world storage and chunk streaming readiness.

| Gate | Result | Notes |
|---|---|---|
| Red CLI regression | Passed after implementation | `cargo test -p mc-server --test cli check_reports_missing_world_dir_warning -- --nocapture` first failed because a loopback config with no `[data].world_dir` rendered `operator_warnings: []`, then passed after `--check` included `missing_world_dir`. |
| CLI surface | Passed | `cargo test -p mc-server --test cli -- --nocapture` passed 7/7 after the new warning. |
| Evidence limit | Partial | This is operator check-output evidence for one missing-world-dir readiness warning. It does not validate filesystem permissions, backup/restore, `/save-all`/`/stop`, existing-world migration, corruption recovery, dependency/security audit, public deployment safety, or data/protocol drift. |

## 2026-07-04 World Directory File Check Warning Slice

This local O4 stabilization slice makes `mc-server --check` surface a configured
world path that cannot be opened as a world directory because it is an existing
file. The runtime would fail `ensure_world_region_root` and continue without a
usable world; the check path now exposes that before serve time as
`world_dir_not_directory`.

| Gate | Result | Notes |
|---|---|---|
| Red CLI regression | Passed after implementation | `cargo test -p mc-server --test cli check_reports_world_dir_file_warning -- --nocapture` first failed because a config whose `[data].world_dir` pointed at a temp file rendered `operator_warnings: []`, then passed after `--check` included `world_dir_not_directory`. |
| Evidence limit | Partial | This is operator check-output evidence for one existing-path filesystem type warning. It does not validate filesystem permissions, missing-directory creation, backup/restore, `/save-all`/`/stop`, existing-world migration, corruption recovery, dependency/security audit, public deployment safety, or data/protocol drift. |

## 2026-07-04 Missing World Path Check Warning Slice

This local O4 stabilization slice makes `mc-server --check` surface a configured
`[data].world_dir` path that does not exist on disk. Serve-time behavior is
unchanged: Solaris may create a fresh world directory, but the check output now
warns operators before they mistake that path for an existing persistent world.
Non-`NotFound` metadata failures are reported separately as
`world_dir_metadata_unavailable` rather than mislabeled as missing paths, but
that defensive branch is not counted as separately tested in this slice.

| Gate | Result | Notes |
|---|---|---|
| Red CLI regression | Passed after implementation | `cargo test -p mc-server --test cli check_reports_missing_world_dir_path_warning -- --nocapture` first failed because a loopback config with a configured absent path rendered `operator_warnings: []`, then passed after `--check` included `world_dir_missing_on_disk`. |
| CLI surface | Passed | `cargo test -p mc-server --test cli -- --nocapture` passed 12/12 after the new warning. |
| Evidence limit | Partial | This is operator check-output evidence for one configured missing-path warning. It does not validate permissions, backup/restore, `/save-all`/`/stop`, existing-world migration, corruption recovery, dependency/security audit, public deployment safety, or actual serve-time world creation. |

## 2026-07-04 World Directory Parent File Check Warning Slice

This local O4 stabilization slice makes `mc-server --check` distinguish a
missing `[data].world_dir` that can be created from a missing path blocked by an
existing file-valued ancestor. The latter now reports
`world_dir_parent_not_directory`; serve-time `create_dir_all(world_dir/region)`
would fail for that shape and start without usable world storage.

| Gate | Result | Notes |
|---|---|---|
| Red CLI regression | Passed after implementation | `cargo test -p mc-server --test cli check_reports_world_dir_parent_file_warning -- --nocapture` first failed because a config whose `[data].world_dir` was below a temp file rendered `world_dir_metadata_unavailable`, then passed after `--check` included `world_dir_parent_not_directory` and did not include `world_dir_missing_on_disk`. |
| World-dir warning filter | Passed | `cargo test -p mc-server --test cli world_dir -- --nocapture` passed 4/4, including the existing plain missing-path regression and the new file-parent regression. |
| Evidence limit | Partial | This is operator check-output evidence for one synthetic file-parent path failure. It does not validate permissions, symlink races, backup/restore, `/save-all`/`/stop`, existing-world migration, corruption recovery, public deployment safety, or actual serve-time world creation/failure handling. |

## 2026-07-04 World Region File Check Warning Slice

This local O4 stabilization slice makes `mc-server --check` surface an existing
`[data].world_dir` whose legacy `region` path is blocked by a file and no modern
`dimensions/minecraft/overworld/region` directory exists. The runtime
`ensure_world_region_root` path would fail to create the legacy region
directory for that shape and start without usable world storage; the check path
now warns as `world_region_not_directory`.

| Gate | Result | Notes |
|---|---|---|
| Red CLI regression | Passed after implementation | `cargo test -p mc-server --test cli check_reports_world_region_file_warning -- --nocapture` first failed because a config whose `[data].world_dir` was a directory with a file-valued `region` child rendered `operator_warnings: []`, then passed after `--check` included `world_region_not_directory` without `world_dir_not_directory` or `world_dir_missing_on_disk`. |
| Modern-layout suppression regression | Passed with mutation check | `cargo test -p mc-server --test cli check_accepts_legacy_region_file_when_modern_region_exists -- --nocapture` passed after adding coverage for a file-valued legacy `region` path when `dimensions/minecraft/overworld/region` exists. A temporary mutation that ignored the modern-layout directory made the test fail by emitting `world_region_not_directory`; restoring the guard made it pass again. |
| Runtime helper regression | Passed with mutation check | `cargo test -p mc-server ensure_world_region_root_reports_blocked_legacy_region_file -- --nocapture` passed for the serve-time helper path by asserting a file-valued legacy `region` root returns the `creating empty world region directory` error. A temporary mutation that treated an existing file as acceptable made the test fail with `unwrap_err()` receiving `Ok(())`; restoring the directory-only guard made it pass again. |
| World-dir warning filter | Passed | `cargo test -p mc-server --test cli world_dir -- --nocapture` passed 4/4, preserving the existing absent-config, missing-path, file-path, and file-parent warning coverage. |
| Evidence limit | Partial | This is operator check-output evidence plus direct `ensure_world_region_root` helper evidence for one synthetic blocked legacy-region path. It does not validate permissions, symlink races, modern-layout corruption, backup/restore, `/save-all`/`/stop`, existing-world migration, corruption recovery, public deployment safety, or end-to-end serve startup/fallback behavior. |

## 2026-07-04 Vanilla Sidecar Metadata Drift Warning Slice

This local D1/W1 stabilization slice makes `mc-server --check` read
`data.vanilla_data_dir/version.json` when a sidecar is configured and warn when
the metadata is missing, unreadable as UTF-8, malformed, missing required `id`,
`world_version`, or `protocol_version` fields, or declares
release/world/protocol values that differ from Solaris'
`mc_protocol::TARGET_RELEASE`, `WORLD_VERSION`, or `PROTOCOL_VERSION`
constants. The warnings point operators back to `tools/extract-vanilla-data.sh`
before a stale sidecar is treated as authoritative at serve time.

| Gate | Result | Notes |
|---|---|---|
| Red CLI regression | Passed after implementation | `cargo test -p mc-server --test cli check_reports_vanilla_data_protocol_mismatch_warning -- --nocapture` first failed because a synthetic sidecar with `protocol_version = 999999` rendered `operator_warnings: []`, then passed after `--check` included `vanilla_data_protocol_mismatch`. |
| Metadata regressions | Passed after implementation | `cargo test -p mc-server --test cli vanilla_data_version -- --nocapture` first failed for missing and invalid `version.json` metadata, then later failed for a valid JSON object without `protocol_version`. The follow-up focused run `cargo test -p mc-server --test cli vanilla_data_ -- --nocapture` first failed 4/10 for release-id mismatch, world-version mismatch, missing `id`, and missing `world_version`, then passed 10/10 after `--check` required all three fields and emitted separate release/world/protocol mismatch warnings. A focused read-failure regression first showed invalid UTF-8 bytes reporting `vanilla_data_version_missing`, then passed after non-not-found read errors mapped to `vanilla_data_version_invalid`; the final sidecar filter passed 11/11. |
| Evidence limit | Partial | This is operator check-output evidence for sidecar `version.json` presence, readability as UTF-8, parseability, required `id`, `world_version`, and `protocol_version`, plus release/world/protocol mismatch warnings. It is not a dependency/license/security audit, stale registry/tag/report schema validation, vanilla oracle evidence, runtime sidecar load enforcement, or lockfile-change policy proof. |

## 2026-07-04 Vanilla Sidecar Root Check Warning Slice

This local D1/W1 stabilization slice makes `mc-server --check` validate the
configured `data.vanilla_data_dir` root before checking `version.json`. Missing
sidecar roots now report `vanilla_data_dir_missing_on_disk`; paths that exist
but are not directories report `vanilla_data_dir_not_directory`. Other metadata
failures have a defensive `vanilla_data_dir_metadata_unavailable` branch, but
that branch is not counted as separately tested in this slice.

| Gate | Result | Notes |
|---|---|---|
| Red CLI regressions | Passed after implementation | `cargo test -p mc-server --test cli vanilla_data_dir -- --nocapture` first failed 0/2 because both a missing sidecar root and a file-valued sidecar root collapsed to `vanilla_data_version_missing`; after implementation it passed 2/2 with root-specific warning codes. |
| CLI surface | Passed | `cargo test -p mc-server --test cli -- --nocapture` passed 14/14 after the new root warnings. |
| Evidence limit | Partial | This is operator check-output evidence for two synthetic sidecar-root path failures. It is not runtime sidecar load enforcement, stale registry/tag/report schema validation, dependency/license/security audit, vanilla oracle evidence, or lockfile-change policy proof. |

## 2026-07-04 Vanilla Sidecar Parent File Check Warning Slice

This local D1/W1 stabilization slice makes `mc-server --check` distinguish a
missing configured `data.vanilla_data_dir` that can be created by extraction
from a sidecar path blocked by an existing file-valued ancestor. The latter now
reports `vanilla_data_dir_parent_not_directory` instead of the generic
`vanilla_data_dir_metadata_unavailable`.

| Gate | Result | Notes |
|---|---|---|
| Red CLI regression | Passed after implementation | `cargo test -p mc-server --test cli check_reports_vanilla_data_dir_parent_file_warning -- --nocapture` first failed because a synthetic sidecar path below a temp file rendered `vanilla_data_dir_metadata_unavailable`, then passed after `--check` included `vanilla_data_dir_parent_not_directory` and did not include `vanilla_data_dir_missing_on_disk` or `vanilla_data_version_missing`. |
| Sidecar root filter | Passed | `cargo test -p mc-server --test cli vanilla_data_dir -- --nocapture` passed 3/3, including missing sidecar root, file-valued sidecar root, and file-parent sidecar root. |
| Evidence limit | Partial | This is operator check-output evidence for one synthetic sidecar parent-path failure. It is not runtime sidecar extraction, stale registry/tag/report schema validation, dependency/license/security audit, vanilla oracle evidence, symlink race coverage, or lockfile-change policy proof. |

## 2026-07-04 Vanilla Sidecar Registry Tree Warning Slice

This local D1/W1 stabilization slice makes `mc-server --check` warn when a
configured sidecar has current target `version.json` metadata but lacks the
required registry JSON tree under `data/minecraft`. The warning is
`vanilla_data_registry_tree_incomplete`, and the check follows
`mc_data::KNOWN_REGISTRIES` with a metadata-only recursive JSON-file presence
probe for each required registry directory.

| Gate | Result | Notes |
|---|---|---|
| Red CLI regression | Passed after implementation | `cargo test -p mc-server --test cli check_reports_vanilla_data_registry_tree_warning -- --nocapture` first failed because a synthetic sidecar with matching `version.json` but no registry tree rendered `operator_warnings: []`, then passed after `--check` included `vanilla_data_registry_tree_incomplete` without version missing/invalid/mismatch warnings. |
| Sidecar warning filter | Passed | `cargo test -p mc-server --test cli vanilla_data_ -- --nocapture` passed 13/13, including the existing sidecar root and `version.json` warnings plus the new registry-tree warning. |
| CLI surface | Passed | `cargo test -p mc-server --test cli -- --nocapture` passed 23/23 after the new warning. |
| Evidence limit | Partial | This is operator check-output evidence for one synthetic current-version sidecar missing the registry JSON tree. It does not parse registry JSON, prove tag/report/block-light/recipe/loot schema freshness, validate runtime sidecar loading, cover partial stale registry contents, handle symlink races, or provide vanilla oracle, dependency/license/security, or lockfile-change evidence. |

## 2026-07-04 Vanilla Block-Light Report Warning Slice

This local D1/W1 stabilization slice makes `mc-server --check` validate
`reports/block_light.json` with the existing `mc_data::block_light::load`
parser after the configured sidecar has matching target metadata and the
required registry tree is present. Missing, malformed, shape-invalid,
out-of-range, or wrong-release block-light reports now warn as
`vanilla_data_block_light_report_invalid`.

| Gate | Result | Notes |
|---|---|---|
| Red CLI regression | Passed after implementation | `cargo test -p mc-server --test cli check_reports_malformed_vanilla_block_light_warning -- --nocapture` first failed because a synthetic sidecar with current `version.json`, a minimal required registry tree, and a malformed `reports/block_light.json` rendered `operator_warnings: []`, then passed after `--check` included `vanilla_data_block_light_report_invalid` without registry-tree or version warnings. |
| Runtime loader filter | Passed | `cargo test -p mc-server effective_block_light -- --nocapture` passed 4/4, preserving existing serve-time block-light loader coverage for embedded fallback, missing sidecar file, wrong target version, and table coverage vs block states. |
| CLI surface | Passed | `cargo test -p mc-server --test cli -- --nocapture` passed 24/24 after the new warning. |
| Evidence limit | Partial | This is operator check-output evidence for one malformed block-light report and focused serve-time loader regressions. It does not prove broad block-light schema variants, registry/tag/recipe/loot schema freshness, runtime sidecar load enforcement through `--check`, stale partial registry contents, vanilla oracle evidence, dependency/license/security audit, or lockfile-change evidence. |

## 2026-07-04 Vanilla Tags Availability Warning Slice

This local D1/W1 stabilization slice makes `mc-server --check` reuse the
serve-time tags loader after target `version.json` metadata, required registry
tree presence, and block-light report checks pass. Missing `reports/registries.json`,
malformed tag support data, empty tags, or tags without required resolved
`minecraft:block`, `minecraft:item`, and `minecraft:entity_type` entries now
warn as `vanilla_data_tags_unavailable`.

| Gate | Result | Notes |
|---|---|---|
| Red CLI regression | Passed after implementation | `cargo test -p mc-server --test cli check_reports_missing_vanilla_tags_warning_after_block_light -- --nocapture` first failed because a synthetic sidecar with current `version.json`, a minimal required registry tree, valid `reports/block_light.json`, and a minimal `reports/registries.json` but no `data/minecraft/tags` rendered `operator_warnings: []`, then passed after `--check` included `vanilla_data_tags_unavailable` without version, registry-tree, or block-light warnings. |
| Runtime loader filter | Passed | `cargo test -p mc-server effective_tags -- --nocapture` passed 3/3, preserving existing serve-time tag loader coverage for empty sidecars, missing required tag registries, and tag registries without protocol ids. |
| Evidence limit | Partial | This is operator check-output evidence for one missing-tags shape and focused serve-time tag loader regressions. It does not prove broad tag schema freshness, registry report completeness, tag parity, recipe/loot schema freshness, runtime sidecar load enforcement through `--check`, vanilla oracle evidence, dependency/license/security audit, or lockfile-change evidence. |

## 2026-07-04 Vanilla Recipes Availability Warning Slice

This local D1/W1 stabilization slice makes `mc-server --check` reuse the
serve-time recipe loader after target `version.json` metadata, required registry
tree presence, block-light report, and tags checks pass. Missing recipe data,
malformed recipe JSON, or sidecars with no supported shaped, shapeless,
smelting, blasting, smoking, or campfire cooking entries now warn as
`vanilla_data_recipes_unavailable`.

| Gate | Result | Notes |
|---|---|---|
| Red CLI regression | Passed after implementation | `cargo test -p mc-server --test cli check_reports_missing_vanilla_recipes_warning_after_tags -- --nocapture` first failed because a synthetic sidecar with current `version.json`, a minimal required registry tree, valid `reports/block_light.json`, minimal `reports/registries.json`, and minimal resolved block/item/entity_type tags but no `data/minecraft/recipe` rendered `operator_warnings: []`, then passed after `--check` included `vanilla_data_recipes_unavailable` without version, registry-tree, block-light, or tags warnings. |
| Runtime loader filter | Passed | `cargo test -p mc-server effective_recipes -- --nocapture` passed 3/3, preserving existing serve-time recipe loader coverage for embedded fallback, usable vanilla sidecar recipes, and configured sidecars with no supported recipes. |
| Evidence limit | Partial | This is operator check-output evidence for one missing-recipe-data shape and focused serve-time recipe loader regressions. It does not prove broad recipe schema freshness, recipe parity, recipe-book behavior, loot schema freshness, runtime sidecar load enforcement through `--check`, vanilla oracle evidence, dependency/license/security audit, or lockfile-change evidence. |

## 2026-07-04 Vanilla Loot Availability Warning Slice

This local D1/W1 stabilization slice makes `mc-server --check` reuse the
serve-time loot loader after target `version.json` metadata, required registry
tree presence, block-light report, tags, and recipes checks pass. Missing loot
data, malformed loot JSON, or sidecars with no supported simple block/entity
drops now warn as `vanilla_data_loot_unavailable`.

| Gate | Result | Notes |
|---|---|---|
| Red CLI regression | Passed after implementation | `cargo test -p mc-server --test cli check_reports_missing_vanilla_loot_warning_after_recipes -- --nocapture` first failed because a synthetic sidecar with current `version.json`, a minimal required registry tree, valid `reports/block_light.json`, minimal `reports/registries.json`, minimal resolved block/item/entity_type tags, and one supported shapeless recipe but no `data/minecraft/loot_table` rendered `operator_warnings: []`, then passed after `--check` included `vanilla_data_loot_unavailable` without version, registry-tree, block-light, tags, or recipes warnings. |
| Runtime loader filter | Passed | `cargo test -p mc-server effective_loot -- --nocapture` passed 3/3, preserving existing serve-time loot loader coverage for embedded fallback, usable simple vanilla sidecar drops, and configured sidecars with no supported simple loot. |
| Evidence limit | Partial | This is operator check-output evidence for one missing-loot-data shape and focused serve-time loot loader regressions. It does not prove broad loot schema freshness, loot parity, multi-pool or conditional loot behavior, recipe schema freshness, runtime sidecar load enforcement through `--check`, vanilla oracle evidence, dependency/license/security audit, or lockfile-change evidence. |

## 2026-07-04 Locked Dependency Inventory Snapshot

This local D1 evidence snapshot records the locked Cargo graph without changing
dependencies or `Cargo.lock`. The repo still has no configured dependency
audit, advisory, cargo-vet, SBOM, or third-party license policy tooling.

| Gate | Result | Notes |
|---|---|---|
| Locked metadata inventory | Passed | `cargo metadata --locked --format-version 1` reported 194 total packages: 13 path/workspace packages, 181 registry packages, and 0 git-sourced packages. |
| Direct external dependencies | Passed | `cargo metadata --locked --format-version 1` reported 14 unique direct normal registry dependencies across workspace packages: `anyhow`, `bytes`, `clap`, `flate2`, `lz4_flex`, `md-5`, `serde`, `serde_json`, `thiserror`, `tokio`, `toml`, `tracing`, `tracing-subscriber`, and `uuid`; and 9 unique direct dev registry dependencies: `assert_cmd`, `bytes`, `criterion`, `predicates`, `proptest`, `serde_json`, `tempfile`, `tokio`, and `uuid`. |
| Duplicate-version tree | Partial | `cargo tree --locked --workspace --all-targets --duplicates` surfaced duplicate `getrandom`, `r-efi`, and `wit-bindgen` versions through the current dev/test transitive graph, primarily via `proptest`/`tempfile` and WASI support paths. No dependency change was made in this slice. |
| Compression reverse paths | Passed | `cargo tree --locked --workspace --all-targets -i flate2` showed `flate2 v1.1.9` is used by `mc-net`, `mc-protocol`, `mc-world`, and `mc-worldgen`; `cargo tree --locked --workspace --all-targets -i lz4_flex` showed `lz4_flex v0.11.6` enters through `mc-world`; `cargo tree --locked --workspace --all-targets -i libz-sys` failed with no matching package, so the current lockfile does not include `libz-sys`. |
| Audit tooling presence | Partial | `command -v cargo-deny` and `command -v cargo-audit` returned no paths; `cargo --list` exposed `metadata` and `tree` but not deny/audit/geiger/vet/about commands; repo search found no `deny.toml`, cargo-audit/cargo-vet/cargo-about config, or SBOM. |
| Evidence limit | Partial | This is a locked dependency inventory and local tooling-presence snapshot. It is not an advisory database scan, third-party license allow/deny review, source provenance review, unsafe-code audit, SBOM, lockfile remediation, stale protocol/data schema audit, vanilla oracle evidence, or runtime sidecar enforcement evidence. |

## 2026-07-04 Login Compression Boundary Evidence Slice

This local P2 stabilization slice adds synthetic raw-TCP evidence around the
login compression boundary. With `compression_threshold = 1`, the harness
observes `SetCompression` uncompressed, then inspects the next raw frame before
normal decode and requires a positive compressed-frame `data_length` for
`LoginSuccess`.

| Gate | Result | Notes |
|---|---|---|
| Raw TCP compression boundary | Passed first run | `cargo test -p mc-server --test login login_compresses_post_set_compression_frames_at_configured_threshold -- --nocapture` passed on the first run, proving the current implementation already sends `LoginSuccess` with compressed framing after `SetCompression` at threshold `1`. This is added normal-path evidence, not a bug-fix regression. |
| Login/config suites | Passed | `cargo test -p mc-server --test login -- --nocapture` passed 8/8, and `cargo test -p mc-server --test configuration -- --nocapture` passed 8/8. |
| Evidence limit | Draft debt | This is synthetic raw-TCP evidence for one compression threshold and one post-`SetCompression` frame. It is not vanilla oracle evidence, real-client evidence, malformed compressed-frame coverage, compression performance evidence, compression-level coverage, or broad play-state compression proof. |

## 2026-07-04 Login Malformed Compression Rejection Slice

This local P2 stabilization slice adds one synthetic raw-TCP fail-closed case
after compression is negotiated. The harness drains `LoginSuccess`, then sends a
compressed-layout client frame in place of `LoginAcknowledged` with
`data_length = 256` and a single non-zlib payload byte, requiring Solaris to
close the connection before Configuration.

| Gate | Result | Notes |
|---|---|---|
| Focused malformed compressed frame | Passed after fixture correction | `cargo test -p mc-server --test login login_rejects_malformed_compressed_ack_frame -- --nocapture` first failed before reaching the protocol path because the test used an over-16-character login name. After correcting the fixture to `BadZipAck`, the test passed and no production code changed. |
| Login suite | Passed | `cargo test -p mc-server --test login -- --nocapture` passed 9/9, including the prior compression-boundary case and the new malformed compressed-frame rejection case. |
| Evidence limit | Draft debt | This is one synthetic login-state malformed-zlib frame after `SetCompression`. It is not vanilla oracle evidence, real-client evidence, a malformed-frame corpus, below-threshold/mismatched-length server integration coverage, compression performance evidence, compression-level coverage, or broad play-state compression proof. |

## 2026-07-04 Falling-Block Landing/Removal Slice

This local B6 stabilization slice extends falling-block coverage beyond the M47
start window. A protocol client breaks the support under sand, captures the
`AddEntity` falling-block id and block-state data, then waits for a landing
`BlockUpdate`/`SectionBlocksUpdate`, a landing-chunk `LightUpdate`, and
`RemoveEntities` for that same entity.

| Gate | Result | Notes |
|---|---|---|
| Red protocol regression | Passed after implementation | `cargo test -p mc-test-harness --test physics_validation falling_block_lands_as_block_and_despawns_entity -- --nocapture` first failed waiting for falling-block landing/removal, then failed again after the light assertion because no landing `LightUpdate` arrived. It passed after the entity tick path placed grounded falling-block state into a non-solid cached landing cell through the shared background edit pipeline, broadcast the block delta/light update, and removed the entity. |
| Existing start regression | Passed | `cargo test -p mc-test-harness --test physics_validation falling_blocks_start_when_support_breaks -- --nocapture` and `cargo test -p mc-net falling_block -- --nocapture` still pass, preserving the M47 start evidence. |
| Evidence limit | Partial | This is protocol-client evidence for one sand landing/removal/light-update path in a local fixture. It is not fresh vanilla oracle evidence, owner-run PrismLauncher evidence, broad sand/gravel/anvil behavior, falling-anvil damage/breakage semantics, item drops, persistence/crash recovery, performance, or soak evidence. |

## 2026-07-04 Play Admission Disconnect Reason Slice

This local P3 stabilization slice hardens the existing play-state admission
tests by decoding the `PlayDisconnect` NBT payload and requiring exact text for
two denial paths: server capacity and duplicate offline profile.

| Gate | Result | Notes |
|---|---|---|
| Focused play admission reasons | Passed | `cargo test -p mc-server --test play play_state_rejects -- --nocapture` passed both `play_state_rejects_second_client_when_server_is_full` and `play_state_rejects_duplicate_offline_profile`. The first attempted run failed to compile because `mc-server` tests did not directly depend on `mc-nbt`; the final test parses the reason through the repo NBT parser via a test-only dev-dependency. |
| Evidence limit | Partial | This is synthetic TCP/session harness evidence for two existing offline-mode admission failures and their user-facing disconnect text. It is not online-mode/session auth, operator file persistence, auth-concurrency, public deployment, real-client evidence, or vanilla oracle evidence. |

## 2026-07-04 Campfire In-Flight Disk Persistence Slice

This local E2/S1 slice persisted in-flight campfire cooking state into the
existing opaque block-entity storage path. At that checkpoint, the stored NBT
kept the visible `Items` payload plus Solaris-owned timer arrays
`solaris_cooking_remaining` and `solaris_cooking_total`; the later vanilla
timer-NBT slice below replaces new writes with vanilla timer arrays while
keeping legacy reads.

| Gate | Result | Notes |
|---|---|---|
| Red disk persistence regression | Passed after implementation | `cargo test -p mc-test-harness --test block_edit -- survival_campfire_in_flight_state_flushes_to_disk --exact --nocapture` first failed after a disk-backed server inserted porkchop into a campfire, flushed dirty chunks, reopened `WorldStorage`, and found no persisted campfire block entity. It passed after insert/tick paths wrote opaque campfire block-entity NBT with the input stack and timer arrays. |
| Existing campfire route | Passed | `cargo test -p mc-test-harness --test block_edit -- survival_campfire_cooks_held_input_into_item_entity --exact --nocapture` still passes, preserving the visible input, cooked item entity, pickup, and clear-update path. |
| Unit campfire surface | Passed | `cargo test -p mc-net campfire -- --nocapture` passed the existing four campfire unit tests after adding persisted timer state. |
| Evidence limit | Partial | This is protocol-harness/storage evidence for disk flush/reopen of one in-flight campfire input. It does not hydrate in-flight cooking back into a fresh `SessionRegistry` after server restart, prove vanilla campfire NBT field names, run a real client, provide vanilla oracle evidence, cover hopper/comparator automation, smoke/sound/particles, ejection vector/timing, or broad block-state parity. |

## 2026-07-04 Campfire In-Flight Restart Hydration Slice

This local E2/S1 slice hydrates Solaris-owned persisted campfire cook timers
from opaque block-entity NBT when the restarted server loads and emits the
chunk. The client-facing chunk block-entity tag strips the Solaris timer
arrays and keeps only update payload fields.

| Gate | Result | Notes |
|---|---|---|
| Red restart hydration regression | Passed after implementation | `cargo test -p mc-test-harness --test block_edit -- survival_campfire_in_flight_state_resumes_after_reopen --exact --nocapture` first failed after a fresh server reopened the same disk world and no cooked campfire output appeared after the chunk loaded. It passed after chunk preparation decoded persisted campfire `Items` plus `solaris_cooking_remaining`/`solaris_cooking_total`, restored an idempotent `SessionRegistry` cooking state, and resumed the output/clear-update path. |
| Campfire harness route | Passed | `cargo test -p mc-test-harness --test block_edit -- campfire --nocapture` passed the existing cook-to-output route, disk flush/reopen persistence route, and new restart hydration route. |
| Runtime/chunk/wire units | Passed | `cargo test -p mc-net campfire -- --nocapture` passed the four campfire unit tests; `cargo test -p mc-net chunk_stream -- --nocapture` passed the chunk-stream constructor/cache/backpressure surface after threading hydration metadata; `cargo test -p mc-world client_block_entities_strip_solaris_campfire_persistence_fields -- --nocapture` proved Solaris timer arrays are stripped from client-facing opaque block-entity NBT. |
| Evidence limit | Partial | This is protocol-harness lazy chunk-load hydration evidence for one campfire input in a restarted server. By itself it did not prove headless/no-player cooking progression before chunk load after restart; the later startup hydration slice below covers that narrower gap. It does not prove vanilla campfire NBT field names, real-client/manual coverage, vanilla oracle evidence, hopper/comparator automation, smoke/sound/particles, ejection vector/timing, performance, soak, or broad block-state parity. |

## 2026-07-04 Campfire Zero-Client Server-Tick Slice

This local E2/S1/S2 slice moves campfire cooking progression from the
per-client play loop to the shared 50ms server runtime ticker. The in-flight
cooking state remains owned by `SessionRegistry`; the server ticker only drives
the existing state machine and side effects once per server tick.

| Gate | Result | Notes |
|---|---|---|
| Red zero-client regression | Passed after implementation | `cargo test -p mc-test-harness --test block_edit -- survival_campfire_finishes_while_no_clients_are_connected --exact --nocapture` first failed after the only connected protocol client inserted porkchop into a campfire, disconnected for five seconds, reconnected a fresh protocol client, and no cooked item entity appeared within the short reconnect window. It passed after `run_campfire_cooking_ticks` was called from the server runtime ticker and the old per-client campfire tick call was removed. |
| Campfire harness route | Passed | `cargo test -p mc-test-harness --test block_edit -- campfire --nocapture` passed all four campfire harness tests: cook-to-output, disk flush/reopen persistence, restart hydration, and zero-client server ticking. |
| Unit campfire surface | Passed | `cargo test -p mc-net campfire -- --nocapture` passed the four campfire unit tests after moving the tick driver. |
| Evidence limit | Partial | This proves same-runtime zero-client campfire progression and reconnect visibility through the protocol harness. By itself it did not prove no-player progression before chunk load after a fresh restart; the later startup hydration slice below covers that narrower gap. It does not prove vanilla campfire NBT field names, real-client/manual coverage, vanilla oracle evidence, hopper/comparator automation, smoke/sound/particles, ejection vector/timing, performance, soak, or broad block-state parity. |

## 2026-07-04 Campfire Restart Pre-Client Server-Tick Slice

This local E2/S1 slice hydrates persisted in-flight campfire cooking during
server bind by scanning existing disk chunks without generation or cache
mutation. After startup hydration, the shared server ticker can finish
campfire cooking before any client loads the chunk.

| Gate | Result | Notes |
|---|---|---|
| Red pre-client restart regression | Passed after implementation | `cargo test -p mc-test-harness --test block_edit -- survival_campfire_finishes_after_restart_before_any_client_reconnects --exact --nocapture` first failed after a disk-backed server inserted porkchop into a campfire, flushed the in-flight timer, stopped, restarted fresh, waited five seconds with no clients, then reconnected and no cooked item entity appeared within the short reconnect window. It passed after `WorldStorage::visit_existing_chunks_without_generation` and `hydrate_persisted_campfire_cooking` restored persisted campfire cooking during `bind`, before client chunk streaming. |
| Storage scanner unit | Passed | `cargo test -p mc-world visit_existing_chunks_without_generation_scans_disk_without_cache_mutation -- --nocapture` passed, proving the disk scanner visits existing chunks while leaving chunk and region caches empty. |
| Campfire harness route | Passed | `cargo test -p mc-test-harness --test block_edit -- campfire --nocapture` passed all five campfire harness tests, including disk persistence, lazy restart hydration, same-runtime zero-client ticking, and pre-client post-restart ticking. |
| Unit campfire surface | Passed | `cargo test -p mc-net campfire -- --nocapture` passed the four campfire unit tests after startup hydration wiring. |
| Evidence limit | Partial | This proves one protocol/storage path for no-client campfire progression before chunk load after a fresh restart. It does not prove vanilla campfire NBT field names, real-client/manual coverage, vanilla oracle evidence, hopper/comparator automation, smoke/sound/particles, ejection vector/timing, broad region-scan performance, crash recovery, soak, or broad block-state parity. |

## 2026-07-04 Campfire Vanilla Timer NBT Slice

This local E2/S1 slice aligns persisted campfire cook timer field names and
progress semantics with the local vanilla 26.1.2 decompile. New persisted
campfire block entities write `CookingTimes` as spent progress and
`CookingTotalTimes` as recipe totals. The hydration path also keeps reading the
older Solaris-owned remaining/total arrays so worlds produced by the previous
local slice still resume.

| Gate | Result | Notes |
|---|---|---|
| Oracle check | Passed | `.analysis/decompiled/server-26.1.2/net/minecraft/world/level/block/entity/CampfireBlockEntity.java` shows vanilla load/save uses `CookingTimes` and `CookingTotalTimes`; `CookingTimes` maps to cooking progress, not remaining ticks. |
| Red vanilla-field regression | Passed after implementation | `cargo test -p mc-test-harness --test block_edit -- survival_campfire_in_flight_state_flushes_to_disk --exact --nocapture` first failed when the disk-persistence harness expected `CookingTimes`/`CookingTotalTimes` and no `solaris_cooking_*` arrays. It passed after persistent campfire NBT wrote vanilla arrays. |
| Runtime and wire units | Passed | `cargo test -p mc-net campfire_persistent_nbt_uses_vanilla_cooking_arrays_and_reads_legacy -- --nocapture` passed, covering vanilla write/read plus legacy Solaris timer-array readback. `cargo test -p mc-world client_block_entities_strip_solaris_campfire_persistence_fields -- --nocapture` passed after client-facing opaque block-entity NBT stripped both vanilla timer arrays and legacy Solaris timer arrays, matching vanilla campfire update-tag behavior for timers. |
| Evidence limit | Partial | This proves oracle-backed campfire timer field names and spent-progress conversion for one in-flight input. It does not prove full vanilla item-stack/data-component NBT parity, real-client/manual coverage, hopper/comparator automation, smoke/sound/particles, ejection vector/timing, broad region-scan performance, crash recovery, soak, or broad block-state parity. |

## 2026-07-04 Campfire Lit-State Cooking Slice

This local E2 slice aligns campfire cook progression with the vanilla 26.1.2
ticker split: `CampfireBlock.getTicker` uses `cookTick` only when `LIT=true`
and uses `cooldownTick` when unlit. Solaris now snapshots in-flight campfire
positions, filters the server-ticker progression set through authoritative
block-state `lit=true`, and rechecks lit state before spawning cooked output.

| Gate | Result | Notes |
|---|---|---|
| Oracle check | Passed | `.analysis/decompiled/server-26.1.2/net/minecraft/world/level/block/CampfireBlock.java` shows server-side campfire cook ticking is gated by `blockState.getValue(LIT)`. `CampfireBlockEntity.placeFood` itself does not require lit, so the regression keeps input placement valid while preventing completion. |
| Red lit-state regression | Passed after implementation | `cargo test -p mc-test-harness --test block_edit -- survival_unlit_campfire_does_not_finish_cooking --exact --nocapture` first failed after the test placed porkchop, flipped the authoritative campfire block state to `lit=false`, waited beyond the recipe time, and observed an empty campfire block-entity update. It passed after the shared server campfire ticker skipped unlit campfire positions. |
| Campfire focused route | Passed | `cargo test -p mc-test-harness --test block_edit -- campfire --nocapture` passed all six campfire harness routes, including active cooking, disk persistence, lazy restart hydration, same-runtime zero-client ticking, pre-client post-restart ticking, and unlit no-finish behavior. `cargo test -p mc-net campfire -- --nocapture` passed the five campfire unit tests. |
| Evidence limit | Partial | This proves one protocol/storage path where an unlit campfire retains an input and does not finish cooking. It does not implement or prove full vanilla cooldown decrement semantics, relighting resume behavior, smoke/sound/particle, damage/effects, hopper/comparator automation, real-client/manual evidence, or soak/performance coverage. |

## Frozen M100 Denominator

| ID | Row | M100 scope | Status | Current evidence | Known gaps and debt | Target milestones |
|---|---|---|---|---|---|---|
| P1 | Login, configuration, known packs, registries, tags, and play start | In scope | `partial` | Early protocol harness, packet IDs from `wire-probe`/`javap`, join-path tests from M1/M36, M79 `spawn-smoke` start/play parity, and an ignored `configuration-phase` parity scenario in `crates/mc-test-harness/tests/parity_oracle.rs` that compares `KnownPacks`, `RegistryData`, `UpdateTags`, and `FinishConfiguration` against a local vanilla server using the extracted sidecar; after refreshing the configuration registry set to match vanilla 26.1.2 (`test_environment`/`test_instance` in, `enchantment_provider`/`trade_set`/`villager_trade` out), the scenario now passes locally. A 2026-06-22 real vanilla-client run of `m94-01-join-rejoin-chunks-movement` reached Play and rejoined Play after an agent-driven disconnect. | Broad real-client coverage is still incomplete, and protocol facts still need to be refreshed before new packet claims beyond the currently exercised configuration/start path. | M79, M94, M95, M100 |
| P2 | Login compression | In scope | `draft debt` | M8 spawn burst work kept compression as carry-over debt. The 2026-07-04 raw-TCP regression `login_compresses_post_set_compression_frames_at_configured_threshold` proves `SetCompression` is sent uncompressed and the following `LoginSuccess` uses compressed framing at threshold `1`. The same checkpoint adds `login_rejects_malformed_compressed_ack_frame`, which proves one positive-`data_length` malformed-zlib client frame after `SetCompression` closes before Configuration. | Set Compression still needs vanilla oracle/client proof, a broader malformed compressed-frame corpus, compression performance evidence, compression-level coverage, and broad play-state compression proof. | M79, M90, M91, M100 |
| P3 | Offline/online mode, encryption/session auth, duplicate names, whitelist/banlist/op persistence | In scope | `partial` | Local offline development path exists through prior session/admission work. The 2026-06-14 stabilization slice adds real TOML-to-TCP login/config coverage for a whitelist-enabled offline server allowing a normalized allow-listed player, alongside existing rejection coverage for online-mode, banlist, and whitelist denial paths. The 2026-07-04 play admission slice decodes `PlayDisconnect` NBT and asserts exact text for duplicate offline profile rejection and server-full rejection. The 2026-07-04 `mc-server --check` auth-list slice surfaces blank whitelist and banned-player entries before runtime drops them during normalization. | Online-mode/session auth is undecided; public release readiness is blocked without auth evidence or explicit private/local scope. Whitelist/banlist/duplicate admission evidence is focused static-config and TCP integration coverage only; the blank auth-list warning is check-output evidence only. No operator file persistence, real-client, auth-concurrency, or public deployment evidence has been run. | M89, M98, M100 |
| P4 | Spawn, respawn metadata, `SetDefaultSpawnPosition`, player abilities/gamemode start state | In scope | `partial` | M19/M22/M23 covered command/player state, death guard, and respawn slices. M84 keeps the existing default-spawn packet path and prefers `minecraft:overworld` for play spawn dimension when the registry contains it. | Full vanilla respawn bundle, default spawn packet/client compass behavior, and restart persistence need oracle/client evidence. | M84, M88, M94, M95, M100 |
| W1 | Sidecar availability, vanilla data drift, registry/data schema, stale/missing-data behavior | In scope | `partial` | Local `data/vanilla/reports` exists; M73 loads embedded fallback loot and optional sidecar simple drops. M80 keeps unsupported loot functions, unsupported rolls/bonus rolls, and non-constant or conditional `set_count` shapes skipped rather than silently green. The 2026-07-04 `mc-server --check` slices now warn when a configured sidecar root is missing on disk, is not a directory, or is blocked by a file-valued ancestor; when `version.json` is missing, unreadable/invalid, missing required `id`/`world_version`/`protocol_version`, or has release/world/protocol values that differ from Solaris constants; when a current-version sidecar lacks required registry JSON under `data/minecraft`; after sidecar root, target `version.json`, and registry-tree presence checks pass, when `reports/block_light.json` is missing, malformed, shape-invalid, out of range, or targets the wrong release; after the prior gates pass, when vanilla tags are unavailable or lack required resolved block/item/entity_type entries; after those gates pass, when vanilla recipes are unavailable or contain no supported recipe entries; and, after those gates pass, when vanilla loot tables are unavailable or contain no supported simple block/entity drops. | Broader sidecar extraction/version checks, stale tag/report/recipe/loot schema validation, broader block-light schema variants, partial stale registry contents, stale/missing-data operator behavior beyond sidecar-root, `version.json`, registry-tree, block-light, tag-availability, recipe-availability, and loot-availability checks, and unsupported datapack feature reporting still need M98 validation; unsupported datapack reporting is loader-test/doc visible, not yet broad operator-facing readiness evidence. | M80, M98, M100 |
| W2 | Anvil loading/saving and compression variants, including LZ4 | In scope | `partial` | M2 real `.mca` round-trip exists. The 2026-06-14 storage slices add tests for synthetic gzip, uncompressed, raw LZ4 block streams, unknown compression rejection, oversized compression-flag rejection, and root-extras preservation through codec plus `WorldStorage` edit/flush/reopen paths. Focused gates passed for `anvil::region::tests`, chunk-NBT extras/disk round-trip tests, and `storage::tests::unknown_root_extras_survive_world_storage_edit_flush_reopen`. | Compression coverage is still synthetic except for existing local real-region smoke tests; no fresh vanilla oracle, real-client, crash-recovery, broad storage concurrency, or performance evidence has been run for W2. | M88, M100 |
| W3 | Worldgen and generated exploration | In scope | `accepted divergence` | M5-M7/M20-M21 landed local deterministic worldgen and terrain features. M77 live generated-world probe confirmed Solaris can pre-generate and persist the `[-8..8]` spawn square: 289 chunks, 4 region files, zlib compression id 2, no missing/extra chunks. The 2026-06-22 baked-light/warm-cache probes kept the same functional generated-world stream while reducing fresh and warmed full-window stream times to `327` ms and `338` ms respectively; the runtime-light persistence follow-up kept the warmed stream at `323` ms with `light_compute_ms=0` and an immediate restart at `baked=0`/`flushed=0`. The 2026-07-04 dirty-border startup regression covers an existing disk-backed world with baked view-square light and missing light-border chunks, proving generated warm-cache border chunks stay resident/dirty for save-all instead of being hidden runtime pressure. The 2026-07-04 disk-backed generated-world startup gate covers a fresh temp world through the `mc-server` binary, leaves 361 generated spawn-window chunks resident/dirty before listener, flushes them through `/stop` save-all, restarts the same world, and emits the 289-chunk view-distance-8 stream on both runs. The 2026-07-05 existing-world missing-light gate covers a disk-backed unbaked spawn window through `mc-server`, bakes 289 view-distance chunks in memory, logs `flushed=0`/`dirty=289`, emits the 289-chunk stream, and flushes those chunks on `/stop`. | Vanilla worldgen, structures, biome parity, and broad feature parity are not claimed. Generated-world performance remains non-green because these are ignored local debug gates, historical lock waits/shutdown issues, crash-before-save durability, and broad profile evidence are still open. | M82, M90, M91, M94, M95, M100 |
| C1 | Chunk streaming, chunk visibility, and rejoin stability | In scope | `partial` | M3/M4 chunk harness, PrismLauncher manual pass, and light/chunk oracle notes; M48 moved runtime isolation forward. M77 long `wire-probe` against `cargo run --bin mc-server -- --config example.toml` saw all 289 unique `LevelChunkWithLight` packets for view distance 8, no duplicates, no missing/extra chunks. M97 corrected the focused chunk-stream regression to exercise the same 289-chunk view-distance-8 window instead of a 441-chunk view-distance-10 mismatch. The 2026-07-04 ignored chunk-stream harness run kept that 289-chunk sidecar-dependent guard green while asserting `chunk_prepare` wait/hold counters advanced and in-memory `save_all_flush`/`player_persistence` counters did not change. The 2026-06-22 baked-light/warm-cache debug probes emitted all 289 view-distance-8 chunks in `327` ms fresh and `338` ms after restart, with no slow fetch chunks, no slow light chunks, and no dirty pressure flushes; the final runtime-light persistence probe emitted the same 289 chunks in `323` ms with `fetch_ms=5` and `light_compute_ms=0`. The 2026-07-04 disk-backed generated-world startup gate emitted the same 289 chunks after a fresh binary startup with first/ring1/ring2/full markers `33`/`41`/`54`/`350` ms and after restart with `33`/`42`/`56`/`345` ms, while keeping `pressure_flush_runs=0` and `light_compute_ms=0`. The 2026-07-05 existing-world missing-light gate emitted the same 289 chunks after startup rebake with first/ring1/ring2/full markers `33`/`44`/`56`/`347` ms, `pressure_flush_runs=0`, and `light_compute_ms=0`. A 2026-06-22 real vanilla-client `m94-01-join-rejoin-chunks-movement` run reached Play, moved, disconnected, observed server-side session release, rejoined Play, and wrote after-move/after-rejoin screenshots. | Chunk streaming is still not readiness-green: existing-world missing-light startup is covered only by one focused local gate, earlier warmed streams logged lock waits, and pending-chunk disconnects, cache pressure, slow clients, multi-client soak, broader real-client, and oracle evidence still need coverage. | M90, M91, M94, M96, M100 |
| C2 | Lighting payloads, relight, persisted `.mca` light arrays | In scope | `partial` | M4/M9 lighting tests and oracle notes; M49 snapshot/light-payload debt identified. M77 generated-world stream produced client chunk payloads but optional `block_light.json` assertions remained degraded. The 2026-06-14 storage slices re-emit decoded Anvil `BlockLight`/`SkyLight` section arrays, cover encode/decode with `mc-world` section-light tests, and cover `WorldStorage` flush/reopen persistence for present light arrays. The 2026-06-22 slices rebuild `ChunkLight` from stored section arrays during chunk encoding, bake generated or missing existing-world spawn light before first stream, persist it, preserve baked arrays for light-inert runtime edits, and persist recomputed relight for light-changing edits. | Relight evidence is still focused and lacks broad client visual evidence, startup light-bake budgets, and vanilla oracle coverage. Persisted light arrays now have focused storage, runtime-edit, and stream-performance coverage, but no fresh real-client/oracle evidence for the lighting row. | M82, M88, M90, M91, M95, M100 |
| B1 | Block break/place/use acknowledgements, reach checks, rejection/resync, neighbor updates | In scope | `partial` | M22/M23/M68 block-edit and invalid-action tests. The M100 rejected-placement slice adds real `mc-test-harness` packet/session regressions: `rejected_occupied_use_item_on_resyncs_clicked_and_target_before_ack` drives `ServerboundUseItemOn` against an occupied target and requires authoritative clicked-cell plus target-cell `BlockUpdate` packets before the matching `BlockChangedAck`; `rejected_occupied_bucket_use_item_on_resyncs_blocks_and_held_slot_before_ack` repeats the occupied-target path with a held water bucket and also requires a fresh-state-id authoritative hotbar slot-36 water-bucket `ContainerSetSlot` before ack; `rejected_out_of_reach_use_item_on_resyncs_clicked_and_target_before_ack` drives survival `ServerboundUseItemOn` from outside the 5-block reach against loaded clicked/target dirt cells and requires both cached loaded-cell `BlockUpdate`s before the matching ack; `rejected_out_of_reach_bucket_use_item_on_resyncs_blocks_without_held_slot_before_ack` repeats that out-of-reach path with a held water bucket and requires no held-slot `ContainerSetSlot` before ack. The 2026-07-04 world-border slice adds valid-wire `world_border_hit = true` `UseItemOn` rejections that first failed by placing dirt into an air target and then by loading/resyncing far clicked/target cells, then passed with loaded-only resync/no-resync behavior before ack. These harness regressions are local-sidecar-dependent and degrade/skip when `data/vanilla/reports/blocks.json` or `data/vanilla/reports/registries.json` is absent. A later M100 early-break slice tightens `survival_break_requires_timed_stop_before_mutation`: it now seeds a deterministic stone target, proves early `STOP_DESTROY_BLOCK` does not mutate, and requires an authoritative target-cell stone `BlockUpdate` before the matching rejection ack; the first run failed with `rejected break ack arrived before target-cell BlockUpdate`, then passed after the incomplete-break rejection path sent a resync before ack. A 2026-07-04 out-of-reach survival break slice adds loaded-target resync and far-target no-load/no-resync coverage for `StartDestroyBlock`; the loaded red run acked before the target `BlockUpdate`, the far red run emitted a generated target `BlockUpdate`, and the fixed route uses cached loaded-cell resync before ack. A 2026-07-04 sugar-cane cascade-drop slice adds a survival support-break regression that requires the dirt support replacement, three cane cascade removals, and the resulting `minecraft:sugar_cane x3` pickup after the first red run cleared the cascade without drops. A 2026-06-22 real vanilla-client agent run of `m94-02b-rejected-block-resync` passed the occupied solid, out-of-reach solid, and occupied water-bucket fallback/resync observations and wrote a screenshot artifact. A 2026-06-22 real vanilla-client agent run of `m94-02a-solid-place-break-drop` validated `.analysis/real-client-runs/20260622T144628Z-m94-regression-pack`: it observed target air after a server-timed dirt-like break, a visible dirt drop, held-stack pickup restoration, successful dirt placement through the real-client item-use path, and the expected held-stack decrement. | Broader reach/anti-cheat behavior, stale edits without CAS, broader occupied-target water-bucket fallback variants, the full invalid interaction matrix, vanilla oracle evidence, and real-client desync coverage beyond these focused scenarios are incomplete. | M82, M89, M94, M95, M100 |
| B2 | Doors, trapdoors, buckets, and common block-use interactions | In scope | `partial` | Existing readiness lists usable doors/trapdoors/buckets; block-edit harness covers parts. The 2026-06-22 `m94-02a-solid-place-break-drop` real-client run adds one focused accepted solid-block item-use placement observation through a real vanilla client. | Door/trapdoor, broader bucket, and wider common block-use manual/client retests remain incomplete; exact sounds/events/statistics are not claimed. | M82, M94, M95 |
| B3 | Fluids: water/lava placement, spread, replacement, and interactions | In scope | `partial` | M44/M47 captured narrow water/lava and replacement scenarios, including `.analysis/physics-oracles/water-lava-replacement.vanilla.capture`. The M100 bucket rejection slice adds a focused local-sidecar-dependent packet/session regression for occupied-target water-bucket fallback/resync: both authoritative blocks and the held bucket slot with a fresh inventory state id must arrive before ack. The same occupied water-bucket fallback path passed in the 2026-06-22 real vanilla-client `m94-02b-rejected-block-resync` run. The 2026-06-22 real vanilla-client `m94-02c-water-bucket-place-pickup` run placed water through the real-client item-use path, observed the held item become an empty bucket, picked the source back up, observed the source clear, and restored the held water bucket; the supporting `mc-net` regression `removed_bucket_source_drains_own_spread_from_source_cell` guards the stale-flow source-refill bug found by the first attempted run. The shared-clock fix adds `interaction_fluid_scheduling_uses_shared_simulation_tick`, which failed before the fix with a trigger tick of `5` instead of `105`, and the packet/session harness `water_bucket_spread_waits_for_scheduled_fluid_delay`, which drives `ServerboundUseItemOn` with a water bucket after the server has already ticked and requires no water-spread `BlockUpdate` before the scheduled delay window. The scheduled-fluid save/restart slice adds `save_all_persists_scheduled_fluid_ticks_as_remaining_restart_delay`, which failed before the fix with a fresh-runtime trigger tick of `112` instead of `12` and now covers `save_all`/Anvil/reopen delay accounting. The M100 lava/water harness `lava_bucket_next_to_water_solidifies_through_scheduled_fluid_tick` drives `UseItemOn` with a lava bucket next to an existing water source, observes the lava-source placement/ack, then observes scheduled fluid tick solidification to obsidian; it passed on the first run, so it is added normal-path evidence rather than a bug-fix regression. | Broad fluid spread topology, additional lava-water variants, additional bucket/no-op edge cases, fresh vanilla oracle execution, real-client scheduled-spread/restart/solidification evidence, and broader real-client feel remain open. The water-bucket fallback and scheduled-spread/solidification harnesses degrade/skip when the required `data/vanilla/reports` sidecar files are absent. | M82, M94, M95, M100 |
| B4 | Player water movement and swim feel | In scope | `blocked` | M40-M45 produced Solaris/vanilla captures and prediction harnesses. The 2026-06-22 `m94-07-m40-m41-route-with-metrics` real-client route validated accepted water placement/pickup in the same artifact as visible drop/pickup and entity visibility. | Owner-facing frozen-world/water feel regression was not rerun green; client-local swim feel/camera/pitch remains manual-only and was explicitly blocked in the `m94-07` artifact. | M82, M91, M94, M97, M100 |
| B5 | Collision, fall damage, and movement boundaries | In scope | `partial` | M46 vanilla collision/fall capture and real play-path tests. | Entity-vs-entity push, broader step-up/fall variants, and visual feel remain incomplete. | M82, M85, M86, M94, M95 |
| B6 | Falling blocks: start, landing, placement/removal, and drops | In scope | `partial` | M47 covers falling-block start regression with vanilla capture. The 2026-07-04 `falling_block_lands_as_block_and_despawns_entity` protocol harness adds one sand landing/removal/light-update path: it captures the falling-block entity id, then requires a landing block update, landing-chunk `LightUpdate`, and `RemoveEntities`. | Broad sand/gravel/anvil behavior, falling-anvil damage/breakage semantics, item drops, persistence/crash recovery, fresh vanilla oracle, real-client evidence, and performance/soak coverage remain missing. | M82, M95, M100 |
| F1 | Crops and nether wart: growth, bonemeal, mature and immature drops | In scope | `partial` | M57-M59 and M72 harness/unit coverage for wheat, carrots, potatoes, beetroots, and nether wart. | Immature harvest variants are unit-only; deterministic local drops are not vanilla loot parity; exact RNG, soil/moisture/light, fortune, particles/sounds/events remain open. | M80, M82, M95, M100 |
| F2 | Stems, sweet berries, cocoa, saplings, and tree growth | In scope | `partial` | M60, M62-M64, M66, M72, and M76 harness/unit coverage. | Deterministic tree shapes and drops; no exact feature/RNG/support/collision/effects parity; several families lack real-client or vanilla evidence. | M80, M82, M95, M100 |
| F3 | Vertical plants: sugar cane, cactus, bamboo | In scope | `partial` | M61/M65/M76 unit and harness coverage for visible random-tick growth. M82 adds unit plus physics-harness coverage for visible cactus column clearing when a player places a dirt/solid side neighbor, plus negative unit coverage for cactus placement beside cactus. The 2026-07-04 sugar-cane cascade-drop slice adds physics-harness coverage for survival support break clearing a three-block sugar-cane column and producing a `minecraft:sugar_cane x3` pickup. | Sugar cane whitelist, cactus damage/collision, non-solid side-neighbor parity, bucket/special-mutation cactus neighbor revalidation, non-placement survivability sweeps, bamboo age/stage/leaves/bonemeal, vanilla drop/pickup oracle, and vanilla timing are incomplete. | M82, M95, M100 |
| F4 | Plant support and survivability semantics | In scope | `accepted divergence` | M68/M72/M76 documented deterministic support checks and cascades. | Growth generally does not re-check all vanilla soil/light/support rules; local deterministic semantics must stay documented. | M82, M95, M100 |
| L1 | Loot executor and common block/entity drops | In scope | `partial` | M73 sidecar simple subset and embedded fallback tests; harness covers stone, passive mob, zombie fallback, pickup removal. M80 implementer 3 added loader/runtime support for one-stack constant `minecraft:set_count` counts `1..=64`, with focused `mc-data` tests and an `mc-net` runtime drop-stack test. The 2026-07-04 sugar-cane cascade-drop slice proves support-cascaded sugar-cane blocks use the normal block-drop path and reach inventory pickup as `minecraft:sugar_cane x3`. | Full loot execution still lacks random count ranges, rolls, multiple stacks/pools, tool/entity predicates, fortune/silk, looting, equipment, exact XP, sounds/particles/statistics/events, vanilla oracle, and real-client evidence. | M80, M95, M100 |
| L2 | Dropped item lifecycle: spawn, merge, pickup, despawn, overflow, save/restart, two-client visibility | In scope | `partial` | M22/M40/M73 cover visible item entities and pickups in focused paths. The 2026-06-14 stabilization slice extends the real `save_all` storage test to preserve item stack, age, and pickup delay fields through save/load, alongside existing lifecycle field and restored pickup-delay tests. A follow-up focused test covers `save_all` followed by fresh `bind` startup reload of world time and item entity records. The 2026-06-22 `m94-02a-solid-place-break-drop` real-client run observed a visible dirt drop after a server-timed break and held-stack pickup restoration in `.analysis/real-client-runs/20260622T144628Z-m94-regression-pack`. The 2026-06-22 `m94-06` two-client run added one shared-drop visibility plus pickup-removal check: `SolarisAgent` broke a dirt block, `SolarisObserver` observed the resulting visible dirt item entity, `SolarisAgent` picked it up through the real client, and `SolarisObserver` observed the item removal in `.analysis/real-client-runs/20260622T210703Z-m94-regression-pack`. | Merge/overflow/contention/despawn, shared-pickup contention, XP pickup, and broader visible-drop regressions need evidence. Save/restart evidence is focused Solaris storage/session startup coverage only; no vanilla oracle, real-client drop restart, crash-recovery, or soak evidence has been run. | M80, M88, M94, M96, M100 |
| I1 | Player inventory, cursor state, held item use, crafting table, and recipe execution | In scope | `partial` | M22-M24 and M68-M69 focused inventory/crafting/container tests. The 2026-06-22 `m94-03a-inventory-oak-log-to-planks` real-client run sent the vanilla place-recipe packet for inventory container `0`, consumed one oak log, and observed four added oak planks in `.analysis/real-client-runs/20260622T161221Z-m94-regression-pack`. The 2026-06-22 `m94-03b-two-client-shared-chest` run `.analysis/real-client-runs/20260622T221125Z-m94-regression-pack` moved selected dirt into chest slot `0` through the real client's vanilla menu-click path and had a second real client observe that slot. The 2026-07-04 malformed inventory click slice adds one window-0 valid-wire fail-closed route: a matching-state-id `Pickup` with lying `changed_slots` and impossible carried-item prediction first advanced the resync state id `2 -> 3`, then passed after rejected inventory-click resyncs preserved the authoritative state id while keeping slot `36` and cursor state unchanged. | Recipe-book discovery/window sync, cursor recovery, crafting-table UI, broad containers/stations, broad malformed click fail-closed, exact vanilla click semantics beyond these focused routes, and broad recipe execution remain incomplete. | M80, M87, M89, M94, M95 |
| I2 | Recipe book and recipe data: shaped, shapeless, furnace, blasting, smoking, campfire | In scope | `partial` | Readiness records recipe loading for furnace-family and campfire variants. The 2026-06-22 `m94-03a-inventory-oak-log-to-planks` real-client run covers one shapeless recipe through the actual client/server place-recipe packet path using configured vanilla sidecar recipe display id `697`, with `mc-server` logging `recipe registry loaded entries=1146 source="vanilla_sidecar"`. | Broader recipe execution, unsupported features, recipe-book discovery/window sync, furnace-family client runs, and campfire recipe client runs need classification and tests. | M80, M87, M94, M95 |
| K1 | Containers: chest, barrel, furnace, smoker, blast furnace, clicks, viewers, block-entity ids | In scope | `partial` | M69-M71 harness and unit coverage for specialized furnace menus and barrel/furnace ids. M81 adds docs-only comparator debt classification; no runtime automation code landed. The 2026-06-22 `m94-03b` route passed a focused two-client real-client chest-slot deposit/observe path in `.analysis/real-client-runs/20260622T221125Z-m94-regression-pack`: primary chest placement/open/slot transfer/slot convergence/close and secondary same-chest open/slot `0` observation both passed with screenshots. The 2026-06-23 `m94-03c` route passed a focused two-client real-client live chest-slot update path in `.analysis/real-client-runs/20260622T232643Z-m94-regression-pack`: primary deposited dirt and kept the screen open, secondary quick-moved the slot out, and primary observed the still-open slot become empty. The 2026-06-23 shared-chest stale-click slice adds `stale_chest_click_after_peer_mutation_resyncs_without_mutating_storage`, which first proved an old peer-window click could pick up a just-mutated chest stack, then passed after chest windows checked a shared `SessionRegistry` chest state id before mutation. The 2026-06-23 shared-furnace stale-click slice adds `stale_furnace_click_after_peer_mutation_resyncs_without_mutating_storage` and `furnace_slot_dispatches_advance_and_carry_shared_state_id`, covering old furnace-window clicks after peer slot mutation and exact state-id `FurnaceSlots` dispatch. The 2026-07-04 `two_clients_stale_chest_click_after_peer_update_resyncs` and `two_clients_stale_furnace_click_after_peer_update_resyncs` harnesses cover live TCP/session versions for one chest slot update and one furnace input-slot update followed by stale observer clicks. The 2026-07-04 `unsupported_chest_click_modes_resync_without_trusting_client_slots` and `malformed_furnace_clicks_resync_without_trusting_client_slots` harnesses cover valid-wire unsupported chest/furnace click modes (`QuickCraft`, `Clone`, `PickupAll`) plus one malformed furnace pickup that lies about the carried item; post-click carried-item rollback rejects the conflicting non-empty client prediction and resyncs without mutating storage. | Barrel animation/sounds/events, furnace lit-state, hopper automation, comparator block propagation/client visibility, exact state machine, swap/throw edge cases, item max-stack metadata integration, save/restart, real-client concurrent-click coverage, broad shared-container convergence, broad malformed-click coverage, and contention need evidence. | M81, M87, M88, M94, M95 |
| K2 | Common survival stations beyond crafting/furnace family | In scope | `partial` | M87 adds a safe-rejection runtime path for unsupported common stations: brewing stand, anvil variants, enchanting table, smithing table, grindstone, stonecutter, loom, cartography table, composter, cauldron variants, lectern, fletching table, beacon, and crafter. `mc-net` unit coverage guards an independently declared expected block-id/name table for the policy, with a dedicated cauldron-family regression test; `mc-test-harness` covers stonecutter use over the real `UseItemOn` path and asserts no unimplemented screen opens and no adjacent held-block placement occurs. | Brewing, anvil repair/rename, enchanting, smithing transformations, grindstone repair/disenchant, stonecutting recipes, loom banners, cartography recipes/maps, composter fill/output, cauldron fill/drain/content interactions, lectern book interactions, fletching behavior, beacon effects/payments, and crafter automation remain deferred M101+ gameplay debt unless M95/M100 evidence reopens one as a blocker. No vanilla oracle or real-client evidence exists for M87. | M87, M94, M95, M100 |
| A1 | Automation and redstone-lite: hoppers, comparators, buttons, levers, plates, powered doors | In scope | `draft debt` | M81 draft MVP decision is docs-only after removing an unused comparator draft helper; no runtime automation code or helper-only tests landed. Current docs identify hopper/comparator debt for campfires, furnaces, barrels, and chests. | No item max-stack metadata integration, comparator block placement, neighbor updates, wire propagation, hopper transfer scheduling, powered controls, client-visible redstone state, campfire automation, oracle/client evidence, or performance evidence yet; complex redstone ordering, pistons, observers, and quasi-connectivity remain non-goal or post-MVP. | M81, M82, M91, M95 |
| E1 | Signs: regular, hanging, waxed, styled/filtered/clickable text | In scope | `partial` | M74 harness covers regular sign editor open, plain four-line text, mismatch rejection, and block-entity update. The 2026-06-22 `m94-04a-regular-sign-place-text` real-client run placed an oak sign through the real-client item-use path, observed the held stack decrement, observed the real sign editor, sent plain four-line text from inside the client process, and observed the client-side sign block entity converge in `.analysis/real-client-runs/20260622T153556Z-m94-regression-pack`. The follow-up `survival_sign_text_survives_flush_and_reopen` harness drives sign placement and text update through the packet path against disk-backed `WorldStorage`, flushes dirty chunks, reopens the world, and asserts the persisted sign NBT plus client block-entity wire record. | Hanging signs, waxed semantics, styled/filtered/clickable text, after-close visual assertion, sounds/statistics/events, broad real-client visual parity, and real-client restart evidence are not claimed. | M82, M94, M95 |
| E2 | Campfires: cooking, visual metadata, persistence, automation, effects | In scope | `partial` | M55/M75 harness covers held-input cooking, `Items` updates, cooked item entity, pickup, and completion clear. The 2026-07-04 disk-persistence slice now flushes and reopens a disk-backed world after inserting porkchop into a campfire and verifies an opaque campfire block entity persists the input stack plus cook timers. The follow-up restart hydration slice reopens the same disk world with a fresh server, loads the chunk, restores the in-flight cooking state into `SessionRegistry`, observes the cooked item output/clear-update path, and proves persistent timer arrays are stripped from client-facing chunk block-entity NBT. The zero-client server-tick slice disconnects the only protocol client after insertion, waits longer than the recipe time, reconnects a fresh protocol client, and observes the cooked item entity after server-ticker-driven progression. The restart pre-client slice scans existing disk chunks during bind, hydrates persisted campfire cooking before any client loads the chunk, waits longer than the recipe time with no clients, and observes the cooked item entity after reconnect. The vanilla timer-NBT slice uses the 26.1.2 decompile oracle for `CookingTimes`/`CookingTotalTimes`, writes new campfire timers with vanilla spent-progress semantics, and keeps legacy Solaris timer-array readback. The lit-state slice uses the 26.1.2 `CampfireBlock.getTicker` oracle to prove an in-flight input on a `lit=false` campfire does not finish while the server ticker runs. | Full vanilla campfire item-stack/data-component NBT parity, full unlit cooldown/relight resume semantics, hopper/comparator, smoke/sound/particle, ejection vector/timing, real-client/manual evidence, performance/soak, and broad block-state parity remain open. | M81, M82, M88, M94, M95 |
| E3 | Beds, sleep, time, weather, and safe dimension/portal boundaries | In scope | `partial` | Beds set respawn points in current readiness; respawn paths covered by earlier survival milestones. M84 adds a draft single-player night sleep skip to next morning through the existing `SetTime` path, uses current session world time for join-time time sync, keeps multiplayer quorum deferred with player feedback, documents weather as disabled, and keeps full portal travel outside scope while preferring the Overworld dimension on join. | Bed occupancy, wake-position fidelity, client-visible SetTime clock-map behavior/time sync, client-visible sleep animation parity, multiplayer quorum, sleep skip save/restart persistence, weather transitions, portal transfer, vanilla oracle evidence, and real-client evidence remain incomplete. | M84, M88, M94, M95 |
| V1 | Vehicles and movement objects: boats and minecarts | In scope | `draft debt` | M83 added `mc-entity` runtime vehicle state and focused unit coverage for boat-style mount/steer/dismount, passenger cleanup on removal, non-vehicle/double-mount rejection, snapshot sanitization, and minecart mount with steering explicitly unsupported. | No item spawn/use path, protocol passenger packets, client steering ingestion, collision bounds, rail movement, save/restart, or two-client visibility. Minecart rail physics, powered rails, chest/furnace minecarts, variants, sounds/events, and broad boat water physics remain deferred. | M83, M94, M95 |
| N1 | Entity ecosystem: spawn/despawn, sync, AI/pathing, damage, drops/XP, persistence | In scope | `draft debt` | M17/M20/M23/M24/M33/M73 cover initial mobs, combat, drops, and XP. M85 added a bounded loaded-chunk pathing primitive with runtime tests for flat movement, one-block step-up, simple detour, blocked terrain, unloaded-boundary refusal, and play-loop hostile refusal to step into an unloaded target chunk. The 2026-06-22 `m94-05-entities-combat-death-respawn` real-client run observed a newly summoned visible `minecraft:cow` near the player in `.analysis/real-client-runs/20260622T190401Z-m94-regression-pack`. | Entity-family table, spawn/despawn caps, terrain-material path predicates, persistence, two-client visibility, richer passive/hostile behavior, hostile combat, drops/XP, vanilla oracle, broader real-client evidence, and AI budget metrics need hardening. | M85, M86, M88, M94, M96 |
| N2 | Entity-vs-entity collision and push behavior | In scope | `draft debt` | M46 names this as deferred. | Collision/push with mobs, players, items, vehicles, and client-visible effects need evidence. | M82, M85, M94, M95 |
| G1 | Combat damage model: melee, projectiles, fall, fire/lava, drowning, suffocation, starvation | In scope | `partial` | M33/M54/M56 cover local combat, bows, shields, fall, mob drops/XP. | Damage sources, armor, durability, effects, attribution, timings, knockback, particles/sounds, and two-client evidence remain incomplete. | M86, M94, M95 |
| G2 | Bows/arrows and projectile lifecycle | In scope | `partial` | M54 harness/unit coverage for arrow spawn/move/consume/despawn/hit/damage. | Exact physics, metadata/sticking, tipped/spectral/crossbow, attribution, pickup edge cases, and oracle/client evidence are missing. | M86, M94, M95 |
| G3 | Shields and equipment behavior | In scope | `partial` | M56 harness covers frontal melee/arrow block after activation delay. M86 `mc-net` shield tests cover damage-scaled durability for blocked projectile and hostile-melee damage, emitted inventory slot updates, and active-use clearing when a shield breaks. | Axe disable, exact angle/timing, sounds/particles, and broader damage-source parity are missing; pose metadata is limited to the M70 using-item flag path, not full visual/effects parity. | M86, M94, M95 |
| G4 | Death, respawn, XP, and item/XP drop consequences | In scope | `partial` | M22/M23/M33/M73 cover death guards, respawn, XP, and drops in focused paths. The 2026-06-22 `m94-05-entities-combat-death-respawn` real-client run observed the real `DeathScreen`, performed the vanilla respawn client command, and ended back in Play with `current_screen=none`; the supporting harness now requires respawn chunk replay plus the vanilla-pinned load-start game event. | Full death bundle, XP curves/orb lifecycle, item loss/pickup under contention, and broader real-client evidence are incomplete. | M86, M88, M94, M96 |
| S1 | Save/restart persistence for players, chunks, block entities, containers, entities, drops, time/weather | In scope | `partial` | M2/M5/M23/M24/M55/M71/M75 include focused persistence slices. The 2026-06-14 Anvil/storage slices add focused coverage for saving and reloading section light arrays through the codec and `WorldStorage` flush/reopen path. The sign restart harness `survival_sign_text_survives_flush_and_reopen` now covers one client-visible block-entity path: packet-driven regular sign placement/text update, dirty flush, fresh `WorldStorage` reopen, and client block-entity wire reconstruction. The 2026-06-22 `m94-06-save-restart-two-client-visibility` real-client run placed one dirt marker through the real client, sent `save-all`, stopped Solaris with `kill -INT`, restarted, reconnected the same real client, and observed the marker persisted; the latest validated artifact with the same restart path is `.analysis/real-client-runs/20260622T210703Z-m94-regression-pack`. The scheduled-fluid save/restart slice adds a server save-all/Anvil/reopen regression proving pending fluid ticks persist as fresh-runtime remaining delay (`112` before fix, `12` after fix). The campfire in-flight disk-persistence slice adds one protocol/storage regression that flushes and reopens a disk-backed world after campfire input insertion and verifies the persisted opaque block entity retains the input stack plus remaining/total cook timers. The campfire restart hydration slice adds a fresh-server reopen regression that restores the persisted in-flight campfire state when the chunk loads and resumes output/clear-update behavior. The campfire zero-client slice adds same-runtime no-client cook progression plus reconnect visibility after insertion. The campfire restart pre-client slice adds bind-time disk-chunk scanning, startup hydration, no-client post-restart cook progression, and reconnect visibility before client-driven chunk hydration. | Crash recovery, fsync/rename durability, cursor/open-container recovery, dropped items, time/weather, LZ4, backup/restore, corrupted data handling, real-client scheduled-tick restart evidence, real-client restart beyond one placed block, real-client campfire restart evidence, and full restart/client evidence remain open. | M88, M98, M100 |
| S2 | Multiplayer visibility, contention, disconnect/reconnect, slow readers, shared edits/containers/pickups | In scope | `partial` | M36 session/admission/reconnect and pickup tests; M69 container filters. M96 added an ignored bounded protocol-harness replay scaffold for sequential same-block edit contention surrogate, reconnect after move/drop, measured slow-reader outbound pressure, and entity broadcast visibility, but local missing vanilla reports made the runtime execution degraded/skipped in implementer 19/20. The 2026-06-22 `m94-01` real-client run adds one single-client disconnect/rejoin path after movement, with prompt server-side session release before reconnect. The 2026-06-22 `m94-06` artifact adds one single-client reconnect after a real server restart and marker-persistence check, one two-client live block-edit visibility check, one two-client shared-drop visibility check, and one shared-pickup removal check where `SolarisObserver` saw primary-client effects in `.analysis/real-client-runs/20260622T210703Z-m94-regression-pack`. The 2026-06-22 `m94-03b` artifact `.analysis/real-client-runs/20260622T221125Z-m94-regression-pack` adds one focused two-client shared chest path where `SolarisObserver` opened the same chest and saw primary-deposited dirt in slot `0`. The 2026-06-23 `m94-03c` artifact `.analysis/real-client-runs/20260622T232643Z-m94-regression-pack` adds one focused two-client live shared-chest update path where the primary kept the container open, the secondary withdrew slot `0`, and the primary observed the slot become empty. The 2026-06-23 shared-container stale-click slices add unit-level real storage/packet-path coverage for rejecting old chest and furnace clicks after a peer mutation advances shared container state. The 2026-07-04 protocol-client harnesses add two-viewer TCP/session stale-click paths after live peer chest and furnace updates. The 2026-07-05 slow-reader pressure gate adds bounded retry-task coverage plus a sidecar protocol-harness run where two healthy observers each received `128/128` spawn packets while a paused peer produced outbound pressure, with `max_reliable_command_retries_in_flight=1`. The 2026-07-05 slow-client write-timeout slice adds focused unit evidence that a stalled outbound write from the play-loop queue closes the session through the normal cleanup path and increments `slow_client_write_timeouts`. | True concurrent same-block races, disconnect cancellation of already-running chunk jobs, long soak, broad real-client evidence, real-client concurrent chest/furnace click coverage, shared item-pickup contention, XP pickup, queue bounds under sustained load, broader two-client join/move/edit coverage, broad slow-client recovery/shedding policy, pressure-threshold control-channel shedding, and contention evidence are missing. | M94, M96, M100 |
| O1 | Performance budgets for low-end, balanced, and high-end profiles | In scope | `blocked` | M37 debug-load budgets and M42 lock-pressure run exist. M77 generated-world validation captured concrete debug-build numbers: startup pre-generation wrote 289 chunks in ~2.1s and flushed them; live view-distance-8 spawn stream emitted 289 chunks but needed 17.3s and produced repeated 150-377ms runtime ticks. The 2026-06-22 baked-light/warm-cache probes moved first-stream border generation, light computation, and restart disk fetch off the join stream: the fresh empty-world stream emitted all 289 chunks with first/ring1/ring2/full markers `8`/`16`/`31`/`327` ms, `fetch_ms=109`, and `light_compute_ms=0`; the warmed restart stream emitted all 289 chunks with `9`/`20`/`30`/`338` ms, `fetch_ms=108`, and `light_compute_ms=0`. The follow-up simulation/light-inert slice reran a 15s debug protocol probe in `.analysis/perf-runs/20260622T162829Z-loaded-simulation-light-inert`; it emitted no `runtime tick exceeded` warnings in that window and periodic metrics showed `random_tick_us=353..541`, `fluid_tick_us=342..408`, and `tick_us=972..1811`. A later existing-world diagnostic `.analysis/perf-runs/20260622T163447Z-generated-world-full-window` captured the remaining slow-light path (`light_compute_ms=47773`, `slow_light_compute_chunks=289`, `elapsed_ms=16326`); `.analysis/perf-runs/20260622T163821Z-generated-world-rebaked-full-window` backfilled 289 missing light arrays and streamed with `light_compute_ms=0`/`elapsed_ms=332`; the final persistence probes `.analysis/perf-runs/20260622T164812Z-generated-world-preserved-light-client` and `.analysis/perf-runs/20260622T164835Z-generated-world-preserved-light-startup-check` kept the warmed stream at `elapsed_ms=323` and immediate restart at `baked=0`/`flushed=0`. The 2026-07-04 dirty-border startup regression covers the no-new-view-light case that still generated missing border chunks and keeps those dirty chunks resident for save-all instead of runtime pressure. The 2026-07-04 disk-backed generated-world startup gate first failed at `startup_to_listener=19641` ms, then passed after startup worker saturation and deferred fresh disk flush with `first_startup_ms=9295`, `first_full_ms=350`, `second_startup_ms=1644`, and `second_full_ms=345`. The 2026-07-05 existing-world missing-light gate first failed because startup flushed the 289 rebaked chunks before the listener, then passed with `startup_ms=8768`, `full_ms=347`, `light_compute_ms=0`, `pressure_flush_runs=0`, `baked=289`, `flushed=0`, and `dirty=289`. A later 2026-07-05 rerun of the disk-backed generated-world startup gate caught a player-stop shutdown/save race: before the fix `/stop` flushed only 72 of 361 generated chunks and left `dirty_after=289`; after quiesce-before-save it passed with `first_startup_ms=9148`, `first_full_ms=341`, `second_startup_ms=1590`, and `second_full_ms=342`. The 2026-07-05 slow-reader pressure gate kept two healthy observers at `128/128` spawn packets under paused-reader pressure and kept `max_reliable_command_retries_in_flight=1` in that gate plus the M96 bounded replay. The real-client follow-up `.analysis/real-client-runs/20260622T174026Z-m94-regression-pack` passed `m94-02c` with no runtime tick budget warnings after `.analysis/real-client-runs/20260622T173420Z-m94-regression-pack` had reproduced random-tick-dominated warnings. | Generated-world performance remains blocked for readiness: the new existing-world missing-light and slow-reader evidence is still focused local debug/sidecar evidence, earlier warmed streams logged lock waits, and the new real-client evidence is still one focused scenario. The 2026-07-04 and 2026-07-05 disk-backed/slow-reader gates are useful regression evidence but not a full performance gate: they did not run low/balanced/high profiles, 20-player view-distance-8 >18 TPS, long soak, or hardware/build-matrix reporting. Required evidence still includes fresh empty-world pre-generation, restart/open, long `wire-probe` or real-client spawn stream, tick p95/p99, first/ring/full chunk latency, `light_compute_ms`, `fetch_ms`, lock waits, dirty flush pressure, slow-client policy, and hardware/build info across target profiles. | M91, M93, M96, M99, M100 |
| O2 | Concurrency ownership and blocking-path review | In scope | `blocked` | M39/M42/M48/M49 identified locks, runtime isolation, snapshots, and cache debt. M77 live generated-world probe produced `chunk_prepare` lock waits up to hundreds of ms and `save_all_flush` hold warnings during shutdown. M90 added dirty-generation correctness and unchanged-snapshot fast-clean guards. The 2026-07-05 dirty-flush lock-narrowing slice now clones dirty snapshots during planning without NBT encoding and bumps `dirty_generation` when a post-plan mutable access forks a shared dirty snapshot, so normal hot paths invalidate stale plans instead of re-encoding under `SaveAllFlush`. M97 added an ignored focused in-memory generated-world guard, runnable with `cargo test -p mc-test-harness --test chunk_stream vanilla_client_receives_spawn_view_distance_window -- --ignored --nocapture`, that requires `data/vanilla/reports/blocks.json` and `data/vanilla/reports/block_light.json`, asserts chunk CPU pipeline metrics were exercised, and checks IO/CPU worker concurrency stays within the configured `ChunkPipelinePolicy` limits while streaming the view-distance-8 spawn window. The 2026-07-04 update to that ignored guard adds lock-pressure delta assertions around the full stream: `chunk_prepare` wait/hold counters must advance, while in-memory `save_all_flush` and `player_persistence` counters must stay unchanged. The chunk-stream summary now also breaks out dirty-pressure flush runs plus plan/write/commit totals and maxima so future generated-world probes can separate flush pressure from the coarse `fetch_ms` bucket. The M100 rejected-placement resync slice keeps authoritative clicked/target lookup; the out-of-reach resync slice uses cached loaded-cell lookup, then emits `BlockUpdate` plus ack after dropping the world lock. The 2026-06-22 baked-light/warm-cache slice removed first-join on-demand generation of the 72 light-border chunks, removed per-emitted-chunk light recompute when baked section arrays are present, and moved restart disk fetch for the spawn window to startup warm-cache. Fresh and warmed debug probes both reported `pressure_flush_runs=0`, `slow_fetch_chunks=0`, and `slow_light_compute_chunks=0`. The 2026-07-04 dirty-border startup regression closes a disk-backed warm-cache edge by keeping generated border chunks dirty/resident for save-all when view-square light was already baked. The 2026-07-04 disk-backed startup gate moves the fresh empty-world disk write out of the listener critical path without introducing stream pressure flushes: the gate requires `pressure_flush_runs=0`, logs the 361 generated chunks as dirty/resident before listener, and then proves `/stop` save-all flushes those 361 chunks before the warmed restart. A 2026-07-05 rerun first falsified that under live player-stop ordering with `flushed=72/361` and `dirty_after=289`; after requesting shutdown before the final player-stop save, the same gate proved the 361-chunk flush again. The 2026-07-05 ignored VD8 multi-client disk-backed stop gate, `cargo test -p mc-test-harness --test load_scenarios vd8_multi_client_stop_drains_and_flushes_disk_world_under_stream_load -- --ignored --exact --nocapture`, then caught the broader shutdown lifecycle race: the first run left `active_cpu=2` after `serve()` returned, and after chunk-stream drop invalidation plus listener pipeline drain it still left `dirty_after=5`; adding a final listener shutdown save after connection/pipeline drain made the same gate pass with 4 clients, 9 streamed chunks verified by disk reopen, `dirty_before=32`, `dirty_after=0`, `active_cpu=0`, and `active_io=0`. The 2026-07-05 existing-world missing-light gate moves the 289 rebaked-chunk write out of the listener critical path as well: the gate requires `baked=289`, `flushed=0`, `dirty=289`, `pressure_flush_runs=0`, and `/stop` save-all of the 289 chunks. The 2026-07-05 slow-reader pressure gate bounds reliable retry task growth per backpressured recipient, tracks `max_reliable_command_retries_in_flight`, and keeps the sidecar healthy-observer and M96 replay gates at max in-flight `1`. The follow-up simulation-boundary slice makes random block ticks and scheduled fluid ticks sample loaded chunks rather than desired/ticketed chunks, with focused `mc-net` regressions proving newly ticketed chunks are ignored until `mark_loaded`; it also tracks `light_edit_chunks` so random/fluid loops skip full relight for light-inert state changes, preserves baked light for those light-inert edits, and persists recomputed baked light after relight. A pre-guard diagnostic in `.analysis/perf-runs/20260622T162551Z-loaded-simulation-boundary` reproduced `runtime tick exceeded` warnings dominated by `random_tick_us=161506..488201`, while `.analysis/perf-runs/20260622T162829Z-loaded-simulation-light-inert` had no slow-tick warnings in the same bounded probe shape. The final restart check after a client run in `.analysis/perf-runs/20260622T164835Z-generated-world-preserved-light-startup-check` reported `baked=0`, `flushed=0`, and shutdown `dirty_before=0`. The real-client follow-up adds resident-only random neighbor reads and background incremental relight from saved baked light; `.analysis/real-client-runs/20260622T174026Z-m94-regression-pack` no longer logged the random-tick runtime warnings reproduced in `.analysis/real-client-runs/20260622T173420Z-m94-regression-pack`. The scheduled-fluid restart slice makes `save_all` and chunk-pressure dirty flushes plan chunk payloads with `SessionRegistry::simulation_tick()` so live scheduled tick NBT stores remaining delay rather than absolute runtime tick; focused pressure-path tests still pass. | Dirty-commit now has focused unchanged-snapshot fast-clean, plan-without-encoding, and mutable-alias invalidation coverage, but this is still unit-level evidence until measured on live shutdown/pressure paths. The ignored generated-world guards now prove one in-memory stream exercises `ChunkPrepare` metrics without save-all/player-persistence lock entry, two disk-backed binary startup/stream paths avoid dirty pressure while deferring fresh or rebaked writes to stop save-all, one rerun of the disk-backed startup gate proves player-stop quiesce-before-save closes the live shutdown/save race for that path, one ignored VD8 multi-client disk-backed stop gate proves one streamed shutdown path drains chunk pipeline activity and performs a final post-drain save with disk-reopen evidence, and one sidecar slow-reader shape bounds retry task growth while healthy observers receive entity broadcasts. They still do not prove broad tick p95/p99, lock wait budgets, crash-before-save behavior, shutdown flush health under load, or slow-client disconnect/recovery policy. The guards are explicit sidecar-dependent evidence, not part of the default cargo baseline. M90/M91/M97 must still fix or budget remaining/historical `chunk_prepare` waits, dirty flush commit cost under live mutation, login playerdata sync load, disk/worldgen/compression/network writes under locks, and broader slow-client blocking. The simulation/light-persistence and focused real-client evidence must be rerun as a full generated-world performance/concurrency gate before O2 can count green. | M90, M91, M96, M100 |
| O3 | Autoscale profiles, runtime control, health/drain/load shedding, slow-client policy | In scope | `draft debt` | M92 added a draft `RuntimeControlPlane`, autoscale config parsing, normalized `--check` output, and explicit `draft_noop_not_wired` runtime status. M93 added `mc_net::AutoscaleSoakReport`, a bounded report model over explicitly attempted scenarios plus existing chunk queue stop reasons, worker resource metrics, outbound pressure counters, and `SaveAllReport` recovery visibility. The 2026-07-05 runtime-control chunk-send/prepare-dispatch slices wire enabled autoscale from `mc-server::ServerConfig::to_network` into a shared `RuntimeControlHandle`, pass it through `BoundServer`/Play into `ChunkStreamState`, observe server tick/worker and per-stream ready/in-flight queue pressure without holding the controller mutex across socket writes, apply `RuntimeControlLimits::chunk_send_rate` to `emit_ready_batch`, and apply live `chunk_load_rate`/`chunk_generate_rate` as a cap on `chunk_prepare_batch_size` because prepare workers perform the load/generate work before framing. `--check` status now reports `live_chunk_send_rate`. The authenticated `/status` admin command now reports the current runtime-control snapshot, including drain state, action, pressure, limits, hysteresis counters, and reason; focused packet-path coverage forces a drain snapshot through `BoundServer::runtime_control_handle()` and reads the exact `ClientboundSystemChat` feedback. Player `/stop` now requests runtime-control drain and shutdown before final save-all, so active simulation/chunk-stream work can quiesce before generated chunks are flushed. Console `stop` is wired to request drain and shutdown before save, the binary Ctrl-C shutdown helper requests runtime-control drain before listener drain and final save, and `BoundServer::serve()` requests runtime-control drain when the shared `ShutdownHandle` is signalled directly. The disk-backed generated-world startup gate also first failed with player `/stop` flushing only 72 of 361 generated chunks and leaving `dirty_after=289`, then passed after quiesce-before-save with `first_startup_ms=9148`, `first_full_ms=341`, `second_startup_ms=1590`, and `second_full_ms=342`. The ignored VD8 multi-client disk-backed stop gate first failed with `active_cpu=2` after `serve()` returned and then with `dirty_after=5`; after chunk-stream drop invalidation, listener chunk-pipeline drain, and a final listener shutdown save it passed with 4 clients, 9 streamed chunks verified by disk reopen, `dirty_before=32`, `dirty_after=0`, `active_cpu=0`, and `active_io=0`. The slow-client write-timeout slice bounds stalled outbound queue writes in the play loop, closes the session through existing cleanup, exposes `slow_client_write_timeouts`, and teaches `AutoscaleSoakReport` to count that timeout as slow-client pressure. Focused coverage includes `mc-net` control-plane/autoscale-soak tests plus live-send, prepare-dispatch, status-format, slow-client outbound timeout, and direct serve-shutdown unit RED/GREEN coverage, `mc-server` config/check-output assertions, command harness coverage for `/status` plus player `/stop` drain and quiesce-before-save, and the Ctrl-C shutdown helper RED/GREEN. | Live runtime autoscale affects chunk send-rate and the prepare dispatch budget derived from load/generate limits, operators can inspect the current snapshot through an authenticated command, and player `/stop`, the Ctrl-C shutdown helper, and direct shutdown-handle signalling drive runtime-control drain. Player `/stop` now has focused quiesce-before-save evidence for the disk-backed generated-world startup gate and one ignored VD8 multi-client post-drain save gate, console stop now has one ignored binary stdin quiesce gate, and stalled outbound queue writes now have focused unit-level slow-client shedding evidence. Runtime view-distance replanning, separately metered load-vs-generate queues, unauthenticated health endpoint/readiness model, memory-pressure detection/shedding, broad slow-client recovery/shedding policy, pressure-threshold control-channel shedding before socket-write stall, typed controller snapshot input for soak reports, live low-end/balanced/high-end soak, slow-disk scenario, reconnect storm, broader drain/restart evidence, broader console-stop coverage, and long chunk-generation storm remain missing. Current evidence is partial runtime-control integration and reporting scaffolding; it must not count as green autoscale behavior or infer scenario coverage from counters alone. | M92, M93, M99, M100 |
M100 O2/O3 console-stop note: the 2026-07-05 unit RED for console-stop ordering first failed because the shutdown-before-save helper did not exist; after wiring console `stop` through runtime-control drain plus shutdown request before `save_all`, `cargo test -p mc-net --lib console_stop_requests_shutdown_and_drain_before_save -- --nocapture` passed with 1 executed test. The ignored binary stdin gate `cargo test -p mc-server --test generated_world_startup disk_backed_generated_world_console_stop_drains_stream_load -- --ignored --exact --nocapture` passed with 4 clients, 10 streamed chunks, console stop shutdown-before-save ordering, quiescent final listener save (`flushed=0`, `planned=0`, `dirty_before=0`, `dirty_after=0`), and disk-reopen verification for streamed chunks. This is one focused console-input shutdown path, not broad shutdown flush health, crash-before-save durability, 20-player VD8 performance, soak, slow-client shedding, health/readiness, or full O2/O3 readiness.

M100 O2/O3 entity-ticker note: the 2026-07-05 unit RED for entity ticker shutdown first failed because the ticker drain/final-save lifecycle helpers did not exist; after retaining the ticker `JoinHandle`, draining it before listener final save, and routing final save through the drain helper, `cargo test -p mc-net --lib entity_ticker_drain_waits_for_in_flight_tick_before_final_save -- --nocapture` and `cargo test -p mc-net --lib listener_final_save_waits_for_entity_ticker_drain -- --nocapture` each passed with 1 executed test. This is focused ordering evidence for the shutdown ticker/final-save seam; it is not broad simulation quiesce proof for falling blocks, campfires, random ticks, scheduled fluids, long soak, or full O2/O3 readiness.

M100 O3 slow-client shedding note: the 2026-07-05 unit RED for stalled outbound writes first failed because the slow-client timeout helper/outcome did not exist; after adding a bounded outbound write timeout to the play-loop queue path, `cargo test -p mc-net --lib outbound_command_write_timeout_sheds_stalled_client -- --nocapture`, `cargo test -p mc-net --lib play_loop_closes_session_when_outbound_write_stalls -- --nocapture`, and `cargo test -p mc-net --lib bounded_soak_report_counts_slow_client_write_timeout_as_pressure -- --nocapture` each passed with 1 executed test. This is focused unit-level evidence that a stalled outbound queue write closes its play session and is visible in pressure/autoscale reporting; it is not real-client slow-reader evidence, broad recovery/shedding policy, pressure-threshold control-channel shedding before a socket write stalls, 20-player load, or soak evidence.

| O4 | Operator/admin docs, safe config validation, backups, recovery, public-risk warnings | In scope | `partial` | README/example config exist; replacement readiness has setup notes. The 2026-07-04 `mc-server --check` slices add structured `operator_warnings` for unsupported online-mode auth, public-bind offline-mode auth, local-dev operator blockers, absent `[data].world_dir`, configured world paths missing on disk, `[data].world_dir` paths that are existing files, missing world paths blocked by file-valued ancestors, existing world directories whose legacy `region` path is blocked by a file while no modern overworld region directory exists, `[simulation].save_interval_ticks = 0` clamping to one-tick save pressure, empty or whitespace-only `[admin].operators` entries, and empty or whitespace-only `[auth].whitelist`/`[auth].banned_players` entries. `--check` now fails early for syntactically invalid `network.bind_address`. The command-path harness now proves permitted `/save-all` and `/stop` commands return exact system-chat feedback on an in-memory no-world server and that `/stop` signals the shared shutdown handle. | Fresh-operator guide, broader unsafe public defaults, health/status, backups/restore, known non-goals, broader filesystem/permissions checks including inaccessible world metadata, dependency/security audit, data/protocol drift checks, disk-backed save/stop durability, drain behavior, Mojang online-mode auth, TCP bind/port availability, and serve-time missing-world creation evidence need M98 pass. | M89, M98, M100 |
| Q1 | Vanilla oracle scenario suite and capture inventory | In scope | `partial` | M43-M47 produced useful physics captures. M79 wires `tools/m79-oracle-scenarios/spawn-smoke.manifest` and `tools/m79-oracle-scenarios/configuration-phase.manifest` through `tools/run-m79-oracle-suite.sh`; the runner writes reports under `.analysis/oracle-suite/`, and both `spawn-smoke` and `configuration-phase` now pass locally with positive oracle-comparison markers when the required vanilla artifacts are present. | Oracle execution is still local-artifact-dependent, the scenario inventory is still narrow, and M43-M47 capture inventory plus oracle links for every counted row remain incomplete. | M79, M95, M99, M100 |
| Q2 | Real-client automation/manual evidence pack | In scope | `blocked` | M94 has a bounded fourteen-scenario manifest and an approved PrismLauncher/Java-agent runner with fail-closed `--check`, `--run`, and `--validate-run` modes. On 2026-06-22/23 the runner produced passed real-client artifacts for eight focused rows: `m94-02b`, `m94-01`, `m94-02a`, `m94-02c`, `m94-04a`, `m94-03a`, `m94-03b` (`.analysis/real-client-runs/20260622T221125Z-m94-regression-pack`: two-client shared chest deposit/observe), and `m94-03c` (`.analysis/real-client-runs/20260622T232643Z-m94-regression-pack`: two-client live shared-chest slot update). It also produced blocked partial broad-row artifacts for `m94-03` (`.analysis/real-client-runs/20260622T180005Z-m94-regression-pack`: recipe plus simple chest open/close), `m94-02` (`.analysis/real-client-runs/20260622T180920Z-m94-regression-pack`: solid break/drop/place plus water place/pickup), `m94-04` (`.analysis/real-client-runs/20260622T182159Z-m94-regression-pack`: regular sign placement/text, editor close, and after-close text), `m94-05` (`.analysis/real-client-runs/20260622T190401Z-m94-regression-pack`: visible cow plus death/respawn), `m94-07` (`.analysis/real-client-runs/20260622T191650Z-m94-regression-pack`: water place/pickup, visible drop/pickup, dirt replacement, visible cow, and focused server-log metrics), and `m94-06` (`.analysis/real-client-runs/20260622T210703Z-m94-regression-pack`: runner-managed save-all, graceful restart, same-client reconnect, one placed-block persistence check, one two-client live block-visibility check, one two-client shared-drop visibility check, and one two-client shared-pickup removal check). The 2026-07-04 validation-tooling slices make `--validate-run` require exact `schema: solaris.real_client_observations.v1`, `client_gate: agent-run-real-client`, and `quality_label: stabilization`, fail closed when a passed observed scenario that requires screenshots has no existing `screenshots/` artifact, when observations name a scenario id absent from the copied manifest, and when a referenced required screenshot is not a structurally valid PNG; the in-client agent driver also rejects captured screenshots that are not structurally valid PNG files before writing passed observations, rejects `passed` reports for broad blocked-only M94 scenario ids, and rejects non-loopback `server_addr` targets before bridge RPCs. | The full M94 pack is still non-green: eight of fourteen manifest scenarios have passed focused real-client evidence, and the six broad rows `m94-02`, `m94-03`, `m94-04`, `m94-05`, `m94-06`, and `m94-07` have blocked partial evidence. Supporting harness regressions are local-sidecar-dependent, protocol bots do not count as client evidence, and the exact-schema/exact-gate/exact-quality-label/screenshot-existence/scenario-id/PNG-structure/blocked-only/server-addr-loopback validation guards are artifact-shape and evidence-policy checks only. Q2 still does not provide full manual/client coverage, performance evidence, vanilla oracle evidence, broad two-client contention/shared-state evidence, screenshot visual-content validation, proof the loopback target is Solaris beyond local address policy, or soak evidence. | M78, M94, M99, M100 |
| Q3 | Property/replay corpus and malformed action fail-closed coverage | In scope | `draft debt` | M51-M53 started parity runner/diff/fuzzer concepts; M89 plans malformed packet safety. M96 added a deterministic bounded multiplayer replay scaffold in `load_scenarios.rs`; it is ignored unless local vanilla sidecars are present and implementer 19/20 could only compile/skip it because local vanilla reports were absent. The 2026-07-04 unsupported/malformed container click harnesses add focused valid-wire fail-closed matrices for chest and furnace storage paths using `QuickCraft`, `Clone`, and `PickupAll`, plus one malformed furnace `Pickup` with a lying carried item: client `changed_slots` are ignored, and a conflicting non-empty carried-item prediction is rejected after Solaris computes the post-click server cursor; authoritative container content is resent, and storage remains unchanged. A later 2026-07-04 window-0 inventory click harness adds one matching-state-id malformed `Pickup` route with lying `changed_slots` and impossible carried-item prediction: rejected click resyncs preserve the authoritative inventory state id and do not move the slot or cursor. A later 2026-07-04 carried-item harness adds one valid-wire malformed hotbar-selection route: `ServerboundSetCarriedItem { slot: 99 }` is ignored instead of clamped, preserving the previous selected slot for subsequent survival placement. The same pass adds one valid-wire `UseItemOn` world-border-hit route where `world_border_hit = true` is rejected with clicked/target resync instead of normal placement. The out-of-reach survival break slice adds valid-wire `StartDestroyBlock` rejection shapes for loaded-target resync before ack and far-target no-load/no-resync before ack. | Replay manifests, failing-seed files, cross-mechanic invariants, broad malformed/invalid action matrices, invalid-wire decoder/fuzzer corpus, corruption fail-closed assertions, and bug-seed workflow need hardening. | M79, M89, M96, M97, M100 |
| D1 | Dependencies, `Cargo.lock`, license/security, and data/protocol drift | In scope | `partial` | ADR 0001/0002 govern data and protocol sourcing. The 2026-07-04 `mc-server --check` slices add operator-visible sidecar root and metadata guards: `vanilla_data_dir_missing_on_disk`, `vanilla_data_dir_not_directory`, `vanilla_data_dir_parent_not_directory`, `vanilla_data_version_missing`, `vanilla_data_version_invalid`, `vanilla_data_release_mismatch`, `vanilla_data_world_version_mismatch`, `vanilla_data_protocol_mismatch`, `vanilla_data_registry_tree_incomplete`, `vanilla_data_block_light_report_invalid`, `vanilla_data_tags_unavailable`, `vanilla_data_recipes_unavailable`, and `vanilla_data_loot_unavailable` against `mc_protocol` target constants, `mc_data::KNOWN_REGISTRIES` tree presence, the block-light report loader, the serve-time tag loader, the serve-time recipe loader, and the serve-time loot loader. A locked dependency inventory snapshot records 194 Cargo packages (13 path/workspace, 181 registry, 0 git), direct normal/dev registry dependency sets, duplicate-version output for `getrandom`/WASI-support branches in the dev/test graph, compression reverse paths for `flate2`/`lz4_flex`, and absence of configured cargo-deny/audit/geiger/vet/about/SBOM tooling. | This remains inventory/check-output evidence only. Dependency advisory scan, third-party license allow/deny review, source provenance review, unsafe-code audit, SBOM, broader stale protocol/data detection, tag/recipe/loot schema drift, registry JSON parse/content freshness, inaccessible sidecar metadata, and lockfile-change rationale must be explicit in M100 evidence. | M98, M99, M100 |

## Accepted Non-Goals And Divergences Appendix

These rows are visible to preserve the M77 denominator decision trail. They
do not count toward the 80% numerator unless the owner explicitly reopens
scope.

| Row | Status | Rationale | Reopen trigger |
|---|---|---|---|
| Other dimensions, Nether/End parity, portals as full travel systems | `non-goal` | M77-M100 scope is vanilla 26.1.2 overworld survival. M84 only requires safe handling or explicit rejection. | Owner changes core MVP scope. |
| Full vanilla worldgen, structures, villages, and trading | `accepted divergence` | Solaris-owned deterministic worldgen exists; bit-for-bit vanilla terrain/structures are outside current MVP. | Owner requires vanilla terrain/structure parity. |
| Full redstone computer parity, pistons, observers, quasi-connectivity, exact update order | `non-goal` | M81 is redstone-lite/automation for common survival support only. | Evidence shows a deferred redstone path blocks normal scoped survival. |
| Modded clients, plugin APIs, custom datapacks beyond the local sidecar, resource-pack-specific behavior | `non-goal` | Roadmap scopes only a normal vanilla 26.1.2 client and the local data sidecar. M100 keeps only bounded extension/script boundary primitives; the 2026-07-05 API-firewall slice makes `xtask code-health` fail on public runtime-handle/transport leaks and internal workspace dependencies from `mc-extension`/`mc-script`, and replaces public `TryRecvError` receive returns with an API-owned queue error. | New ADR changes supported client/data surface. |
| Bit-perfect loot, RNG, sounds, particles, statistics, and game events for every mechanic | `accepted divergence` | Current MVP tracks client-observable playability and documents missing exactness; exact parity rows are split where they block normal survival. | M95 coverage audit finds a specific omission is client-visible enough to block. |
| Transparent shared-world horizontal sharding | `non-goal` | Autoscale scope allows profiles, load shedding, and independent instances, not multi-writer shared-world sharding. | New scaling ADR designs it. |

## M78-M100 Ledger Targets

| Milestone | Rows primarily touched |
|---|---|
| M78 | Q2 |
| M79 | P1, P2, Q1, Q3 |
| M80 | W1, F1-F3, L1-L2, I1-I2 |
| M81 | A1, K1, E2 |
| M82 | B1-B6, F1-F4, E1-E3, K1 |
| M83 | V1 |
| M84 | P4, E3 |
| M85 | N1, N2 |
| M86 | G1-G4 |
| M87 | I1-I2, K1-K2 |
| M88 | W2, P4, E2-E3, G4, S1 |
| M89 | P3, B1, I1, O4, Q3 |
| M90 | C1-C2, O2 |
| M91 | C1, B3-B4, O1-O2 |
| M92 | O3 |
| M93 | O1, O3 |
| M94 | Q2 plus every client-visible row in P, C, B, F, L, I, K, E, V, N, G, S |
| M95 | Every in-scope row; computes conservative 80% coverage numerator |
| M96 | S2, O1-O2, Q3 and cross-mechanic survival soak rows |
| M97 | Evidence-backed blockers from M94-M96 only |
| M98 | O4, D1 and operator-facing non-goal/divergence docs |
| M99 | Full rehearsal across every in-scope row |
| M100 | Final decision across all rows marked in scope |

## Current 80% Blockers

The following rows currently block any honest 80% coverage claim because
they are `blocked`, `unknown`, or broad `draft debt`, or because they lack
the required oracle/client evidence:

- Q2 real-client automation/manual evidence pack.
- Q1 systematic vanilla oracle scenario suite.
- B4 water/swim feel and frozen-world/manual regression retest.
- P3 online/offline auth and public-safety scope.
- K2 common survival stations gameplay beyond M87 safe rejection: station menus/recipes/effects are explicitly deferred and lack oracle/client evidence.
- V1 vehicles remain draft debt: M83 landed only `mc-entity` domain support and explicit deferrals; client-visible spawn/mount/move/persistence/visibility evidence is still missing.
- O1-O3 performance, concurrency, and autoscale evidence, including generated-world join/chunk-stream debt: the 2026-06-22 baked-light/warm-cache and runtime-light persistence probes reduced the 289-chunk stream to sub-400ms and kept the immediate restart at `baked=0`/`flushed=0`; the 2026-07-04 disk-backed startup gate brought one fresh binary startup/stream/restart path under the 10s listener budget, and the 2026-07-05 existing-world missing-light gate kept rebaked chunks out of stream pressure while deferring the 289-chunk flush to `/stop`. Historical lock/shutdown issues and broad profile evidence are still non-green.
- M92 adds draft O3 autoscale decision primitives and config visibility; the
  2026-07-05 M100 slices wire live chunk send-rate control plus a prepare
  dispatch cap derived from live load/generate limits. Runtime view-distance
  replanning, separately metered load/generate queues, health/readiness
  semantics, slow-client shedding, and low-end/balanced/high-end soak remain
  non-green; authenticated `/status` is snapshot visibility only.
- S1 crash recovery and persistence breadth.
- S2 multiplayer soak and contention evidence.
- M60-M76 farming/plant/campfire/sign/container/loot rows that are currently Solaris-harness-only or deterministic-divergence-only.

## M95 Conservative Coverage Audit

The M95 audit is reproducible with:

```sh
cargo run -p mc-test-harness --bin coverage-audit -- docs/VALIDATION_LEDGER.md
```

Current frozen-denominator accounting is conservative: only `ready` rows with
focused runtime coverage plus separate vanilla oracle or real-client evidence
for the exact row enter the numerator. Unit-only, Solaris-only,
wire-probe-only, protocol-metadata-only, negated, partial, blocked, unknown,
draft-debt, accepted-divergence, and non-goal rows do not count.

| Metric | Value |
|---|---:|
| In-scope denominator rows | 46 |
| Conservative numerator rows | 0 |
| Current conservative coverage | 0.00% |
| Rows needed for 80% | 37 |
| Gap to 80% | 37 rows |

Status breakdown: 30 `partial`, 4 `blocked`, 3 `unknown`, 7 `draft debt`, 2
`accepted divergence`, and 0 `ready`. See
[`VALIDATION_COVERAGE_AUDIT.md`](VALIDATION_COVERAGE_AUDIT.md) for the M95
snapshot and counting rule.
