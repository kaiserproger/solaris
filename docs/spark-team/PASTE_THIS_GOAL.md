# Вставить одной командой в новом Codex-сеансе из корня Solaris

```text
/goal quaka-whaka-zaka-du. Run the owner-authorized autonomous Solaris campaign in docs/spark-team/AUTOPILOT.md until the same integrated campaign HEAD proves PLAYABLE-GREEN, PARITY-GREEN, PERF-GREEN and the final L2/T10-04 gate. First rely on the already loaded repository AGENTS.md, then read only docs/spark-team/AUTOPILOT.md and OWNER_AUTHORIZATION.md; do not load BOARD.md, ALL_IN_ONE.md, the whole manifest, old sessions, or broad source trees. Run board.py validate and autopilot.py doctor, then autopilot.py bootstrap and move to the returned campaign_root. From then on use autopilot.py as the routing authority and repeat its state machine without asking me to say continue: dispatch up to two compatible cards; for every subagent dispatch spawn exactly the returned project custom role with only the generated packet and wait for all dispatched workers; for a primary-coordinator dispatch execute that one packet in the primary thread; inspect every result with candidate; route REVIEW candidates to one fresh read-only solaris_reviewer, record pass/changes/blocked, integrate only an unchanged passing candidate, checkpoint useful BLOCKED evidence without satisfying dependencies, refresh the dashboard, and dispatch again immediately. When dispatch returns template-required, generate the template packet, use one solaris_explorer, materialize one exact measured JSON spec, validate it, and continue. Every generated packet is the finite <goal_checkpoint> required by AGENTS.md; its route is the only route authority. Keep the primary thread coordinator-only, keep raw logs in ignored artifact paths, preserve unrelated dirty owner files, and never reset, clean, stash, overwrite owner work, push, merge to main, tag, change git identity, expose secrets/Mojang bytes, poll long jobs, treat timeout as success, or substitute unit tests for required vanilla-oracle, real-client, persistence, concurrency, or performance evidence. Local campaign/task worktrees, branches, commits, task-card checkboxes, one independent review, squash integration, ignored evidence, and autonomous selection of the next finite card are explicitly authorized. If a worker returns changes, give only the concrete findings to that same worker for one bounded repair pass; do not start a reviewer carousel. Finish every independent local path before stopping for a client rig, paid account, credential, clean host, or human observation. Stop as blocked only when the dashboard has no ready work, no materializable template, no coordinator checkpoint, no active repairable candidate, and every remaining chain has an exact external/irreducible blocker with fingerprint, artifact, unlock command, and next unlocked task. Do not stop after planning, bootstrap, one task, one batch, a partial green, compaction, or a progress report; durable Git-common-dir state is the continuation source after compaction or restart.
```
