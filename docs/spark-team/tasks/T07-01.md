# T07-01 — Заморозить low/balanced/high profile configs и budgets

Status: `DONE`

| Field | Value |
|---|---|
| Wave | `W07` — Профилирование low/balanced/high |
| Priority | `P0` |
| Route | `scaling` |
| Kind | `docs-and-config` |
| Dispatch | `READY after dependencies` |
| Ledger rows | `O1, O3` |
| Depends on | `T00-05` |
| Write locks | `PERF, COORD-DOCS` |
| Runtime leases | `NONE` |
| Required evidence | `performance` |
| Agent | `solaris_evidence:T07-01` |
| Worktree / branch | `/home/kaiserroman/solaris-spark-worktrees/tasks/T07-01 / agent/t07-01-12f1a5d3` |
| Base SHA | `12f1a5d3708d4d2b2e5466532e8da69610e7f464` |
| Started | `2026-07-24T11:27:58+00:00` |

## Dispatch gate

- [x] All dependencies are `DONE` on the integrated tree.
- [x] Coordinator confirmed no active write-lock or runtime-lease conflict.
- [x] Exact base SHA, worktree, port range and run directory are assigned.
- [x] Exact commands/artifact paths for every `Required evidence` leg are pasted into `Validation log` before implementation.

## Live checklist

- [x] CLAIMED — agent/worktree/base SHA filled
- [x] BASELINE / RED — current behavior or evidence gap reproduced
- [x] IMPLEMENTED — smallest task-scoped change complete, or audit artifact written
- [x] TESTING — exact focused commands/results/log paths recorded below
- [x] SELF-REVIEW — scope, authority, negative-code and diff inspected
- [x] INDEPENDENT REVIEW — one read-only verdict recorded
- [x] DONE — integrated commit/diff/evidence and one next action recorded

## Outcome

Три воспроизводимых профиля с CPU/memory/VD/client/entity/workload limits, required metrics, build mode and pass/degraded rules; без подгонки после результата.

## Read-only context — do not broaden

- `docs/milestones/M91.md`
- `docs/M52_OPERATOR_PERFORMANCE_NOTES.md`
- `crates/mc-net/src/control_plane.rs`

For any file over 400 lines: never `cat` it. Use one `rg -n` anchor batch, then open at most three windows of at most 160 lines each.

## Owned write paths

- `docs/performance/CORE_PROFILE_MATRIX.md`

Any additional path requires coordinator approval and a lock check. If the real root cause lives elsewhere, stop `partial` and request one child card.

## Bounded discovery / search anchors

- Use existing >18 TPS/VD8 target and current metric names
- Freeze exact p95/p99/latency budgets from clean baseline

## Non-goals

- No scope beyond the stated outcome.

## Required evidence legs

- `performance`
- Record build mode, commit, hardware, profile config, seed, duration, clients/VD, p50/p95/p99/max, queues/locks/memory and result artifact path.

## Required validation

- `Статическая проверка config keys against code`
- `Markdown links/path check`

## Done when

- [x] The exact outcome above is proven on the authoritative/runtime or evidence path.
- [x] One dominant failure, stale/race/rejection or missing-artifact boundary is covered when applicable.
- [x] No sleep, polling, elapsed-time success or timeout-as-success was added.
- [x] No unrelated source, formatting churn, new abstraction or compatibility layer was introduced.
- [x] Validation is classified precisely as unit/wire/oracle/real-client/performance/concurrency/persistence.
- [x] Every `Required evidence` leg has a concrete current-tree artifact; no unit-only substitution for oracle/client/perf.
- [x] Card contains changed files, diff/commit hash, commands, log paths, known gaps and one next action.


## Autonomous dispatch

- Packet: `/home/kaiserroman/solaris/.git/spark-autopilot/packets/T07-01-dispatch.md`
- Task: `T07-01`
- Agent role: `solaris_evidence`
- Worktree: `/home/kaiserroman/solaris-spark-worktrees/tasks/T07-01`
- Branch: `agent/t07-01-12f1a5d3`
- Base SHA: `12f1a5d3708d4d2b2e5466532e8da69610e7f464`
- Slot / ports: `A` / `25570-25579`
- Run directory: `.analysis/runs/t07-01-a`
- Owner checkout (read-only): `/home/kaiserroman/solaris`

## Validation log

- Frozen profile contract: `docs/performance/CORE_PROFILE_MATRIX.md`.
- Current campaign calibration: `.analysis/runs/t07-01-a/m52-debug-baseline.log` — PASS; debug build, 4 clients, 8 summons, 5,754 ms, chunk CPU peak 2, IO peak 1, queue peak 1, session lock max wait 121 us / hold 5,189 us, world lock max wait 11 us / hold 50 us.
- Runtime change: `AutoscaleProfile::HighEnd` now owns VD `8..32` with the existing 50 ms tick target and tick/queue/memory/SLA hysteresis.
- `cargo test -p mc-net control_plane::tests::` — PASS, 34/34 including exact 32->8 pressure degradation and 8->32 healthy recovery.
- `cargo test -p mc-server example_config_enables_high_end_eight_to_thirty_two_autoscale` — PASS, 1/1.
- `cargo test -p mc-server parses_autoscale_overrides_and_builds_bounded_policy` — PASS, 1/1.
- Static config-key and Markdown-path check — PASS; every documented TOML key exists in `mc-server`, HighEnd literals exist in `control_plane`, and referenced source/evidence paths exist.
- `cargo fmt --all -- --check` — PASS after a separate mechanical cleanup commit for the previous loot task.
- `cargo run -p xtask -- code-health` — PASS, `0 fail / KEEP`.
- `git diff --check` — PASS.
- The 20-client VD8 and 20-client VD32 envelopes remain explicitly `degraded`, not green; the card freezes their inputs and budgets before those runs.

## Review

- Reviewer: `manual-codexpro-readonly`
- Verdict: `PASS`
- Findings: `[]`
- Scope check: runtime policy, production example, config assertion, and one profile matrix are the minimum coherent vertical slice for the owner-requested VD8..32 contract. `playable.toml` remains unchanged at VD4. No result-driven budget relaxation or unsupported TOML key was added.

## Closeout

```yaml
verdict: pass
status: complete
base_tree: 0cc648826a14f25f1aeae759f379d463337bd3bd
diff_hash: 1a4f4e0eeab4a9095aaca3f1a1823aa3099278c6c40bc745342d3b9e1ae03aca
changed_files:
  - crates/mc-net/src/control_plane.rs
  - crates/mc-server/src/lib.rs
  - example.toml
  - docs/performance/CORE_PROFILE_MATRIX.md
  - docs/spark-team/tasks/T07-01.md
validation:
  - cargo test -p mc-net control_plane::tests::: pass (34/34)
  - mc-server example high_end 8..32 config test: pass
  - mc-server autoscale override test: pass
  - static config/path check: pass
  - cargo fmt --all -- --check: pass
  - cargo run -p xtask -- code-health: pass (0 fail / KEEP)
evidence:
  - performance: M52 debug calibration log with hardware/build/result metadata
  - config: production example and exact runtime policy assertions
  - contract: frozen low/balanced/high measurement matrix and pass/degraded/blocked rules
known_gaps:
  - Low quota, balanced 20-client VD8, and high 20-client VD32 end-to-end profiles are frozen but not yet fully executed; they remain degraded until their dedicated queue cards run.
  - VD32 changes view distance only; simulation distance remains 8 in the production example.
next: execute T01-04 and the remaining ready DAG without changing the frozen profile budgets
```
