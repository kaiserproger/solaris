# T04-06 — Loot context vertical slice: Fortune/Silk/Looting/burning

Status: `DONE`

| Field | Value |
|---|---|
| Wave | `W04` — Drops, loot, farming и renewable progression |
| Priority | `P1` |
| Route | `parity` |
| Kind | `implementation` |
| Dispatch | `READY after dependencies` |
| Ledger rows | `L1, G1` |
| Depends on | `T04-05` |
| Write locks | `RUST-DATA, RUST-ENTITY` |
| Runtime leases | `NONE` |
| Required evidence | `unit, wire` |
| Agent | `solaris_worker:T04-06` |
| Worktree / branch | `/home/kaiserroman/solaris-spark-worktrees/tasks/T04-06 / agent/t04-06-7392ce54` |
| Base SHA | `7392ce54d30382fab14412a1880b2d7e1b9b93e7` |
| Started | `2026-07-24T11:19:24+00:00` |

## Dispatch gate

- [x] All dependencies are `DONE` on the integrated tree.
- [x] Coordinator confirmed no active write-lock or runtime-lease conflict.
- [x] Exact base SHA, worktree, port range and run directory are assigned.
- [x] Exact commands/artifact paths for every `Required evidence` leg are pasted into `Validation log` before implementation.

## Live checklist

- [x] CLAIMED — agent/worktree/base SHA filled
- [x] BASELINE / RED — current behavior or evidence gap reproduced
- [x] IMPLEMENTED — smallest task-scoped change complete, or audit artifact written
- [x] TESTING — exact focused commands/results/log paths recorded below
- [x] SELF-REVIEW — scope, authority, negative-code and diff inspected
- [x] INDEPENDENT REVIEW — one read-only verdict recorded
- [x] DONE — integrated commit/diff/evidence and one next action recorded

## Outcome

Exactly one block tool context and one entity killer context use authoritative equipment/effect facts and match oracle output; unsupported combinations fail explicitly.

## Read-only context — do not broaden

- `crates/mc-data/src/loot/context.rs`
- `crates/mc-data/src/loot/entity_26_1_2/evaluate.rs`
- `crates/mc-entity/src/equipment_26_1_2/drops.rs`

For any file over 400 lines: never `cat` it. Use one `rg -n` anchor batch, then open at most three windows of at most 160 lines each.

## Owned write paths

- `crates/mc-data/src/loot/context.rs`
- `crates/mc-data/src/loot/entity_26_1_2/evaluate.rs`
- `crates/mc-data/tests/loot_context.rs`

Any additional path requires coordinator approval and a lock check. If the real root cause lives elsewhere, stop `partial` and request one child card.

## Bounded discovery / search anchors

- Do not expose live ECS handles to mc-data
- Pass immutable bounded context DTO

## Non-goals

- No scope beyond the stated outcome.

## Required evidence legs

- `unit`
- `wire`

## Required validation

- `cargo test -p mc-data loot_context`
- `cargo test -p mc-entity equipment`

## Done when

- [x] The exact outcome above is proven on the authoritative/runtime or evidence path.
- [x] One dominant failure, stale/race/rejection or missing-artifact boundary is covered when applicable.
- [x] No sleep, polling, elapsed-time success or timeout-as-success was added.
- [x] No unrelated source, formatting churn, new abstraction or compatibility layer was introduced.
- [x] Validation is classified precisely as unit/wire/oracle/real-client/performance/concurrency/persistence.
- [x] Every `Required evidence` leg has a concrete current-tree artifact; no unit-only substitution for oracle/client/perf.
- [x] Card contains changed files, diff/commit hash, commands, log paths, known gaps and one next action.


## Autonomous dispatch

- Packet: `/home/kaiserroman/solaris/.git/spark-autopilot/packets/T04-06-dispatch.md`
- Task: `T04-06`
- Agent role: `solaris_worker`
- Worktree: `/home/kaiserroman/solaris-spark-worktrees/tasks/T04-06`
- Branch: `agent/t04-06-7392ce54`
- Base SHA: `7392ce54d30382fab14412a1880b2d7e1b9b93e7`
- Slot / ports: `A` / `25570-25579`
- Run directory: `.analysis/runs/t04-06-a`
- Owner checkout (read-only): `/home/kaiserroman/solaris`

## Validation log

- Baseline defect reproduced: `cargo test -p mc-net play::survival::tests::fortune_level_changes_ore_count_for_the_break_seed -- --exact` failed with Fortune output `4` instead of oracle `3` after T04-05.
- Root cause: `sample_block_pool_roll_count` consumed two random draws for constant `rolls=1` and `bonus_rolls=0`; vanilla constant number providers consume no RNG. Coordinator approved `crates/mc-data/src/loot.rs` as the task-scoped root-cause path under the existing `RUST-DATA` lock.
- `cargo test -p mc-data loot` — PASS, 58 loot unit tests plus filtered integration context tests.
- `cargo test -p mc-data --test loot_context` — PASS, 11/11.
- `cargo test -p mc-entity equipment` — PASS, 29/29 equipment tests.
- `cargo test -p mc-net play::survival::tests::` — PASS, 19/19 survival tests.
- `cargo run -p xtask -- code-health` — PASS, `0 fail / KEEP`.
- `git diff --check` — PASS.
- Block tool wire/oracle slice: exact diamond-ore table with authoritative pickaxe enchantments yields Silk Touch ore block and Fortune III count `3` for the existing seed.
- Entity killer/effect wire/oracle slice: immutable `EntityLootContext` with living player killer, Looting II equipment facts, burning cow flag, and smelting lookup yields three cooked beef.
- Negative boundaries: enchantments on an empty block tool and equipment enchantments on a non-living killer reject explicitly; no ECS handles cross into `mc-data`.

## Review

- Reviewer: `manual-codexpro-readonly`
- Verdict: `PASS`
- Findings: `[]`
- Scope check: all runtime facts are copied into bounded immutable loot DTOs; no live ECS reference, sleep, polling, compatibility layer, or readiness inflation was added. The only additional source path fixes the directly reproduced RNG regression introduced by the dependency card.

## Closeout

```yaml
verdict: pass
status: complete
base_tree: 67aee500baf349d821eb6a268ce8ff90e874415a
diff_hash: 8fe90caad3eb71eaa04dff600463bba1d502b3cf082523ac67868f26e91dbcc6
changed_files:
  - crates/mc-data/src/loot.rs
  - crates/mc-data/src/loot/context.rs
  - crates/mc-data/src/loot/entity_26_1_2/evaluate.rs
  - crates/mc-data/tests/loot_context.rs
  - docs/spark-team/tasks/T04-06.md
validation:
  - cargo test -p mc-data loot: pass (58 loot unit tests; related integration filters pass)
  - cargo test -p mc-data --test loot_context: pass (11/11)
  - cargo test -p mc-entity equipment: pass (29/29)
  - cargo test -p mc-net play::survival::tests::: pass (19/19)
  - cargo run -p xtask -- code-health: pass (0 fail / KEEP)
evidence:
  - unit: bounded context validation and exact negative states
  - wire: mc-data integration tests construct and evaluate immutable block/entity contexts
  - oracle: existing Fortune seed expectation restored; Silk/Looting/burning/smelt exact outputs asserted
known_gaps:
  - The full entity-loot catalog is not yet the production mob-drop source in mc-net; this card closes the bounded context/evaluator vertical slice required by its owned scope, not that later runtime migration.
next: select the production entity-loot integration card after its dependencies are ready
```
