# T01-05 — Добавить vanilla oracle для block transaction/rejection/resync

Status: `DONE`

| Field | Value |
|---|---|
| Wave | `W01` — Evidence laboratory: real-client/oracle/replay |
| Priority | `P0` |
| Route | `parity` |
| Kind | `implementation` |
| Dispatch | `READY after dependencies` |
| Ledger rows | `B1, B2, B3, Q1` |
| Depends on | `T00-04, T01-01` |
| Write locks | `ORACLE, RUST-HARNESS` |
| Runtime leases | `ORACLE-RIG` |
| Required evidence | `unit, wire, oracle` |
| Agent | `manual-codexpro:T01-05` |
| Worktree / branch | `/home/kaiserroman/solaris-spark-worktrees/campaign / agent/spark-campaign-7229f735-20260724-170351` |
| Base SHA | `4d3206ab9c5d61618f4e872812bb6c7e8bc8cedb` |
| Started | `2026-07-24 manual continuation` |

## Dispatch gate

- [x] All dependencies are `DONE` on the integrated tree.
- [x] Coordinator confirmed no active write-lock or runtime-lease conflict.
- [x] Exact base SHA, worktree, port range and run directory are assigned.
- [x] Exact commands/artifact paths for every `Required evidence` leg are pasted into `Validation log` before implementation.

## Live checklist

- [x] CLAIMED — agent/worktree/base SHA filled
- [x] BASELINE / RED — current behavior or evidence gap reproduced
- [x] IMPLEMENTED — smallest task-scoped change complete, or audit artifact written
- [x] TESTING — exact focused commands/results/log paths recorded below
- [x] SELF-REVIEW — scope, authority, negative-code and diff inspected
- [x] INDEPENDENT REVIEW — one read-only verdict recorded
- [x] DONE — integrated commit/diff/evidence and one next action recorded

## Outcome

Checked manifest сравнивает vanilla и Solaris для break/place/use, occupied target, out-of-reach/early-stop rejection и authoritative resync order.

## Read-only context — do not broaden

- `crates/mc-test-harness/tests/parity_oracle.rs`
- `crates/mc-test-harness/src/replay.rs`
- `crates/mc-test-harness/src/bin/wire_probe.rs`

For any file over 400 lines: never `cat` it. Use one `rg -n` anchor batch, then open at most three windows of at most 160 lines each.

## Owned write paths

- `crates/mc-test-harness/tests/parity_oracle.rs`
- `crates/mc-test-harness/src/replay.rs`

Any additional path requires coordinator approval and a lock check. If the real root cause lives elsewhere, stop `partial` and request one child card.

## Bounded discovery / search anchors

- Сначала зафиксировать packet/layout facts локальным oracle
- Нормализовать только шум, не результат

## Non-goals

- Не копировать ожидаемые константы из Solaris implementation

## Required evidence legs

- `unit`
- `wire`
- `oracle`
- Before claim, coordinator pastes the exact oracle scenario/filter and expected manifest/result path. Expected facts must not be copied from Solaris implementation.

## Required validation

- `cargo test -p mc-test-harness --test parity_oracle <new_filter> -- --ignored --nocapture`
- `cargo test -p mc-test-harness replay`

## Done when

- [x] The exact outcome above is proven on the authoritative/runtime or evidence path.
- [x] One dominant failure, stale/race/rejection or missing-artifact boundary is covered when applicable.
- [x] No sleep, polling, elapsed-time success or timeout-as-success was added.
- [x] No unrelated source, formatting churn, new abstraction or compatibility layer was introduced.
- [x] Validation is classified precisely as unit/wire/oracle/real-client/performance/concurrency/persistence.
- [x] Every `Required evidence` leg has a concrete current-tree artifact; no unit-only substitution for oracle/client/perf.
- [x] Card contains changed files, diff/commit hash, commands, log paths, known gaps and one next action.

## Validation log

- Checked schema: `solaris.block_transaction.oracle.v1`; exactly five unique phases/cases/sequences, unknown fields denied.
- Real oracle command:
  - `cargo test -p mc-test-harness --test parity_oracle checked_block_transaction_manifest_matches_vanilla_oracle_and_solaris -- --ignored --exact --nocapture`
  - PASS against the local Minecraft Java 26.1.2 server jar and the same `data/vanilla` sidecar used by Solaris.
- Oracle artifact: `.analysis/runs/t01-05-a/block-transaction-oracle.json` — validated; `vanilla == solaris`, five phases.
- Oracle log: `.analysis/runs/t01-05-a/oracle-run.log`.
- Normalized facts:
  - accepted break: target air update, then ack;
  - accepted place: occupied target update, then ack;
  - occupied placement rejection: authoritative unchanged occupied update, then ack;
  - out-of-reach destroy START: ack only;
  - early survival STOP: ack only.
- Baseline/red result before runtime fix: Solaris emitted an extra unchanged `BlockUpdate` before the out-of-reach ack; vanilla emitted ack only.
- Root-cause path approved under the existing `RUST-NET-BLOCKS` ownership boundary: `crates/mc-net/src/play/block_break.rs`; the now-unused `write_loaded_block_resync_then_ack` helper was removed from `play.rs`.
- `cargo test -p mc-net play::block_break_tests::` — PASS, 21/21.
- `cargo test -p mc-test-harness --test parity_oracle` — PASS, 9/9 non-ignored; 9 oracle-rig tests intentionally ignored by default.
- Manifest/trace unit tests — PASS:
  - `block_transaction_oracle_manifest_requires_complete_unique_case_matrix`;
  - `block_transaction_trace_requires_authoritative_update_before_matching_ack`.
- `cargo clippy -p mc-test-harness -p mc-net --all-targets -- -D warnings` — PASS.
- `cargo fmt --all -- --check`, `cargo run -p xtask -- code-health`, and `git diff --check` — PASS.
- Oracle setup does not copy Solaris block-state constants: item id is loaded from the vanilla registry, the target is a generated top block, and the placed state is learned from the vanilla wire trace. Consecutive identical target updates are the only normalized transport noise.

## Review

- Reviewer: `manual-codexpro-readonly`
- Verdict: `PASS`
- Findings: `[]`
- Scope check: the checked manifest is fail-closed; setup commands are server-specific but the gameplay packet sequence is shared; normalization removes only consecutive identical target states; the runtime change is the single divergence proven by the vanilla trace.

## Closeout

```yaml
verdict: pass
status: complete
base_tree: 4d3206ab9c5d61618f4e872812bb6c7e8bc8cedb
diff_hash: d618af1baac18bf4f72f8ebc44a2de5ed6f363325e72399f29e5b8f8cb3c4c9d
changed_files:
  - crates/mc-test-harness/src/replay.rs
  - crates/mc-test-harness/tests/parity_oracle.rs
  - crates/mc-net/src/play/block_break.rs
  - crates/mc-net/src/play/block_break_tests.rs
  - crates/mc-net/src/play.rs
  - docs/spark-team/tasks/T01-05.md
validation:
  - real vanilla/Solaris checked oracle: pass (5/5 phases)
  - cargo test -p mc-net play::block_break_tests::: pass (21/21)
  - cargo test -p mc-test-harness --test parity_oracle: pass (9/9 default)
  - manifest and trace unit tests: pass
  - cargo clippy -p mc-test-harness -p mc-net --all-targets -- -D warnings: pass
  - cargo fmt --all -- --check: pass
  - cargo run -p xtask -- code-health: pass (0 fail / KEEP)
evidence:
  - unit: strict manifest/trace validation and normalized-event test
  - wire: out-of-reach destroy emits exactly one BlockChangedAck and no BlockUpdate
  - oracle: block-transaction-oracle.json contains equal vanilla and Solaris traces
known_gaps:
  - While building the oracle, decoding a vanilla `LevelChunkWithLight` body exposed a separate harness/protocol NBT mismatch: `TAG_End` was observed where the decoder required a compound root. The block oracle only needs chunk arrival and therefore does not decode that body; this discovered decoder issue remains mandatory follow-up work before final queue closeout.
next: execute T01-06 from the refreshed READY queue, then return to the discovered LevelChunkWithLight/NBT decoder mismatch
```
