# T01-02 — Разрезать broad block/fluid real-client gate на независимые focused phases

Status: `BLOCKED`

| Field | Value |
|---|---|
| Wave | `W01` — Evidence laboratory: real-client/oracle/replay |
| Priority | `P0` |
| Route | `playable` |
| Kind | `implementation` |
| Dispatch | `READY after dependencies` |
| Ledger rows | `B1, B2, B3, B4, Q2` |
| Depends on | `T00-03` |
| Write locks | `CLIENT-JAVA` |
| Runtime leases | `NONE` |
| Required evidence | `unit, wire, real-client-agent` |
| Agent | `solaris_worker:T01-02` |
| Worktree / branch | `/home/kaiserroman/solaris-spark-worktrees/tasks/T01-02 / agent/t01-02-7392ce54` |
| Base SHA | `7392ce54d30382fab14412a1880b2d7e1b9b93e7` |
| Started | `2026-07-24T11:16:03+00:00` |

## Dispatch gate

- [x] All dependencies are `DONE` on the integrated tree.
- [x] Coordinator confirmed no active write-lock or runtime-lease conflict.
- [x] Exact base SHA, worktree, port range and run directory are assigned.
- [x] Exact commands/artifact paths for every `Required evidence` leg are pasted into `Validation log` before implementation.

## Live checklist

- [x] CLAIMED — agent/worktree/base SHA filled
- [x] BASELINE / RED — current behavior or evidence gap reproduced
- [x] IMPLEMENTED — smallest task-scoped change complete, or audit artifact written
- [x] TESTING — exact focused commands/results/log paths recorded below
- [ ] SELF-REVIEW — scope, authority, negative-code and diff inspected
- [x] INDEPENDENT REVIEW — one read-only verdict recorded
- [ ] DONE — integrated commit/diff/evidence and one next action recorded

## Outcome

Каждая фаза имеет отдельный ID, exact observations и fail-closed result; umbrella ID только агрегирует уже зелёные фазы.

## Read-only context — do not broaden

- `client-mod/solaris-client-agent/java-agent/src/main/java/dev/solaris/agent/javaagent/M94BlocksFluidsFarmingDropsScenario.java`
- `client-mod/solaris-client-agent/java-agent/src/test/java/dev/solaris/agent/javaagent/M94BlocksFluidsFarmingDropsScenarioTest.java`

For any file over 400 lines: never `cat` it. Use one `rg -n` anchor batch, then open at most three windows of at most 160 lines each.

## Owned write paths

- `client-mod/solaris-client-agent/java-agent/src/main/java/dev/solaris/agent/javaagent/M94BlocksFluidsFarmingDropsScenario.java`
- `client-mod/solaris-client-agent/java-agent/src/test/java/dev/solaris/agent/javaagent/M94BlocksFluidsFarmingDropsScenarioTest.java`

Any additional path requires coordinator approval and a lock check. If the real root cause lives elsewhere, stop `partial` and request one child card.

## Bounded discovery / search anchors

- Сначала перечислить текущие broad blockers
- Не добавлять серверные debug команды как success path

## Non-goals

- Не чинить серверное поведение
- Не запускать длинный client gate

## Required evidence legs

- `unit`
- `wire`
- `real-client-agent`
- Before claim, coordinator pastes the exact `SOLARIS_REAL_CLIENT_AGENT_SCENARIO=... bash tools/run-real-client-regression.sh --run` command and expected artifact path from the current scenario matrix. Java tests alone do not satisfy this leg.

## Required validation

- `cd client-mod/solaris-client-agent && ./gradlew :java-agent:test --tests "*M94BlocksFluidsFarmingDropsScenarioTest*"`
- `cargo test -p mc-test-harness --test real_client_manifest`
- Attempted `cd client-mod/solaris-client-agent && ./gradlew :java-agent:test --tests "*M94BlocksFluidsFarmingDropsScenarioTest*"` from the task worktree. Current environment cannot download or resolve MCP mappings (`net.minecraft` symbols unavailable); command fails at dependency sync stage.
- `cargo test -p mc-test-harness --test real_client_manifest` not yet executed after code changes because the Java agent regression dependency blocker must be cleared first.

## Done when

- [ ] The exact outcome above is proven on the authoritative/runtime or evidence path.
- [ ] One dominant failure, stale/race/rejection or missing-artifact boundary is covered when applicable.
- [ ] No sleep, polling, elapsed-time success or timeout-as-success was added.
- [ ] No unrelated source, formatting churn, new abstraction or compatibility layer was introduced.
- [ ] Validation is classified precisely as unit/wire/oracle/real-client/performance/concurrency/persistence.
- [ ] Every `Required evidence` leg has a concrete current-tree artifact; no unit-only substitution for oracle/client/perf.
- [ ] Card contains changed files, diff/commit hash, commands, log paths, known gaps and one next action.


## Autonomous dispatch

- Packet: `/home/kaiserroman/solaris/.git/spark-autopilot/packets/T01-02-dispatch.md`
- Task: `T01-02`
- Agent role: `solaris_worker`
- Worktree: `/home/kaiserroman/solaris-spark-worktrees/tasks/T01-02`
- Branch: `agent/t01-02-7392ce54`
- Base SHA: `7392ce54d30382fab14412a1880b2d7e1b9b93e7`
- Slot / ports: `A` / `25570-25579`
- Run directory: `.analysis/runs/t01-02-a`
- Owner checkout (read-only): `/home/kaiserroman/solaris`

## Validation log

- Dispatch packet: `/home/kaiserroman/solaris/.git/spark-autopilot/packets/T01-02-dispatch.md`
- Evidence legs: `unit, wire, real-client-agent`
- Assigned artifact root: `.analysis/runs/t01-02-a`
- Before implementation, replace/extend this seed with exact current-tree commands and artifact paths, then check the fourth Dispatch gate item.
- Card-required validation:
  - `cd client-mod/solaris-client-agent && ./gradlew :java-agent:test --tests "*M94BlocksFluidsFarmingDropsScenarioTest*"`
  - `cargo test -p mc-test-harness --test real_client_manifest`

## Review

- Reviewer: `read-only`
- Verdict: `BLOCKED`
- Findings: `Implementation diff is scoped and correct for phase routing, but Java harness dependency failure blocks required unit/client gate execution in this worktree. Real-client harness command remains unrun due mapping issue.`

## Closeout

```yaml
verdict: blocked
status: checkpoint-blocked
base_tree: 7392ce54d30382fab14412a1880b2d7e1b9b93e7
diff_hash: 7d878ba9e600b14357a43e73263db29280a91ac26fd8612584d2830219de27b4
changed_files:
  - client-mod/solaris-client-agent/java-agent/src/main/java/dev/solaris/agent/javaagent/M94BlocksFluidsFarmingDropsScenario.java
  - client-mod/solaris-client-agent/java-agent/src/test/java/dev/solaris/agent/javaagent/M94BlocksFluidsFarmingDropsScenarioTest.java
  - docs/spark-team/tasks/T01-02.md
validation:
  - "cd client-mod/solaris-client-agent && ./gradlew :java-agent:test --tests \"*M94BlocksFluidsFarmingDropsScenarioTest*\" (failed: unresolved net.minecraft mapping/classpath symbols in this worktree)"
  - "cargo test -p mc-test-harness --test real_client_manifest (not run yet)"
  - "python3 docs/spark-team/scripts/autopilot.py candidate --task T01-02 (pass)"
  - "python3 docs/spark-team/scripts/autopilot.py review --task T01-02 --verdict blocked --reviewer read-only --summary ... "
evidence: []
known_gaps:
  - Gradle mapping dependency failure blocks execution of the Java test leg in the current sandbox.
  - Real-client harness validation (mc-test-harness) was not executed because unit gate did not produce a clean baseline.
next: resolve independent-review blocker, then redispatch this card
```
