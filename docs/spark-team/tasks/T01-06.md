# T01-06 — Добавить vanilla oracle для inventory/crafting/container state machine

Status: `DONE`

| Field | Value |
|---|---|
| Wave | `W01` — Evidence laboratory: real-client/oracle/replay |
| Priority | `P0` |
| Route | `parity` |
| Kind | `implementation` |
| Dispatch | `READY after dependencies` |
| Ledger rows | `I1, I2, K1, Q1` |
| Depends on | `T00-04, T01-01` |
| Write locks | `ORACLE, RUST-HARNESS` |
| Runtime leases | `ORACLE-RIG` |
| Required evidence | `unit, wire, oracle` |
| Agent | `manual-codexpro:T01-06` |
| Worktree / branch | `/home/kaiserroman/solaris-spark-worktrees/campaign / agent/spark-campaign-7229f735-20260724-170351` |
| Base SHA | `3df62d07ca96af31f74a993d6a064bc6fd4b2881` |
| Started | `2026-07-25 manual continuation` |

## Dispatch gate

- [x] All dependencies are `DONE` on the integrated tree.
- [x] Coordinator confirmed no active write-lock or runtime-lease conflict.
- [x] Exact base SHA, worktree, port range and run directory are assigned.
- [x] Exact commands/artifact paths for every `Required evidence` leg are pasted into `Validation log` before implementation.

## Live checklist

- [x] CLAIMED — agent/worktree/base SHA filled
- [x] BASELINE / RED — current behavior or evidence gap reproduced
- [x] IMPLEMENTED — smallest task-scoped change complete, or audit artifact written
- [x] TESTING — exact focused commands/results/log paths recorded below
- [x] SELF-REVIEW — scope, authority, negative-code and diff inspected
- [x] INDEPENDENT REVIEW — one read-only verdict recorded
- [x] DONE — integrated commit/diff/evidence and one next action recorded

## Outcome

Checked manifest сравнивает state_id/cursor/slots для craft, quick-move, stale click и close/reopen conservation.

## Read-only context — do not broaden

- `crates/mc-test-harness/tests/parity_oracle.rs`
- `crates/mc-test-harness/src/replay.rs`
- `crates/mc-protocol/src/packets/play.rs`

For any file over 400 lines: never `cat` it. Use one `rg -n` anchor batch, then open at most three windows of at most 160 lines each.

## Owned write paths

- `crates/mc-test-harness/tests/parity_oracle.rs`
- `crates/mc-test-harness/src/replay.rs`

Any additional path requires coordinator approval and a lock check. If the real root cause lives elsewhere, stop `partial` and request one child card.

## Bounded discovery / search anchors

- Ограничить первый slice chest + crafting table
- Furnace расширить отдельной follow-up карточкой при необходимости

## Non-goals

- No scope beyond the stated outcome.

## Required evidence legs

- `unit`
- `wire`
- `oracle`
- Before claim, coordinator pastes the exact oracle scenario/filter and expected manifest/result path. Expected facts must not be copied from Solaris implementation.

## Required validation

- `cargo test -p mc-test-harness --test parity_oracle <new_filter> -- --ignored --nocapture`
- `cargo test -p mc-test-harness replay`

## Done when

- [x] The exact outcome above is proven on the authoritative/runtime or evidence path.
- [x] One dominant failure, stale/race/rejection or missing-artifact boundary is covered when applicable.
- [x] No sleep, polling, elapsed-time success or timeout-as-success was added.
- [x] No unrelated source, formatting churn, new abstraction or compatibility layer was introduced.
- [x] Validation is classified precisely as unit/wire/oracle/real-client/performance/concurrency/persistence.
- [x] Every `Required evidence` leg has a concrete current-tree artifact; no unit-only substitution for oracle/client/perf.
- [x] Card contains changed files, diff/commit hash, commands, log paths, known gaps and one next action.

## Validation log

- Checked schema: `solaris.container_state.oracle.v1`; ten exact chest/crafting phases, unknown fields denied, menu/case matrix validated, slots strictly sorted/unique, initial and reopen baselines normalized to zero.
- Real oracle:
  - `cargo test -p mc-test-harness --test parity_oracle checked_container_state_manifest_matches_vanilla_oracle_and_solaris -- --ignored --exact --nocapture`
  - PASS against the local Minecraft Java 26.1.2 jar and the same vanilla reports/data sidecars used by Solaris.
- Oracle artifact: `.analysis/runs/t01-06-a/container-state-oracle.json` — PASS; `vanilla == solaris`, ten phases.
- Oracle log: `.analysis/runs/t01-06-a/oracle-run.log`.
- Normalized semantic revisions:
  - chest: initial `0`, quick-move-in `2`, quick-move-out `4`, stale `4`, reopen `0`;
  - crafting: initial `0`, grid/result prepared `3`, result quick-move `6`, stale `6`, reopen `0`.
- Stale-click revision-only bumps were observed as optional vanilla transport noise (`+0` or `+1`) with identical slots/cursor; only stale phases normalize that bump to the preceding semantic revision. Mutation revisions remain exact.
- Baseline/red findings fixed in runtime paths:
  - chest storage-to-player quick-move used the first main-inventory slot; vanilla uses the reverse player range and selected menu slot `62` in the checked fixture;
  - chest shared authority incremented one revision per transaction instead of one per changed chest/player/cursor slot;
  - crafting incremented one revision per transaction instead of result/input/player/cursor slot count.
- Coordinator-approved root-cause paths: `mc-net` chest/crafting/session/simulation authority files listed in Closeout.
- Focused unit/wire tests:
  - reverse chest range — PASS;
  - chest revision source+destination count `2` — PASS;
  - crafting result+input+destination count `3` — PASS;
  - strict manifest/trace negatives — PASS;
  - neutral unknown-command fence and player-slot projection mapping — PASS.
- `cargo test -p mc-net chest` — PASS, 24/24.
- `cargo test -p mc-net crafting` — PASS, 29/29.
- `cargo test -p mc-test-harness --test parity_oracle` — PASS, 11/11 default; 10 oracle-rig tests intentionally ignored by default.
- `cargo clippy -p mc-net -p mc-test-harness --all-targets -- -D warnings` — PASS.
- `cargo fmt --all -- --check`, `cargo run -p xtask -- code-health`, and `git diff --check` — PASS.
- Fixture stability: chest/table/platform are seeded at Y=200; the earlier Y=80 fixture was discarded after natural bamboo cascade drops polluted vanilla inventory.

## Review

- Reviewer: `manual-codexpro-readonly`
- Verdict: `PASS`
- Findings: `[]`
- Scope check: authoritative before/after diffs drive revision increments in direct, queued and regional chest paths; double-chest storage, player inventory and cursor are all included; crafting counts result, nine grid slots, player inventory and cursor. No stale transport-only bump was baked into runtime authority.

## Closeout

```yaml
verdict: pass
status: complete
base_tree: 3df62d07ca96af31f74a993d6a064bc6fd4b2881
diff_hash: 1ec16ed275f3198a9aa3968ee4e5f4f9b9ce58bc9632bdab62ab43fb08ae4c05
changed_files:
  - crates/mc-test-harness/src/replay.rs
  - crates/mc-test-harness/tests/parity_oracle.rs
  - crates/mc-net/src/play.rs
  - crates/mc-net/src/play/containers.rs
  - crates/mc-net/src/play/containers/chest.rs
  - crates/mc-net/src/play/session/container_views.rs
  - crates/mc-net/src/play/session/transactions.rs
  - crates/mc-net/src/play/session/tests.rs
  - crates/mc-net/src/play/simulation.rs
  - crates/mc-net/src/play/tests.rs
  - docs/spark-team/tasks/T01-06.md
validation:
  - real vanilla/Solaris container oracle: pass (10/10 phases)
  - cargo test -p mc-net chest: pass (24/24)
  - cargo test -p mc-net crafting: pass (29/29)
  - strict container manifest/trace units: pass
  - cargo test -p mc-test-harness --test parity_oracle: pass (11/11 default)
  - cargo clippy -p mc-net -p mc-test-harness --all-targets -- -D warnings: pass
  - cargo fmt --all -- --check: pass
  - cargo run -p xtask -- code-health: pass (0 fail / KEEP)
evidence:
  - unit: strict schema, trace validation, range/revision regression tests
  - wire: full state-id/slot/cursor snapshots and stale/reopen conservation
  - oracle: container-state-oracle.json contains equal vanilla and Solaris traces
known_gaps:
  - Furnace state-machine oracle remains outside this bounded chest/crafting slice and must be handled by its queued follow-up.
  - The separately discovered vanilla `LevelChunkWithLight`/network-NBT `TAG_End` decoder mismatch remains mandatory follow-up before final campaign closeout.
next: refresh the DAG and execute the next READY P0 card
```
