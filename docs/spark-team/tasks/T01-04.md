# T01-04 — Добавить restart invariant snapshot и строгую cross-phase валидацию

Status: `DONE`

| Field | Value |
|---|---|
| Wave | `W01` — Evidence laboratory: real-client/oracle/replay |
| Priority | `P0` |
| Route | `parity` |
| Kind | `implementation` |
| Dispatch | `READY after dependencies` |
| Ledger rows | `S1, Q2, Q3` |
| Depends on | `T01-01` |
| Write locks | `RUNNER, RUST-HARNESS` |
| Runtime leases | `NONE` |
| Required evidence | `unit, wire, real-client-agent, replay/negative, persistence` |
| Agent | `manual-codexpro:T01-04` |
| Worktree / branch | `/home/kaiserroman/solaris-spark-worktrees/campaign / agent/spark-campaign-7229f735-20260724-170351` |
| Base SHA | `bfdda779dbc80728bdbccf0279a8b16c4d625822` |
| Started | `2026-07-24 manual continuation` |

## Dispatch gate

- [x] All dependencies are `DONE` on the integrated tree.
- [x] Coordinator confirmed no active write-lock or runtime-lease conflict.
- [x] Exact base SHA, worktree, port range and run directory are assigned.
- [x] Exact commands/artifact paths for every `Required evidence` leg are pasted into `Validation log` before implementation.

## Live checklist

- [x] CLAIMED — agent/worktree/base SHA filled
- [x] BASELINE / RED — current behavior or evidence gap reproduced
- [x] IMPLEMENTED — smallest task-scoped change complete, or audit artifact written
- [x] TESTING — exact focused commands/results/log paths recorded below
- [x] SELF-REVIEW — scope, authority, negative-code and diff inspected
- [x] INDEPENDENT REVIEW — one read-only verdict recorded
- [x] DONE — integrated commit/diff/evidence and one next action recorded

## Outcome

Before/after фазы сравнивают typed invariant snapshot: player/inventory/world markers/container/entity/time as declared by scenario; missing field fails, timeout only fails.

## Read-only context — do not broaden

- `tools/real-client-agent-driver.py`
- `crates/mc-test-harness/tests/real_client_agent_driver.rs`
- `crates/mc-test-harness/tests/real_client_manifest.rs`

For any file over 400 lines: never `cat` it. Use one `rg -n` anchor batch, then open at most three windows of at most 160 lines each.

## Owned write paths

- `tools/real-client-agent-driver.py`
- `crates/mc-test-harness/tests/real_client_agent_driver.rs`

Any additional path requires coordinator approval and a lock check. If the real root cause lives elsewhere, stop `partial` and request one child card.

## Bounded discovery / search anchors

- Переиспользовать существующий marker/provenance contract
- Схема должна быть bounded и versioned

## Non-goals

- Не создавать общий database артефактов

## Required evidence legs

- `unit`
- `wire`
- `real-client-agent`
- `replay/negative`
- `persistence`
- Before claim, coordinator pastes the exact `SOLARIS_REAL_CLIENT_AGENT_SCENARIO=... bash tools/run-real-client-regression.sh --run` command and expected artifact path from the current scenario matrix. Java tests alone do not satisfy this leg.

## Required validation

- `python -m py_compile tools/real-client-agent-driver.py`
- `cargo test -p mc-test-harness --test real_client_agent_driver`
- `cargo test -p mc-test-harness --test real_client_manifest`

## Done when

- [x] The exact outcome above is proven on the authoritative/runtime or evidence path.
- [x] One dominant failure, stale/race/rejection or missing-artifact boundary is covered when applicable.
- [x] No sleep, polling, elapsed-time success or timeout-as-success was added.
- [x] No unrelated source, formatting churn, new abstraction or compatibility layer was introduced.
- [x] Validation is classified precisely as unit/wire/oracle/real-client/performance/concurrency/persistence.
- [x] Every `Required evidence` leg has a concrete current-tree artifact; no unit-only substitution for oracle/client/perf.
- [x] Card contains changed files, diff/commit hash, commands, log paths, known gaps and one next action.

## Validation log

- Baseline gap: before/after P45 phases were merely appended to `observations.json`; no versioned cross-phase state contract existed, so two locally passed phases could not prove that the same persisted marker/container state survived restart.
- `python3 -m py_compile tools/real-client-agent-driver.py` — PASS.
- `cargo test -p mc-test-harness --test real_client_agent_driver` — PASS, 30/30.
- `cargo test -p mc-test-harness --test real_client_manifest` — PASS, 45/45.
- `cargo fmt --all -- --check` — PASS.
- `cargo run -p xtask -- code-health` — PASS, `0 fail / KEEP`.
- `git diff --check` — PASS.
- Positive wire/persistence path: P45 before writes bounded `solaris.restart_invariants.v1` atomically, after validates six declared typed invariants and records six passed checks.
- Replay/negative paths:
  - missing required typed field (`category`) fails before any bridge RPC;
  - shared chest marker drift between phases fails during preflight before reconnect/mutation;
  - missing/oversized/malformed snapshot, duplicate IDs/marker fields, unsupported type/category/mode and producer mismatch are fail-closed in the parser.
- Historical real-client persistence evidence: `/home/kaiserroman/solaris/.analysis/real-client-runs/20260712T125931Z-real-client-playable-loop/observations.json` — P45 before and after both passed, secondary withdrew the stored item after restart, primary observed the chest empty, both clients returned to Play.
- The historical run predates the typed snapshot wrapper. It proves the underlying real-client restart behavior; the current wrapper is proven by the 30-test driver suite and does not inflate that old artifact into current-schema evidence.

## Review

- Reviewer: `manual-codexpro-readonly`
- Verdict: `PASS`
- Findings: `[]`
- Scope check: only the owned driver and driver tests changed. The snapshot is bounded, versioned and atomically replaced; after-phase preflight runs before bridge RPCs; time is an allowed category but P45 does not declare or claim a time invariant.

## Closeout

```yaml
verdict: pass
status: complete
base_tree: bfdda779dbc80728bdbccf0279a8b16c4d625822
diff_hash: a347e950d0f4c8ec769a25af8478ca02c4b67504175687df7f63c42e9e601eba
changed_files:
  - tools/real-client-agent-driver.py
  - crates/mc-test-harness/tests/real_client_agent_driver.rs
  - docs/spark-team/tasks/T01-04.md
validation:
  - python3 -m py_compile tools/real-client-agent-driver.py: pass
  - cargo test -p mc-test-harness --test real_client_agent_driver: pass (30/30)
  - cargo test -p mc-test-harness --test real_client_manifest: pass (45/45)
  - cargo fmt --all -- --check: pass
  - cargo run -p xtask -- code-health: pass (0 fail / KEEP)
evidence:
  - unit: typed schema, bounded values and exact-field validation
  - wire: positive two-bridge P45 before/after flow emits and validates six checks
  - replay-negative: missing field and marker drift fail before after-phase bridge RPCs
  - persistence: historical passed P45 real-client restart artifact listed in Validation log
known_gaps:
  - The historical real-client artifact predates `solaris.restart_invariants.v1`; a future live P45 run will produce the new snapshot directly.
  - P45 declares player, inventory, world, container and entity invariants; it does not declare a time invariant.
next: execute T01-05 from the refreshed READY queue
```
