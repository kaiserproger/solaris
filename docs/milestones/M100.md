# M100 - Core Validation Decision

**Status:** Planned. Quality label: `release-ready` only if every required
row is green; otherwise `stabilization`.

## Agent Start Prompt

Read M77-M99, `docs/DEFINITION_OF_DONE.md`, the validation ledger,
readiness docs, operator docs, and all M99 artifacts. Run full preflight.
Validate readiness; do not create readiness by assertion.

## Goal

Decide whether the scoped core MVP is release-ready, with brutal evidence,
or produce the M101+ blocker list.

## 2026-06-13 Static Review Baseline

A 2026-06-13 static review classified the project as a strong
stabilization-alpha/private vanilla-near base, not a release-ready vanilla
replacement. It did not run cargo, a real vanilla/PrismLauncher client,
profiler/load scenarios, or soak tests.

M100 starts from the frozen denominator accounting already in the ledger: 46
in-scope rows, 0 countable `ready` rows, and 0.00% conservative coverage. A row
still needs normal-path runtime tests plus separate vanilla oracle or real-client
evidence to count.

The static review's blocker list is part of the M100 triage input: real-client
and vanilla-oracle evidence, generated-world chunk/light performance, global
`WorldStorage` and `SessionRegistry` lock ownership, entity AI/pathing, water
and swim/movement feel, stale block-edit/CAS safety, public auth/offline-mode,
persistence/crash/soak/autoscale, and plugin API non-goal boundaries.

## Local Stabilization Notes

- Login compression boundary and malformed-frame rejection: raw-TCP integration
  tests now set `compression_threshold = 1` and require `LoginSuccess` to use a
  positive compressed-frame `data_length`, then separately send a compressed-
  layout `LoginAcknowledged` frame after `SetCompression` with
  `data_length = 256` and a non-zlib payload byte. Solaris closes before
  Configuration on that malformed frame. These remain synthetic TCP evidence
  only, not vanilla oracle, real-client, performance, compression-level, broad
  play-state compression, or broad malformed-frame corpus proof.
- Operator public-bind warning surface: `mc-server --check` now includes an
  `operator_warnings` array and reports public-bind blockers for offline-mode
  auth and `allow_local_dev_operators` before the operator reaches the runtime
  bind path. The focused CLI regression first failed because `--check` rendered
  no warning field for a public bind. This is O4 check-output evidence only,
  not a public deployment gate, online-mode auth, backup/recovery, dependency
  audit, security audit, or real operations run.
- Operator online-mode warning surface: `mc-server --check` now reports
  `online_mode_unsupported` for `[auth] online_mode = true` even on loopback
  binds. The focused CLI regression first failed because the loopback config
  rendered only the missing-world warning, then passed once unsupported
  online-mode auth was surfaced independently from public-bind warnings. This is
  O4 check-output evidence only, not Mojang session auth, public deployment
  safety, login UX, real-client/manual auth testing, or broad auth policy
  evidence.
- Operator bind-address validation: `mc-server --check` now rejects syntactically
  invalid `network.bind_address` values before rendering effective JSON. The
  focused CLI regression first failed because `bind_address = "not-an-ip"`
  exited 0 with normal check output, then passed with stderr naming
  `network.bind_address` and the invalid value. This is O4 config-validation
  evidence only, not TCP bind, port availability, firewall/NAT behavior,
  runtime listener startup, or public deployment safety.
- Operator zero save-interval warning: `mc-server --check` now reports
  `simulation_save_interval_ticks_zero` when `[simulation].save_interval_ticks`
  is configured as `0`. Runtime already clamps that value to `1`; the focused
  CLI regression first failed with `operator_warnings: []` while rendering the
  configured `0`, then passed once the check output exposed the continuous
  save-pressure risk. This is O4 check-output evidence only, not a performance
  budget, disk-backed save durability proof, soak run, backup/recovery check,
  or runtime scheduling redesign.
- Operator blank-entry warning: `mc-server --check` now reports
  `admin_operator_entry_blank` when `[admin].operators` contains an empty or
  whitespace-only entry. Runtime lowercases configured operator names, but
  blank entries never grant command permissions. The focused CLI regression
  first failed with `operator_warnings: []` while rendering the raw blank
  entries, then passed once the warning surfaced the ineffective operator
  configuration. This is O4 check-output evidence only, not operator
  normalization, login/runtime permissions, real-client/manual admin
  operation, broader operator policy, or public deployment evidence.
- Auth blank-entry warning: `mc-server --check` now reports
  `auth_whitelist_entry_blank` and `auth_banned_player_entry_blank` when
  `[auth].whitelist` or `[auth].banned_players` contain empty or
  whitespace-only entries. Runtime already trims and drops those entries before
  login checks. The focused CLI regression first failed with
  `operator_warnings: []` while rendering the raw blank auth lists, then passed
  once the inert entries were visible in check output. This is O4/P3
  check-output evidence only, not runtime auth behavior, auth persistence,
  duplicate/case policy, real-client/manual login evidence, or public
  deployment safety.
- Operator save/stop command path: a packet-harness command regression now
  sends permitted player-issued `/save-all` and `/stop` through
  `ServerboundChatCommand`, decodes exact `ClientboundSystemChat` feedback, and
  asserts `/stop` signals the shared `ShutdownHandle`. Temporary mutations to
  the save-all feedback text and shutdown request were both caught before
  restoring production code. This is O4 command-routing and feedback evidence
  on an in-memory no-world server only, not disk-backed save durability,
  backup/recovery, save failure feedback, drain behavior, real-client/manual
  operation, public deployment safety, or health/status output.
- Operator world-dir warning surface: `mc-server --check` now reports
  `missing_world_dir` when `[data].world_dir` is absent and
  `world_dir_missing_on_disk` when the configured path is absent, plus
  `world_dir_not_directory` when it points at a file and
  `world_dir_parent_not_directory` when a missing world path is blocked by a
  file-valued ancestor, and `world_region_not_directory` when an existing
  world directory has a file-valued legacy `region` path without a modern
  overworld region directory. A defensive `world_dir_metadata_unavailable`
  branch exists for other metadata failures, but the focused CLI regressions
  only cover the absent-config, missing-on-disk, file-path, file-parent, and
  blocked-region degraded states, plus modern-layout suppression for a
  file-valued legacy `region` path. A unit regression covers the
  `ensure_world_region_root` blocked-region error path. This is O4
  check-output and helper evidence only, not broad filesystem and permissions
  validation, backup/recovery, migration, corruption recovery, public
  deployment, end-to-end serve startup/fallback coverage, or broader
  operator-guide evidence.
- Vanilla sidecar metadata-drift warning: `mc-server --check` now reads
  `data.vanilla_data_dir` metadata before trusting a configured sidecar. It
  reports `vanilla_data_dir_missing_on_disk` and
  `vanilla_data_dir_not_directory` for unusable sidecar roots,
  `vanilla_data_dir_parent_not_directory` when a missing sidecar path is
  blocked by a file-valued ancestor, then checks `version.json` and reports
  `vanilla_data_version_missing`, `vanilla_data_version_invalid`,
  `vanilla_data_release_mismatch`, `vanilla_data_world_version_mismatch`, or
  `vanilla_data_protocol_mismatch` for absent, unreadable, malformed, missing
  required metadata, or stale release/world/protocol metadata.
  The focused CLI regressions first failed with collapsed or empty warnings
  for synthetic missing/file roots and missing/invalid/mismatched metadata.
  This is D1/W1 check-output evidence only, not a dependency audit, security
  audit, full sidecar schema freshness check, vanilla oracle, or lockfile-change
  policy proof.
- Vanilla sidecar registry-tree warning: `mc-server --check` now reports
  `vanilla_data_registry_tree_incomplete` when a configured sidecar has
  current target `version.json` metadata but lacks required registry JSON under
  `data/minecraft`. The check follows `mc_data::KNOWN_REGISTRIES` and uses a
  metadata-only recursive JSON-file presence probe for each required registry
  directory. The focused CLI regression first failed with `operator_warnings:
  []`, then passed with the new warning and without version missing/invalid or
  mismatch warnings. This is D1/W1 check-output evidence only, not registry JSON
  parse/content freshness, tag/report/block-light/recipe/loot schema validation,
  runtime sidecar load enforcement, vanilla oracle, dependency/license/security
  audit, or lockfile-change proof.
- Vanilla block-light report warning: after target `version.json` metadata and
  required registry-tree presence checks pass, `mc-server --check` now parses
  `reports/block_light.json` through the existing `mc_data::block_light::load`
  validator and reports `vanilla_data_block_light_report_invalid` for missing,
  malformed, shape-invalid, out-of-range, or wrong-release block-light reports.
  The focused CLI regression first failed with `operator_warnings: []`, then
  passed with the new warning and no version or registry-tree warnings.
  Serve-time `effective_block_light` regressions still pass. This is D1/W1
  check-output evidence only, not broad report/schema validation, runtime
  sidecar load enforcement through `--check`, vanilla oracle,
  dependency/license/security audit, or lockfile-change proof.
- Vanilla tags availability warning: after target metadata, registry-tree, and
  block-light checks pass, `mc-server --check` now reuses the serve-time tags
  loader and reports `vanilla_data_tags_unavailable` when sidecar tags cannot
  load or do not include required resolved block/item/entity_type tag entries.
  The focused CLI regression first failed with `operator_warnings: []`, then
  passed for a sidecar with current `version.json`, required registry tree,
  valid `reports/block_light.json`, and minimal `reports/registries.json` but
  no `data/minecraft/tags`; existing `effective_tags` regressions still pass.
  This is D1/W1 check-output evidence only, not broad tag-schema freshness, tag
  parity, recipe/loot validation, vanilla oracle, dependency/license/security
  audit, or lockfile-change proof.
- Vanilla recipes availability warning: after target metadata, registry-tree,
  block-light, and tags checks pass, `mc-server --check` now reuses the
  serve-time recipe loader and reports `vanilla_data_recipes_unavailable` when
  sidecar recipes cannot load or contain no supported shaped, shapeless,
  smelting, blasting, smoking, or campfire cooking entries. The focused CLI
  regression first failed with `operator_warnings: []`, then passed for a
  sidecar with current `version.json`, required registry tree, valid
  `reports/block_light.json`, minimal `reports/registries.json`, minimal
  resolved block/item/entity_type tags, and no `data/minecraft/recipe`;
  existing `effective_recipes` regressions still pass. This is D1/W1
  check-output evidence only, not broad recipe-schema freshness, recipe parity,
  recipe-book behavior, loot validation, vanilla oracle,
  dependency/license/security audit, or lockfile-change proof.
- Vanilla loot availability warning: after target metadata, registry-tree,
  block-light, tags, and recipes checks pass, `mc-server --check` now reuses
  the serve-time loot loader and reports `vanilla_data_loot_unavailable` when
  sidecar loot tables cannot load or contain no supported simple block/entity
  drops. The focused CLI regression first failed with `operator_warnings: []`,
  then passed for a sidecar with current `version.json`, required registry
  tree, valid `reports/block_light.json`, minimal `reports/registries.json`,
  minimal resolved block/item/entity_type tags, one supported shapeless recipe,
  and no `data/minecraft/loot_table`; existing `effective_loot` regressions
  still pass. This is D1/W1 check-output evidence only, not broad loot-schema
  freshness, loot parity, multi-pool or conditional loot behavior, vanilla
  oracle, dependency/license/security audit, or lockfile-change proof.
- Locked dependency inventory snapshot: `cargo metadata --locked` reports 194
  total packages: 13 path/workspace packages, 181 registry packages, and no
  git-sourced packages. The current direct registry set is 14 normal crates and
  9 dev crates; `cargo tree --duplicates` surfaces duplicate `getrandom`,
  `r-efi`, and `wit-bindgen` versions through the dev/test graph, including
  WASI-support paths; compression reverse paths show `flate2` through
  `mc-net`, `mc-protocol`, `mc-world`, and `mc-worldgen`, `lz4_flex` through
  `mc-world`, and no `libz-sys` package in the lockfile.
  `cargo-deny`, `cargo-audit`, geiger, vet, about, and SBOM tooling are not
  configured or run. This is D1 inventory evidence only, not an advisory
  database scan, third-party license review, provenance review, unsafe-code
  audit, lockfile remediation, or data/protocol drift audit.
- Play admission disconnect reasons: the focused play-state TCP regression now
  decodes `PlayDisconnect` NBT and asserts exact user-facing text for a
  duplicate offline profile and for a second connection rejected by the
  configured server capacity. This is P3 harness evidence only, not
  online-mode/session auth, operator file persistence, auth-concurrency,
  public deployment, or real-client proof.
- Falling-block landing/removal: a focused packet/session regression now breaks
  the support under a sand block, captures the spawned falling-block entity,
  then requires the entity to land as sand in the target block cell, emit a
  landing-chunk `LightUpdate`, and despawn via `RemoveEntities`. The first run
  failed waiting for landing/removal; after the light assertion, it failed again
  because no landing light packet arrived. The fixed tick path lands grounded
  `minecraft:falling_block` entities with stored block-state data into
  non-solid cached cells through the shared background edit pipeline, broadcasts
  block and light updates, and removes the entity. This is B6 protocol-client
  evidence only, not fresh vanilla oracle, real-client, broad
  sand/gravel/anvil/drop behavior, persistence, performance, or soak evidence.
- Early survival break rejection: a focused packet/session regression now proves
  an early `STOP_DESTROY_BLOCK` against a deterministic stone target sends the
  authoritative target `BlockUpdate` before the matching `BlockChangedAck`. The
  first run failed with `rejected break ack arrived before target-cell
  BlockUpdate`; the fixed path sends a target resync before ack for incomplete
  survival breaks. This is B1 stabilization evidence only, not broad stale-break
  CAS safety, vanilla oracle, real-client, anti-cheat, performance, or soak
  evidence.
- Lava/water scheduled interaction coverage: a focused packet/session
  regression now drives `UseItemOn` with a lava bucket next to a water source,
  observes the lava source placement/ack, then observes scheduled fluid tick
  solidification to obsidian. This improves B3 normal-path harness evidence but
  passed on the first run; it is not a bug fix, vanilla oracle, real-client
  visual/feel evidence, broad lava/fluid topology coverage, or performance
  evidence.
- Scheduled fluid save/restart accounting: a focused server regression now
  proves `save_all` persists pending fluid ticks as fresh-runtime remaining
  delay instead of absolute uptime tick. The first run failed with trigger
  `112` reloading after restart instead of `12`; the fixed path plans dirty
  chunk payloads with `SessionRegistry::simulation_tick()` for both save-all
  and chunk-pressure flushes. This is B3/S1/O2 stabilization evidence only,
  not real-client restart, crash-recovery, oracle, or soak evidence.
- Generated-world lock-pressure guard: the ignored sidecar-dependent
  view-distance-8 chunk-stream harness now snapshots lock pressure around the
  full 289-chunk stream, proves `chunk_prepare` wait/hold counters advance, and
  asserts the in-memory generated-world stream does not enter `save_all_flush`
  or `player_persistence` lock paths. The focused run passed and logged
  nonzero `chunk_prepare` deltas from the pre-stream snapshot; this is C1/O2
  stabilization evidence only, not a disk-backed generated-world latency gate,
  hardware profile, slow-client, real-client, broad lock-narrowing, or soak
  gate.
- Shared chest stale-click race: a focused storage/packet-path regression now
  proves a stale peer-window chest click resyncs instead of taking the
  just-mutated stack. The first run failed because `state.carried_item` became
  non-empty; the fixed path stores a shared chest state id in
  `SessionRegistry` and carries exact state ids through chest slot broadcasts.
  This is K1/S2 stabilization evidence only, not real-client concurrent-click,
  vanilla oracle, broad container, contention, or soak evidence.
- Shared chest protocol route: a two-client packet/session harness now opens
  the same chest from two live sessions, has one viewer advance the shared
  chest state by inserting dirt, then sends the other viewer's old `state_id`
  click and observes a full `ContainerSetContent` resync with the dirt still in
  chest slot `0` and an empty cursor. The first run passed on current
  production code, so this is evidence for the existing stale-resync path
  rather than a production fix. It is still K1/S2 stabilization evidence only,
  not real-client concurrent-click, vanilla oracle, broad container-state,
  contention, performance, or soak evidence.
- Unsupported/malformed container click fail-closed routes: focused
  packet/session harnesses now send valid `ServerboundContainerClick` packets
  using unsupported `QuickCraft`, `Clone`, and `PickupAll` modes, plus a
  furnace `Pickup` with an impossible client-reported carried item. Solaris
  resyncs authoritative container content, keeps the server cursor
  authoritative, and leaves the backing chest/furnace block entity unchanged.
  The unsupported-mode matrices passed on current production code, while the
  malformed furnace pickup first failed by advancing state `1 -> 2`; the fix
  now validates the post-click carried prediction and rolls back
  inventory/crafting/chest/furnace mutation when the client reports a
  conflicting non-empty carried item. Empty predictions remain
  non-authoritative and are corrected by the resync. This is Q3/K1 evidence
  only, not a broad malformed-click state-machine proof, real-client behavior,
  vanilla oracle, replay corpus, or fuzzing evidence.
- Malformed inventory click fail-closed route: a focused packet/session harness
  now gives dirt into window-0 hotbar slot `36`, then sends a valid-wire
  `ServerboundContainerClick` pickup with a matching state id but an impossible
  post-click carried-item prediction and lying `changed_slots`. The first run
  failed because the rollback resync advanced inventory state id `2 -> 3`; the
  fixed path separates mutation content writes from resync content writes, so
  rejected inventory clicks resend authoritative slot/cursor state without
  advancing the state id. This is Q3/I1 protocol-client evidence only, not
  broad click semantics, real-client behavior, vanilla oracle, replay corpus,
  invalid-wire fuzzing, or cursor recovery evidence.
- Invalid carried-item slot fail-closed route: a focused packet/session harness
  now sends `ServerboundSetCarriedItem { slot: 99 }` while the server has dirt
  selected in hotbar slot `0` and stone in slot `8`, then places a block through
  `ServerboundUseItemOn`. The first run failed because Solaris clamped `99` to
  slot `8` and placed stone. The fixed path ignores invalid carried-item slots,
  leaves the previous selected slot untouched, then places dirt and consumes
  slot `0`. This is Q3/B1/I1 protocol-client evidence only, not active pending
  break/use preservation, broad invalid-action coverage, invalid-wire
  decoder/fuzzer proof, real-client behavior, vanilla oracle, anti-cheat
  evidence, or soak evidence.
- World-border `UseItemOn` fail-closed route: a focused packet/session harness
  now sends `ServerboundUseItemOn` with `world_border_hit = true` against a
  loaded dirt support and adjacent air target. The first run failed because
  Solaris still placed dirt into the target cell. The fixed preflight path
  rejects the action, uses the same loaded-only resync posture as out-of-reach
  rejections, and avoids the normal placement path. A follow-up red run proved
  far `world_border_hit` with a held water bucket must not force block
  load/generation or held-bucket resync before ack. This is Q3/B1
  protocol-client evidence only, not world-border geometry, vanilla oracle,
  real-client desync, broad invalid-action, or anti-cheat evidence.
- Out-of-reach survival break rejection: focused packet/session harnesses now
  cover a loaded out-of-reach `StartDestroyBlock` target and a far
  out-of-reach target. The loaded-target red run failed with the ack arriving
  before the target-cell `BlockUpdate`; the far-target red run failed because
  the rejection path loaded/generated the target and sent a `BlockUpdate`. The
  fixed route resyncs only cached loaded targets before ack and does not force
  far target loads. This is Q3/B1 protocol-client evidence only, not broad
  destroy-action coverage, vanilla oracle, real-client desync, anti-cheat,
  performance, or soak evidence.
- Shared furnace stale-click race: a focused storage/packet-path regression now
  proves a stale peer-window furnace click resyncs instead of taking the
  just-mutated input stack. The first run failed because `state.carried_item`
  became non-empty; the fixed path stores a shared furnace state id in
  `SessionRegistry`, carries exact state ids through furnace slot broadcasts,
  and uses that same counter for smelting-tick slot changes. This is K1/S2
  stabilization evidence only, not real-client concurrent-click, vanilla
  oracle, broad furnace, contention, or soak evidence.
- Shared furnace protocol route: a two-client packet/session harness now opens
  the same furnace from two live sessions, has one viewer advance the shared
  furnace state by inserting raw iron, then sends the other viewer's old
  `state_id` click and observes a full `ContainerSetContent` resync with the
  raw iron still in the input slot and an empty cursor. The first behavioral
  run passed after fixing an overlong test username, so this is evidence for
  the existing stale-resync path rather than a production fix. It is still
  K1/S2 stabilization evidence only, not real-client concurrent-click, vanilla
  oracle, broad furnace-state, contention, performance, or soak evidence.
- Shared chest live-update route: the M94 real-client pack now includes and has
  validated `m94-03c-two-client-shared-chest-live-update` in
  `.analysis/real-client-runs/20260622T232643Z-m94-regression-pack`, a
  two-client route where the primary keeps a chest screen open, the secondary
  withdraws slot `0`, and the primary waits for that still-open slot to become
  empty. This is focused Q2/K1/S2 real-client evidence only; it is not
  stale-click injection, furnace/barrel/station coverage, vanilla oracle,
  performance, or soak evidence.
- Real-client validation screenshot guard: `tools/run-real-client-regression.sh
  --validate-run` now rejects a passed observed scenario whose manifest sets
  `screenshots_required: true` unless `observations.json` lists at least one
  existing artifact under `screenshots/`. The focused runner regression first
  failed because a synthetic passed `m94-01` run with an empty screenshot list
  printed `validated`, then passed after the validator enforced the artifact
  shape. This is Q2 validation-tooling evidence only, not real-client gameplay,
  screenshot content quality, manual/client coverage, vanilla oracle,
  performance, or soak evidence.
- Real-client validation scenario-id guard: `--validate-run` now rejects
  observed scenario ids that do not exist in the copied manifest. The focused
  runner regression first failed because a synthetic passed
  `m94-unknown-scenario` artifact printed `validated`, then passed after the
  validator enforced manifest membership before accepting observations. This
  is Q2 validation-tooling evidence only, not real-client gameplay, manifest
  completeness, manual/client coverage, vanilla oracle, performance, or soak
  evidence.
- Real-client agent screenshot PNG guard: `tools/real-client-agent-driver.py`
  now rejects captured screenshots that are not structurally valid PNG files
  before writing passed observations. The focused driver regression first
  failed because bad screenshot bytes reached the post-screenshot disconnect
  path instead of an invalid-PNG diagnostic, then passed after the driver
  checked PNG structure. This is Q2 validation-tooling evidence only, not
  screenshot visual content, real-client gameplay, manual/client coverage,
  vanilla oracle, performance, or soak evidence.
- Real-client runner screenshot PNG guard: `--validate-run` now rejects
  referenced required-screenshot artifacts that are not structurally valid PNG
  files. The focused runner regression first failed because a synthetic passed
  `m94-01` artifact with `fake png bytes` printed `validated`, then passed
  after the validator checked PNG structure; a paired valid-PNG fixture keeps
  the success path pinned. This is Q2 validation-tooling evidence only, not
  screenshot visual content, real-client gameplay, manual/client coverage,
  vanilla oracle, performance, or soak evidence.
- Real-client broad-scenario blocked-only guard: the in-client agent driver now
  rejects `passed` reports for broad M94 scenario ids that are documented as
  blocked-only (`m94-02`, `m94-03`, `m94-04`, `m94-05`, `m94-06`, and
  `m94-07`) until their missing evidence is implemented. The focused driver
  regression first failed because a fake bridge returned `passed` for
  `m94-02-blocks-fluids-farming-drops` and the driver wrote passed
  observations, then passed after the blocked-only policy returned a failed
  observation instead. This is Q2 validation-tooling evidence only, not broad
  gameplay coverage, manual/client coverage, vanilla oracle, performance, or
  soak evidence.
- Real-client agent server-address loopback guard: the in-client agent driver
  now rejects `--server-addr` targets outside explicit loopback hosts before
  any bridge RPC. The focused driver regression first failed because
  `example.com:25565` was forwarded through the fake loopback bridge and wrote
  passed observations, then passed after the driver required a loopback
  `server_addr` and wrote failed observations without bridge RPCs. This is Q2
  validation-tooling evidence only, not real-client gameplay, manual/client
  coverage, vanilla oracle, performance, or soak evidence.
- Real-client validation client-gate guard: `--validate-run` now requires exact
  `"client_gate": "agent-run-real-client"` and rejects the old misspelled
  `agent-runreal-client` alias. The focused runner regression first failed
  because a synthetic passed artifact with the misspelled gate printed
  `validated`, then passed after the validator rejected it. This is Q2
  validation-tooling evidence only, not real-client gameplay, manual/client
  coverage, vanilla oracle, performance, or soak evidence.
- Real-client validation observations schema guard: `--validate-run` now
  requires exact `"schema": "solaris.real_client_observations.v1"` in
  `observations.json`. The focused runner regression first failed because a
  synthetic passed artifact with `schema: solaris.real_client_observations.v0`
  printed `validated`, then passed after the validator rejected it. This is Q2
  validation-tooling evidence only, not real-client gameplay, manual/client
  coverage, vanilla oracle, performance, or soak evidence.
- Real-client validation quality-label guard: `--validate-run` now requires
  exact `"quality_label": "stabilization"` in `observations.json`. The focused
  runner regression first failed because a synthetic passed artifact with
  `quality_label: release-ready` printed `validated`, then passed after the
  validator rejected it. This is Q2 validation-tooling evidence only, not
  real-client gameplay, release-readiness evidence, manual/client coverage,
  vanilla oracle, performance, or soak evidence.
- Campfire in-flight disk persistence: a disk-backed protocol/storage
  regression now inserts porkchop into a campfire, flushes dirty chunks,
  reopens `WorldStorage`, and verifies the opaque campfire block entity keeps
  the input stack plus cook timer arrays. The
  focused regression first failed with no persisted campfire block entity,
  then passed after insert/tick paths wrote campfire state through the opaque
  block-entity store. This is E2/S1 disk-persistence evidence only, not
  fresh-session cooking hydration after restart, full campfire NBT/data-component
  parity, real-client/manual coverage, hopper/comparator, effects, performance,
  or soak evidence.
- Campfire in-flight restart hydration: a fresh-server restart-shaped
  protocol/storage regression now reopens the same disk world, streams the
  campfire chunk, hydrates the persisted in-flight campfire cooking state into
  a new `SessionRegistry`, and observes the cooked item output plus clear
  block-entity update. Chunk-stream wiring carries decoded hydration metadata
  from loaded chunks to emission, and client-facing opaque block-entity NBT
  strips persistent timer arrays. This is lazy chunk-load E2/S1 evidence
  only, not headless/no-player cook progression before chunk load after
  restart, full campfire NBT/data-component parity, real-client/manual coverage,
  hopper/comparator, effects, performance, or soak evidence.
- Campfire zero-client server ticking: a protocol harness regression now
  inserts porkchop into a campfire, disconnects the only client for longer
  than the recipe time, reconnects a fresh protocol client, and observes the
  cooked item entity. The focused regression first failed because campfire
  cooking was driven from the active play loop, then passed after the server
  50ms runtime ticker became the sole driver of `SessionRegistry` campfire
  cooking. This is E2/S1/S2 same-runtime zero-client/reconnect evidence only,
  not fresh-restart startup hydration by itself, full campfire NBT/data-component
  parity, real-client/manual coverage, hopper/comparator, effects, performance,
  or soak evidence.
- Campfire restart pre-client server ticking: a disk-backed restart
  regression now inserts porkchop into a campfire, flushes the persisted
  in-flight timer, stops the first server, starts a fresh server, waits longer
  than the recipe time before any client reconnects, then reconnects and
  observes the cooked item entity. The focused regression first failed because
  persisted campfires were hydrated only when chunk streaming emitted the
  chunk to a client, then passed after `WorldStorage` gained a read-only
  existing-chunk scanner and `bind` hydrated persisted campfires before the
  server runtime ticker began serving clients. This is E2/S1 startup hydration
  and no-client post-restart ticking evidence only, not full campfire
  NBT/data-component parity, real-client/manual coverage, hopper/comparator,
  effects, broad region-scan performance, crash-recovery, or soak evidence.
- Campfire vanilla timer NBT: local oracle inspection of vanilla 26.1.2
  `CampfireBlockEntity` shows persisted timer arrays are `CookingTimes`
  (spent progress) and `CookingTotalTimes` (recipe totals). The focused
  disk-persistence regression first failed when it required those vanilla
  arrays and no `solaris_cooking_*` arrays, then passed after new writes moved
  to vanilla timer arrays. The runtime unit also keeps legacy Solaris timer
  arrays readable for worlds created by the previous local slice, and the
  client-facing block-entity path strips both vanilla and legacy timer arrays
  from update NBT. This is E2/S1 oracle-backed timer-field evidence only, not
  full campfire item-stack/data-component NBT parity, real-client/manual
  coverage, hopper/comparator, effects, broad region-scan performance,
  crash-recovery, or soak evidence.
- Campfire lit-state cooking: local oracle inspection of vanilla 26.1.2
  `CampfireBlock.getTicker` shows server-side cook ticking runs only when
  `LIT=true`, while unlit campfires use cooldown ticking. The focused protocol
  regression first failed after a porkchop input on a campfire flipped to
  `lit=false` still finished and cleared its visual state, then passed after
  the shared server campfire ticker filtered in-flight positions through
  authoritative `lit=true` block state and rechecked before spawning cooked
  output. This is E2 oracle-backed lit/no-finish evidence only, not full
  cooldown decrement, relight resume, real-client/manual coverage,
  hopper/comparator, effects, performance, crash-recovery, or soak evidence.

## Required Evidence

- Preflight: DoD preflight plus port, `.analysis/test-world`,
  `.analysis/server.jar`, vanilla sidecar, branch, dirty status.
- Cargo baseline after final change: `cargo fmt --all -- --check`,
  `cargo clippy --workspace --all-targets -- -D warnings`,
  `cargo test --workspace`.
- Focused harness: protocol/session, chunks, lighting, block edits,
  fluids, farming, loot, inventory, containers, block entities, entities,
  combat, death/respawn, persistence, multiplayer, load.
- Vanilla oracle for every mechanic counted toward coverage.
- Real vanilla/PrismLauncher client for the complete scoped manual matrix.
- Property/replay corpus with seed manifests for failures.
- Performance for low-end, balanced, and high-end profiles.
- Concurrency evidence: no blocking disk/worldgen/network writes under
  long locks; slow reader cannot stall healthy clients; save/stop/
  reconnect/chunk work does not corrupt state.
- Generated-world join/chunk-stream evidence: starting from an empty
  `example.toml` world or a fresh generated-world fixture, Solaris must
  generate, persist, restart/open, and stream the view-distance-8 spawn
  window with no missing/extra/duplicate chunks and with green recorded
  budgets for first/ring/full chunk latency, tick p95/p99, lock wait/hold,
  `fetch_ms`, `light_compute_ms`, dirty flush pressure, and worker/queue
  saturation. The M77 baseline of 17.3s full-window stream plus repeated
  tick/`chunk_prepare` warnings is explicitly non-green.
- Autoscale evidence: predictable degradation, decision logs, no
  oscillation, no silent corruption.
- Persistence/crash evidence for players, chunks, entities, block
  entities, containers, scheduled ticks, time, weather where scoped.
- Docs match evidence: readiness, operator docs, and validation matrix.

## Acceptance

- `release-ready`: all required in-scope ledger rows green, scoped
  coverage >=80%, no high-severity real-client/manual blockers,
  performance/concurrency/autoscale budgets green.
- The generated-world join/chunk-stream row cannot be waived silently. If
  it still has unexplained tick-budget, `chunk_prepare`, `save_all_flush`,
  or full-window latency misses, M100 is `stabilization` unless the owner
  explicitly accepts the degradation as non-release-ready debt.
- Owner-accepted budget misses are non-green degradations unless the
  report proves they do not affect release safety.
- Accepted non-goals/divergences are excluded from the 80% numerator and
  required-green rows, but remain visible in docs.
- `stabilization`: any required evidence is missing, degraded, manual-
  pending, oracle-missing, blocked, or not run.
- No M0-M76 deferred debt may be silently absent from the final matrix.
