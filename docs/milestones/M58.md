# M58 - Crop harvest drops

**Status:** Closed partial/usable. Branch: `dev/M58-crop-harvest-drops`.
**Owning crates:** `mc-net` for block-break drop selection and item-entity
spawning, `mc-data` only if the explicit crop drop helper belongs beside loot
data, `mc-test-harness` for survival coverage, `docs` for readiness updates at
closeout.

## Goal

Make basic farming self-sustaining in normal survival play: mature crops broken
by a player should produce useful harvest items instead of behaving like generic
block drops.

This follows directly from M57 crop growth and bonemeal. It closes the next
visible farming gap with existing block-break, item-drop, inventory, and harness
paths, and it avoids protocol guessing entirely.

## Scope

- Add deterministic Solaris crop harvest drops for the common age-based crops
  already supported by M57 growth: wheat, carrots, potatoes, beetroots, and
  nether wart.
- Start with a small M58.a vertical slice for wheat only.
- Route crop harvest through the existing survival block-break path and item
  entity spawning path.
- Resolve item ids through the loaded item registry by namespaced id; do not
  hardcode numeric item ids.
- Use block state properties from the loaded block registry to distinguish
  mature and immature crops.
- Allow more than one stack to drop from a broken crop where needed.
- Keep the rules deterministic and documented instead of claiming vanilla loot
  table, fortune, or RNG parity.
- Add focused unit tests first, then a Solaris-only harness test once the wheat
  slice is stable.

## Non-Goals

- Full vanilla loot-table execution.
- Fortune, Silk Touch, shears, explosion decay, tool predicates, or luck/RNG
  parity.
- Automatic replanting, villager farming, composter behavior, bees, farmland
  hydration changes, or crop trampling changes.
- Sound, particle, game-event, statistic, advancement, or recipe-book updates.
- New packet IDs, field layouts, block/entity metadata, or guessed clientbound
  effects.
- Tree/sapling growth, stem crops, sweet berry bushes, cocoa, cactus, sugar cane,
  bamboo, kelp, chorus plants, or mushrooms.

## Strategy

1. Keep M58.a entirely on verified gameplay paths: existing player block break,
   block updates/acks, item entity spawn, pickup, and item registry lookup.
2. Replace the one-stack `block_drop_stack` call site with a small multi-drop
   path, preserving generic block behavior as a single-stack fallback.
3. Add an explicit crop-drop helper that takes the previous block state and
   returns zero or more item stacks for supported crops.
4. For M58.a, support only `minecraft:wheat`:
   - age 7 drops `minecraft:wheat` x1 and `minecraft:wheat_seeds` x1;
   - ages 0-6 drop `minecraft:wheat_seeds` x1;
   - if either item is absent from the item registry, omit only that missing
     stack and do not panic.
5. Use the block state's `age` property and registry block id, not numeric block
   state ids.
6. Spawn each resulting stack through the existing item entity path at the broken
   block center, then run the existing nearby-pickup pass once after spawning.
7. Defer broader crop families until the wheat helper and multi-drop seam are
   covered by focused tests.
8. Update `docs/REPLACEMENT_READINESS.md` only at closeout with the exact crop
   harvest claim that landed.

## Sub-Milestones

### M58.a - Wheat harvest vertical slice

Status: landed; committed as `7e0d8cd feat: add wheat harvest drops`.

Target for immediate implementation:

- Add a crop-aware block-drop path for `minecraft:wheat` in survival block
  breaking.
- If the broken wheat block has `age=7`, spawn two item stacks through existing
  item entities: `minecraft:wheat` x1 and `minecraft:wheat_seeds` x1.
- If the broken wheat block has `age=0` through `age=6`, spawn
  `minecraft:wheat_seeds` x1 only.
- Keep all item resolution by namespaced registry id; do not use numeric item
  ids or hardcoded block state ids.
- Preserve existing generic block drops for non-crop blocks.
- Add focused tests proving mature wheat returns wheat plus seeds, young wheat
  returns seeds only, unsupported crops still use the generic path or return no
  crop override, and missing item ids do not panic.

Acceptance:

- Breaking mature wheat visibly produces harvest items using existing item entity
  spawn packets.
- Breaking immature wheat does not produce wheat.
- Stone, dirt, logs, and other generic block drops continue to use the existing
  drop behavior.
- The break path can spawn multiple drop stacks from one broken block.
- No new packet IDs, packet layouts, entity metadata, sounds, particles, or level
  events are introduced.
- Focused `mc-net` tests pass.

Landed notes:

- Mature `minecraft:wheat` with `age=7` drops `minecraft:wheat` x1 and
  `minecraft:wheat_seeds` x1.
- Immature `minecraft:wheat` with `age=0` through `age=6` drops
  `minecraft:wheat_seeds` x1.
- Item ids are resolved by namespaced registry id. Missing item ids omit only
  the missing stack and do not panic.
- Non-crop generic block drops are preserved.
- One block break can spawn multiple drop stacks, then run pickup once after
  spawning.
- Current support is deterministic local wheat yields only: no fortune, RNG,
  exact vanilla loot-table execution, particles, sounds, stats, events, other
  crops, or harness coverage in this commit.

Landed focused gate:

```sh
cargo test -p mc-net wheat -- --nocapture
cargo fmt --all -- --check
```

Suggested focused gate:

```sh
cargo test -p mc-net crop_drop -- --nocapture
cargo test -p mc-net block_drop -- --nocapture
cargo fmt --all -- --check
```

### M58.b - Survival harness coverage

Status: landed; committed as `1602c51 test: add wheat harvest harness coverage`.

Acceptance:

- Add a non-ignored Solaris-only harness test under `block_edit` or an adjacent
  farming module.
- Test setup places farmland and mature wheat in the in-memory Solaris world,
  breaks the crop through the existing survival block-break path, and observes
  item entity metadata or pickup slot updates for wheat plus seeds.
- The test requires the vanilla data sidecar but does not require
  `.analysis/server.jar`.
- Assertions stay on server-visible drops, inventory, and block state changes,
  not sounds, particles, statistics, or client visual effects.

Landed notes:

- Added `survival_break_mature_wheat_drops_wheat_and_seeds` to the `block_edit`
  harness.
- The test boots Solaris only, plants mature `age=7` wheat over farmland in the
  in-memory world, breaks it through the survival block-break path, asserts the
  crop becomes air, and observes both `minecraft:wheat` x1 and
  `minecraft:wheat_seeds` x1 via item entity metadata or pickup slot updates.
- Young wheat remains unit-covered only.

Landed focused gate:

```sh
cargo test -p mc-test-harness --test block_edit wheat -- --nocapture
cargo fmt --all -- --check
```

### M58.c - Common crop deterministic drops

Status: deferred.

Acceptance:

- Extend the explicit crop-drop helper to the remaining M57-supported age crops:
  carrots, potatoes, beetroots, and nether wart.
- Use deterministic minimum useful drops only:
  - mature carrots drop `minecraft:carrot` x2;
  - immature carrots drop `minecraft:carrot` x1;
  - mature potatoes drop `minecraft:potato` x2;
  - immature potatoes drop `minecraft:potato` x1;
  - mature beetroots drop `minecraft:beetroot` x1 and
    `minecraft:beetroot_seeds` x1;
  - immature beetroots drop `minecraft:beetroot_seeds` x1;
  - mature nether wart drops `minecraft:nether_wart` x2;
  - immature nether wart drops `minecraft:nether_wart` x1.
- Do not add poisonous potatoes, fortune, or RNG extra yields in M58.c.
- Add unit coverage for mature and immature states for each supported crop.

Suggested focused gate:

```sh
cargo test -p mc-net crop_drop -- --nocapture
```

### M58.d - Closeout and readiness update

Status: closed by this docs update; no code changes.

Acceptance:

- Update `docs/REPLACEMENT_READINESS.md` with the exact crop harvest claim that
  landed.
- Record deferred gaps: vanilla loot-table parity, fortune/RNG extra yields,
  poisonous potatoes, particles/sounds/game events, automation, and non-age crop
  families.
- Fill the What Landed Where table with commits and artifacts.
- Baseline validation passes or any local-only skip is documented.

## Validation

Full baseline passed after M58.b:

```sh
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```

Focused gates during implementation:

```sh
cargo test -p mc-net crop_drop -- --nocapture
cargo test -p mc-net block_drop -- --nocapture
cargo test -p mc-test-harness --test block_edit wheat -- --nocapture
```

Manual gate:

```sh
cargo run --bin mc-server -- --config example.toml
# Connect with PrismLauncher, grow or set up wheat, break mature and immature
# wheat, and verify mature wheat yields wheat plus seeds while immature wheat
# yields seeds only. Verify ordinary block drops still appear.
```

## Protocol and Data Oracles

- M58.a must not add new packet IDs or field layouts. It should reuse existing
  verified block break, block update/ack, item entity spawn, pickup, inventory,
  and registry paths.
- No `wire-probe` or `javap` oracle is required for M58.a if sounds, particles,
  statistics, game events, metadata, and new clientbound effects remain deferred.
- Vanilla data sidecar is required for block and item registries. Crop maturity
  must be read from namespaced block id plus the `age` property, not from numeric
  block state ids.
- Crop drop quantities in M58 are Solaris-owned deterministic rules, not a
  translation of Mojang loot-table algorithms.
- If a later slice attempts exact vanilla loot-table execution, fortune/RNG extra
  yields, poisonous potato chance, particles, sounds, statistics, or game events,
  first capture or cite the needed behavior from the local oracle per ADR 0002 and
  keep Mojang data files as runtime input per ADR 0001.

## Risks

- The current generic block-drop path returns at most one stack. M58.a should keep
  the multi-drop seam narrow and prove generic block drops are unchanged.
- Vanilla crop loot tables contain conditions/functions that the current simple
  loot loader intentionally skips. Do not widen the loader into partial generic
  loot execution for M58.a.
- Item entity drop observation in the harness can race pickup. Prefer an existing
  deterministic pickup/inventory observation if available, or wait on existing
  visibility events rather than sleeping.
- Missing item ids from a minimal test registry should not panic. Unit tests need
  a missing-item case.

## Closeout Notes

- M58 landed as a partial/usable wheat slice, not the full common-crop scope from
  the original plan.
- Deterministic wheat harvest drops are implemented through the survival block
  break path: mature `age=7` wheat drops `minecraft:wheat` x1 plus
  `minecraft:wheat_seeds` x1, while immature `age=0` through `age=6` wheat drops
  `minecraft:wheat_seeds` x1 only.
- The block-break drop path now supports multiple spawned drop stacks for one
  broken block, with generic non-crop block drops preserved.
- Solaris harness coverage proves mature wheat survival break behavior. Young
  wheat harvest remains unit-covered only.
- Deferred: other crop harvest drops, exact vanilla loot-table execution,
  fortune/RNG extra yields, sounds, particles, statistics, game events, and
  young wheat harness coverage.
- Deterministic minimum yields are intentionally less generous than vanilla.
  Document this as partial farming support instead of parity.

## What Landed Where

| Sub-milestone | Status | Commit | Key artifact |
|---|---|---|---|
| M58.a - Wheat harvest vertical slice | landed | `7e0d8cd` | crop-aware multi-drop seam for wheat; mature wheat -> wheat + seeds; immature wheat -> seeds only; missing item ids omitted safely; generic block drops preserved; pickup runs once after multi-drop spawn |
| M58.b - Survival harness coverage | landed | `1602c51` | `block_edit` Solaris-only `survival_break_mature_wheat_drops_wheat_and_seeds` covers mature wheat harvest through survival block break; young wheat remains unit-covered only |
| M58.c - Common crop deterministic drops | deferred | n/a | carrots, potatoes, beetroots, and nether wart harvest drops deferred |
| M58.d - Closeout and readiness update | landed | pending docs commit | readiness claim checked, validation recorded, and deferred parity gaps documented |
