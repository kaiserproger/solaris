# M87 - Remaining Survival Stations Decision Pass

**Status:** Complete. Quality label: `draft`.

## Agent Start Prompt

Read M77-M86 and the container/station ledger rows. Run preflight. Decide
station status; implement only M100 blockers.

## Goal

Stop ambiguous readiness for common survival workstations.

## Scope

- Brewing stand, anvil, enchanting table, smithing table, grindstone,
  stonecutter, loom, cartography table, composter, and related menus.
- For each station choose: MVP implementation, safe no-op/rejection,
  deferred debt, or non-goal.
- Recipe-book/display synchronization, window/state id resync, cursor
  stack recovery, and non-player container close/reopen behavior are
  explicit rows.

## Acceptance

- Every common station has status and evidence/gap in the ledger.
- Implemented stations have focused menu tests.
- Deferred stations are visible in readiness/operator docs.

## Closeout

**Status:** Complete. Quality label: `draft`.

M87 resolves the common-station ambiguity with a safe-rejection slice, not
station gameplay breadth. Unsupported station use now acks the client and stops
the interaction before it can fall through into adjacent held-block placement.

### Station Decisions

| Station | M87 status | Notes |
|---|---|---|
| Brewing stand | Safe no-op/rejection | No brewing menu, fuel, bottle, ingredient, potion, or effect semantics claimed. |
| Anvil, chipped anvil, damaged anvil | Safe no-op/rejection | Repair, rename, XP cost, damage chance, and falling-anvil station behavior are deferred. |
| Enchanting table | Safe no-op/rejection | Enchantment offers, lapis/XP costs, bookshelves, and RNG are deferred. |
| Smithing table | Safe no-op/rejection | Trim/upgrade templates and transform recipes are deferred. |
| Grindstone | Safe no-op/rejection | Repair/disenchant and XP return are deferred. |
| Stonecutter | Safe no-op/rejection | Stonecutting recipe output is deferred; harness coverage uses stonecutter as the representative real path. |
| Loom | Safe no-op/rejection | Banner pattern/color application is deferred. |
| Cartography table | Safe no-op/rejection | Map clone/lock/scale behavior is deferred. |
| Composter | Safe no-op/rejection | Fill levels, bone meal output, hopper/comparator behavior, particles, and sounds are deferred. |
| Cauldron, water cauldron, lava cauldron, powder snow cauldron | Safe no-op/rejection | Bucket, bottle, leather armor, banner, powder snow, precipitation, comparator, particles, and sounds are deferred. |
| Lectern | Safe no-op/rejection | Book display/page sync and redstone behavior are deferred. |
| Fletching table | Safe no-op/rejection | Vanilla has no implemented survival UI; any future behavior remains explicit debt. |
| Beacon | Safe no-op/rejection | Payment, effects, pyramid validation, and menu sync are deferred. |
| Crafter | Safe no-op/rejection | Slot toggles, redstone crafting, and automation sync are deferred. |
| Recipe-book/display sync | Deferred debt | No new recipe-book/menu display synchronization was added. |
| Window/state id resync | Partial | Existing crafting/furnace/chest paths retain state-id resync; unsupported stations avoid stateful menus. |
| Cursor stack recovery | Partial | Existing container cursor paths remain; unsupported stations do not mutate cursor state. |
| Non-player container close/reopen | Deferred debt | No new close/reopen behavior for unsupported stations was added. |

### Preflight

| Item | Result |
|---|---|
| Autonomous preflight | Degraded. `pwd`, repo root, branch `dev/M87-survival-stations`, Rust 1.94.1, Cargo 1.94.1, rustfmt, Java 25.0.3, `javap`, `data/vanilla`, and `.analysis/test-world` were present. `.analysis/server.jar` was missing, so vanilla oracle/menu-ID inspection was unavailable. |
| Worktree | Clean before edits according to `git status --short`; local-only files were not staged or touched. |

### Evidence Matrix

| Gate | Result |
|---|---|
| Cargo baseline | Focused gates run after code changes: `cargo fmt --all -- --check`; `cargo test -p mc-net common_container_paper_cuts_resolve_to_existing_menus`; `cargo test -p mc-net cauldron_variants_are_safe_interaction_targets`; `cargo test -p mc-test-harness --test block_edit unsupported_station_use_is_safe_noop_instead_of_block_placement`; `cargo clippy -p mc-net --tests -- -D warnings`. Full workspace `test`/`clippy` were not run for this bounded implementer slice. |
| Focused tests | `mc-net` unit coverage guards an independently declared expected unsupported-station block-id/name table, including cauldron variants, lectern, fletching table, beacon, and crafter. A dedicated cauldron-family regression test separately asserts all cauldron block ids resolve to the safe-interaction decision, so omitting one fails independent of the broader table. The harness test boots a real in-process server, places a stonecutter through `ServerboundUseItemOn`, gives the player dirt, right-clicks the stonecutter, and asserts the server acks without `OpenScreen` or adjacent dirt `BlockUpdate`; this exercises the real play interaction path and is not pass-by-construction. |
| Vanilla oracle | Not run; `.analysis/server.jar` missing. No new packet IDs or menu type IDs were added. |
| Client/manual gate | Not run. |
| Performance | Not measured; this slice adds one block-id lookup on right-click only and no tick scan or hot loop. |
| Concurrency | Reviewed as low risk: the new path reuses the existing world lock pattern for block-use target reads and does not add global locks, async tasks, or tick work. |
| Data/protocol facts | Uses namespaced block ids from the loaded block registry. No new protocol facts. |
| Persistence/storage | Not touched. |
| Dependencies | No dependency or `Cargo.lock` changes. |
| Known gaps | Actual gameplay for brewing/anvil/enchanting/smithing/grindstone/stonecutter/loom/cartography/composter/cauldron/lectern/fletching/beacon/crafter remains deferred M101+ debt unless M95/M100 reopens a specific station as a blocker. Recipe-book/display sync, unsupported station stateful menus, non-player station close/reopen, sounds, particles, statistics, XP, and oracle/client evidence remain absent. |

### What Landed Where

| File | Change |
|---|---|
| `crates/mc-net/src/play/containers.rs` | Added unsupported common station block-id policy. |
| `crates/mc-net/src/play.rs` | Added safe-rejection handling before fallback block placement. |
| `crates/mc-net/src/play/tests.rs` | Added unit coverage for the full unsupported station set. |
| `crates/mc-test-harness/tests/block_edit/furnace_and_chests.rs` | Added runtime harness coverage for stonecutter safe no-op/rejection. |
| `docs/VALIDATION_LEDGER.md` | Updated K2 from `unknown` to `partial` with explicit evidence and deferred gameplay. |
| `docs/REPLACEMENT_READINESS.md` | Documented M87 safe rejection and remaining station gameplay gaps. |
