# M92 - Autoscale And Runtime Control Plane

**Status:** Draft implementation in progress. Quality label: `draft`.

## Agent Start Prompt

Read M77-M91, performance evidence, runtime config, and operator docs.
Run preflight. Add operational autoscale primitives for weak and strong
machines.

## Goal

Make Solaris degrade predictably on weak hardware and use strong hardware
without hiding lock bottlenecks.

## Scope

- Profiles: `low_end`, `balanced`, `high_end` for workers, view/sim
  distance, queues, caches, compression, chunk-send budgets, save cadence.
- Runtime controller: dynamic chunk load/generate/send budgets based on
  tick time, queue depth, memory pressure, worker saturation, first chunk
  SLA.
- Health/status output, graceful drain, load shedding, slow-client policy,
  safe config reload where feasible.
- Shared-world horizontal sharding is non-goal unless a later ADR designs
  it; independent instances behind proxy/lobby are allowed.

## Acceptance

- Autoscale decisions are observable and hysteresis prevents oscillation.
- Low-end profile degrades view/throughput predictably, not correctness.
- High-end profile improves throughput or identifies exact lock blockers.

## Draft Implementation Notes

- `mc-net::RuntimeControlPlane` provides bounded per-instance autoscale
  decisions with hysteresis, pressure classification, snapshots, and drain
  clamping.
- `[autoscale]` config in `mc-server` selects `low_end`, `balanced`, or
  `high_end`; `--check` renders normalized effective bounds and reports
  `runtime_mode = "draft_noop_not_wired"` until live chunk-stream wiring lands.
- Scope remains local-process runtime control only. Transparent
  shared-world horizontal sharding is still a non-goal.
- Live chunk-stream integration, health/admin output, slow-client shedding,
  safe reload, and performance soak remain M93+ debt.

## What landed where

| Slice | Status | Files |
|---|---|---|
| M92.a - Bounded runtime-control primitive | local draft | `crates/mc-net/src/control_plane.rs`, `crates/mc-net/src/lib.rs` |
| M92.b - Config/check surface | local draft | `crates/mc-server/src/lib.rs`, `crates/mc-server/src/main.rs`, `example.toml` |
| M92.c - Operator notes | local draft | `docs/M92_AUTOSCALE_CONTROL_PLANE.md`, this file |
