# M94 - Full Real-Client Regression Pack

**Status:** Planned. Quality label: `stabilization`.

## Agent Start Prompt

Read M78, M82, M91-M93, and the validation ledger. Run preflight. Expand
real-client evidence to the M100 regression matrix.

## Goal

Replace ad-hoc owner manual checks with reproducible real-client evidence
for scoped client-visible behavior.

## Scope

- Real-client scenarios: join/rejoin, movement, chunk load, block
  break/place/use, inventory, crafting, containers, signs, beds,
  campfires, fluids, farming, drops, combat, death/respawn,
  save/restart, two-client visibility.
- Include named M40/M41 route: swim feel, visible drops/pickup, sugar
  cane behavior, nearby mob/aquatic movement, TPS/lock log capture.
- Capture screenshots or structured observations, client logs, server
  logs, scenario manifests, and commit ids.

## Acceptance

- The regression matrix covers every scoped checklist row or marks each
  missing row `degraded`, `manual-pending`, or `blocked`.
- One scenario is not enough for M94; that was only M78.
- Client-visible blockers become M97 bugs or M100 blockers.

## Scaffold Slice - Implementer 17

Quality label: `stabilization`.

- Added `docs/real-client-regression/manifests/m94-regression-pack.json`, a
  bounded ten-scenario real-client regression pack covering M94 checklist
  rows, the rejected solid, survival out-of-reach, and occupied-target
  water-bucket fallback/resync hooks, and the named M40/M41 route.
- Added `docs/real-client-regression/README.md` to pin local artifact layout
  under `.analysis/real-client-runs/<run_id>/` and preserve the rule that
  protocol harnesses are supporting evidence only, not real-client evidence.
- Added a focused manifest-shape test in `mc-test-harness` so future edits do
  not collapse M94 into a single scenario or silently replace real-client
  evidence with protocol-only clients. The test also enforces unique scenario
  ids, required run artifacts, screenshot requirements, and explicit handling for
  scoped rows such as P2/P3/B6 that are manual-pending rather than silently
  omitted.

Dependency status: added `serde_json` as a `mc-test-harness` dev-dependency only
to parse the checked-in JSON manifest in the manifest-shape test. The resulting
`Cargo.lock` change is intentional test-only dependency metadata; no runtime
server dependency was added.

Client/manual gate status: prepared only. No real vanilla client was launched
by this slice, so every new scenario remains `manual-pending` and Q2 remains
non-green until a real-client run records artifacts.

## Agent-Run Slice - 2026-06-22

Quality label: `stabilization`.

- Hardened `tools/run-real-client-regression.sh` for the Java-agent path:
  agent injection now uses `JDK_JAVA_OPTIONS` with `--add-modules
  jdk.httpserver`, avoiding helper `jar` JVM pollution, and the outer driver
  timeout now gives the Python driver a 15-second cushion to write fail-closed
  observations before the process is killed.
- Hardened `tools/real-client-agent-driver.py` for quick-play launches: it
  waits briefly for an already-connected vanilla client before sending an
  explicit `connect`, waits for async screenshot creation, and fails closed if
  the expected screenshot is absent.
- Hardened agent-run diagnostics and startup readiness: the Python driver now
  preserves structured bridge error bodies from HTTP 500 responses, and
  `wait_play` treats the Java-agent premain window before
  `Minecraft.getInstance()` is initialized as transient not-in-play state rather
  than failing the run before the client can finish booting.
- Fixed the Java-agent screenshot facade so requested runner paths such as
  `run/screenshots/m94-02b-rejected-block-resync.png` are passed to vanilla
  `Screenshot.grab` with the run directory as its base. Vanilla appends the
  `screenshots/` directory internally.
- Drove a real vanilla 26.1.2 client through
  `m94-02b-rejected-block-resync` against Solaris and validated
  `.analysis/real-client-runs/20260622T135534Z-m94-regression-pack` with
  `tools/run-real-client-regression.sh --validate-run`.
- Added a Java-agent `move_forward` command and changed agent-driven
  disconnects to close the vanilla client's network `Connection` before clearing
  local client state. The Python driver now also waits for the Solaris
  `saved player state` server-log marker before reconnecting, so duplicate
  session rejection is treated as a fail-closed race instead of hidden by a
  sleep.
- Drove a real vanilla 26.1.2 client through
  `m94-01-join-rejoin-chunks-movement` against Solaris and validated
  `.analysis/real-client-runs/20260622T142127Z-m94-regression-pack` with
  `tools/run-real-client-regression.sh --validate-run`.
- Added focused real-client scenario `m94-02a-solid-place-break-drop` and drove
  a real vanilla 26.1.2 client through it against Solaris. The validated
  artifact is
  `.analysis/real-client-runs/20260622T144628Z-m94-regression-pack`: it used a
  real-client item-use placement path, named vanilla destroy packets from inside
  the real client process for the server-timed break/drop path, observed target
  air after break, observed a visible dirt drop, restored the held dirt stack by
  pickup, then placed dirt into a loaded adjacent air cell and observed the
  placed target plus held-stack decrement.
- Added focused real-client scenario `m94-02c-water-bucket-place-pickup` and
  drove a real vanilla 26.1.2 client through it against Solaris. An initial
  run exposed a Solaris fluid recession bug where a removed bucket source could
  be repopulated by stale weak flowing water. The fix keeps non-source flow
  support tied to a bounded path back to a source and is covered by
  `removed_bucket_source_drains_own_spread_from_source_cell`. The validated
  real-client artifact is
  `.analysis/real-client-runs/20260622T152126Z-m94-regression-pack`: it placed
  water through the real-client item-use path, observed the held item become an
  empty bucket, picked the source up with the empty bucket, observed the source
  clear, and observed the held water bucket restored.
- Added focused real-client scenario `m94-04a-regular-sign-place-text` and
  drove a real vanilla 26.1.2 client through it against Solaris. The validated
  artifact is
  `.analysis/real-client-runs/20260622T153556Z-m94-regression-pack`: it placed
  an oak sign through the real-client item-use path at `(2,78,-11)`, observed
  the held sign stack decrement to empty, observed the real sign editor open,
  sent plain four-line text from inside the real client process, and observed
  the client-side sign block entity converge to
  `Solaris|M94|real-client|sign`.
- Added focused real-client scenario `m94-03a-inventory-oak-log-to-planks` and
  drove a real vanilla 26.1.2 client through it against Solaris. The initial
  fallback-id run used embedded recipe display id `16`; the follow-up sidecar
  pass fixed `mc-server` startup so configured `vanilla_data_dir` recipe JSON is
  authoritative, fixed the driver so an already-open `ConnectScreen` is not
  connected a second time, and validated
  `.analysis/real-client-runs/20260622T161221Z-m94-regression-pack`. That run
  loaded `1146` sidecar recipes, gave the client one oak log, sent the vanilla
  `ServerboundPlaceRecipePacket` for inventory container `0` and sidecar recipe
  display id `697`, observed oak logs converge to `0`, and observed oak planks
  increase from `8` to `12`.
- Added focused two-client shared-chest scenario
  `m94-03b-two-client-shared-chest` to the Java agent, two-bridge driver,
  runner, and manifest. The prepared route has a primary real client place a
  chest, move dirt into chest slot `0` through a vanilla menu-click path, and
  has a secondary real client open the same marked chest and observe the slot.
  Initial direct vanilla/Xvfb and PrismLauncher attempts
  `.analysis/real-client-runs/20260622T213302Z-m94-regression-pack` failed
  before Play with client registry/protocol loading errors, and
  `.analysis/real-client-runs/20260622T213826Z-m94-regression-pack` plus
  `.analysis/real-client-runs/20260622T213933Z-m94-regression-pack` failed before
  the scenario reached Play. The sidecar-backed follow-up
  `.analysis/real-client-runs/20260622T221125Z-m94-regression-pack` passed and
  was accepted by `tools/run-real-client-regression.sh --validate-run`: both
  clients reached Play, the primary placed the marked chest and moved dirt into
  chest slot `0` with `moved_slot=true`/`slot_matched=true`, the secondary
  opened the same chest and observed `slot_matched=true`, and both screenshots
  were written.

Client/manual gate status: seven focused scenarios passed through agent-run
real-client automation. `m94-02b` reached Play in `minecraft:overworld`, passed
occupied solid placement rejection, survival out-of-reach solid placement
rejection, and occupied water-bucket fallback/resync observations, and wrote
`screenshots/m94-02b-rejected-block-resync.png`. `m94-01` reached Play, moved
forward for `750` ms with `horizontal_delta=3.673`, observed server-side session
release before reconnect, rejoined Play in `minecraft:overworld`, and wrote
after-move plus after-rejoin screenshots. `m94-02a` passed focused solid
break/drop/pickup/place observations and wrote
`screenshots/m94-02a-solid-place-break-drop.png`. `m94-02c` passed focused
accepted water-bucket placement and pickup observations and wrote
`screenshots/m94-02c-water-bucket-place-pickup.png`. `m94-04a` passed focused
regular sign placement, editor-open, and plain-text block-entity convergence
observations and wrote `screenshots/m94-04a-regular-sign-place-text.png`.
`m94-03a` passed focused inventory recipe execution and wrote
`screenshots/m94-03a-inventory-oak-log-to-planks.png`. `m94-03b` passed
focused two-client shared-chest deposit/observe and wrote
`screenshots/m94-03b-two-client-shared-chest-primary.png` plus
`screenshots/m94-03b-two-client-shared-chest-secondary.png`.

Follow-up broad-row probes added blocked partial artifacts for `m94-02`,
`m94-03`, `m94-04`, `m94-05`, `m94-06`, and `m94-07`. The latest `m94-05`
artifact is
`.analysis/real-client-runs/20260622T190401Z-m94-regression-pack`: it observed
a visible cow, showed the real death screen, performed respawn, and returned to
Play with `current_screen=none`. The latest `m94-07` artifact is
`.analysis/real-client-runs/20260622T191650Z-m94-regression-pack`: it placed and
picked up water, observed a visible drop and pickup convergence, placed dirt
back into loaded air, observed a visible cow, wrote
`screenshots/m94-07-m40-m41-route-with-metrics.png`, and captured a server log
without runtime tick-budget or background-relight warnings during the focused
route. The latest `m94-06` artifact is
`.analysis/real-client-runs/20260622T210703Z-m94-regression-pack`: the
runner-managed before phase placed a dirt marker, sent `save-all`, stopped
Solaris with `kill -INT`, restarted, reconnected the same real client, and
observed the marker persisted after restart. The same run launched
`SolarisObserver` from a separate PrismLauncher root and passed one two-client
live block-visibility subprobe where the observer saw a primary-client dirt
marker, plus one two-client shared-drop subprobe where the observer saw a dirt
item entity produced by the primary client, plus one two-client shared-pickup
subprobe where the primary collected that dirt through the real client and the
observer saw the item disappear. The full M94 pack remains non-green: the six
broad manifest scenarios have blocked partial evidence, and this slice does not
provide vanilla oracle, broad performance, soak, door/trapdoor,
crop/bonemeal, sugar cane, lava, broad fluid-path, swim-feel,
hanging/waxed/styled sign, bed, campfire, hostile combat, drops/XP, vehicles,
broad AI/pathing, shared contention/broad-container evidence, or broad
recipe-book/container/station/M100 coverage.
