# M94 - Full Real-Client Regression Pack

**Status:** Planned. Quality label: `stabilization`.

## Agent Start Prompt

Read M78, M82, M91-M93, and the validation ledger. Run preflight. Expand
real-client evidence to the M100 regression matrix.

## Goal

Replace ad-hoc owner manual checks with reproducible real-client evidence
for scoped client-visible behavior.

## Scope

- Real-client scenarios: join/rejoin, movement, chunk load, block
  break/place/use, inventory, crafting, containers, signs, beds,
  campfires, fluids, farming, drops, combat, death/respawn,
  save/restart, two-client visibility.
- Include named M40/M41 route: swim feel, visible drops/pickup, sugar
  cane behavior, nearby mob/aquatic movement, TPS/lock log capture.
- Capture screenshots or structured observations, client logs, server
  logs, scenario manifests, and commit ids.

## Acceptance

- The regression matrix covers every scoped checklist row or marks each
  missing row `degraded`, `manual-pending`, or `blocked`.
- One scenario is not enough for M94; that was only M78.
- Client-visible blockers become M97 bugs or M100 blockers.

## Scaffold Slice - Implementer 17

Quality label: `stabilization`.

- Added `docs/real-client-regression/manifests/m94-regression-pack.json`, a
  bounded seven-scenario real-client regression pack covering M94 checklist
  rows and the named M40/M41 route.
- Added `docs/real-client-regression/README.md` to pin local artifact layout
  under `.analysis/real-client-runs/<run_id>/` and preserve the rule that
  protocol harnesses are supporting evidence only, not real-client evidence.
- Added a focused manifest-shape test in `mc-test-harness` so future edits do
  not collapse M94 into a single scenario or silently replace real-client
  evidence with protocol-only clients. The test also enforces unique scenario
  ids, required run artifacts, screenshot requirements, and explicit handling for
  scoped rows such as P2/P3/B6 that are manual-pending rather than silently
  omitted.

Dependency status: added `serde_json` as a `mc-test-harness` dev-dependency only
to parse the checked-in JSON manifest in the manifest-shape test. The resulting
`Cargo.lock` change is intentional test-only dependency metadata; no runtime
server dependency was added.

Client/manual gate status: prepared only. No real vanilla client was launched
by this slice, so every new scenario remains `manual-pending` and Q2 remains
non-green until a real-client run records artifacts.
