# M91 - Performance Budget Pass

**Status:** Planned. Quality label: `stabilization`.

## Agent Start Prompt

Read M77-M90, performance docs, M31, M37, M48-M52, and runtime metrics.
Run preflight. Establish budgets and optimize measured bottlenecks only.

## Goal

Produce reproducible low-end, balanced, and high-end performance evidence.

## Scope

- Workloads: solo join/explore, two-client visibility, 20-client balanced
  target where feasible, view distance 8, entities, block edits,
  containers, fluids/random ticks, autosave, restart.
- Metrics: TPS, tick p50/p95/p99, chunk latency, memory, lock wait/hold,
  queue depth, worker saturation, disk IO, outbound retries/drops.
- Cache/IO metrics: prepared chunk entries/bytes/evictions, region cache
  bytes, dirty queue/flush pressure, snapshot allocations, light payload
  reuse, compression costs.
- Required generated-world regression: isolate or delete `.analysis/test-world`,
  let Solaris generate the empty `example.toml` world, restart, then run a
  long `wire-probe` or real-client join at view distance 8. Record chunk
  coverage, first/ring/full chunk latency, tick p50/p95/p99, `fetch_ms`,
  `light_compute_ms`, lock wait/hold, dirty flush pressure, worker/queue
  saturation, and hardware/build details. The M77 baseline emitted all 289
  chunks but took 17.3s and logged repeated tick/lock budget warnings.
- Interactive fairness while generating chunks and rerun M40/M41 manual
  regression workload with metrics.

## Acceptance

- Budgets and actual numbers are recorded with hardware, build mode, and
  commit.
- Generated-world join/chunk streaming is fixed or carried as explicit
  non-green debt. To count green, the view-distance-8 spawn window must
  stream within the recorded budget, with no unexplained 150ms+ runtime
  ticks and no unexplained `chunk_prepare`/`save_all_flush` budget warnings.
- Balanced profile evaluates 20 players, view distance 8, >18 TPS target
  or remains `stabilization`.
- Low-end and high-end bottlenecks are separated.
