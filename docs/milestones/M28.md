# M28 - Containers and inventory parity

**Status:** implemented locally on `dev/M28-containers-inventory-parity` from
`main` at local tag `m27`; ready for owner review.
**Owning crates:** `mc-protocol` for verified click/resync packet shapes,
`mc-data` for stack and durability facts, `mc-world` for chest block-entity
storage and Anvil round-trip, `mc-net` for server-authoritative inventory and
container behavior, and `mc-test-harness` for wire-level survival coverage.

## Goal

Harden the player inventory and common container loop toward vanilla survival
behavior. M28 focuses on server-authoritative click handling, data-backed stack
rules, persistent chest storage, and concurrent viewer synchronization.

M28 is not full container parity. Barrels, hoppers, brewing stands, anvils,
smithing, enchanting, shulkers, villagers/trading, and broad recipe-book UI
behavior remain future work unless a narrow dependency is needed to keep chest or
inventory behavior correct.

## Strategy

1. Verify packet and storage facts before widening behavior. Click modes,
   buttons, resync packets, screen ids, and chest NBT shape must come from the
   local 26.1.2 oracles, not memory.
2. Keep the server authoritative. Client changed slots and carried stack are
   validation hints only; Solaris state decides every mutation and resync.
3. Replace remaining inventory stack heuristics with `mc-data` item facts where
   the M25 component loader already exposes max stack size and durability.
4. Expand click behavior in narrow, testable slices. Unsupported or impossible
   clicks should reject cleanly and send a complete authoritative resync.
5. Make chest contents world-owned. Container views should be projections over
   block-entity state so persistence, concurrent viewers, and later double-chest
   support share the same backing model.

## Sub-Milestones

### M28.a - Plan and branch baseline

Create the milestone branch and this plan from the reviewed M27 closeout state.

Acceptance:

- Branch starts from `main` at local tag `m27`, or records why it does not.
- Only `docs/milestones/M28.md` is part of the plan slice.
- Local-only files under `.serena/`, `work/`, `YOLO_MODE.md`, `opencode.json`,
  `.analysis/`, and `data/vanilla/` stay uncommitted.

### M28.b - Click-mode oracle audit

Pin the 26.1.2 container click modes, button meanings, and related resync packet
facts before widening mutation logic.

Acceptance:

- Document the local oracle source for `ServerboundContainerClick` mode/button
  semantics used by pickup, quick move, hotbar swap, throw, quick craft, and
  pickup-all.
- Verify any newly used clientbound packet ids/layouts, including screen open,
  slot/content resync, carried-item resync, and close behavior.
- Existing packet codecs either match the verified shapes or are corrected with
  focused round-trip tests.
- No protocol facts are inferred from memory or third-party code.

### M28.c - Data-backed stack rules

Apply item component stack and durability facts consistently through inventory
and container mutation paths.

Acceptance:

- `mc-net` stack splitting, merging, pickup, hotbar swap, crafting result, furnace
  output, bucket, and drop/pickup paths ask `mc-data` for max stack size where
  available.
- Durability/component separation remains preserved by item equality rules; tools
  and damaged stacks do not merge incorrectly.
- Remaining fallback heuristics are explicit, narrow, logged or testable, and
  documented as scope gaps.
- Focused tests cover 64-stack, 16-stack if available, non-stackable/durable, and
  damaged-stack merge rejection paths.

### M28.d - Click semantics expansion

Expand server-authoritative click handling beyond the current pickup and narrow
quick-move behavior.

Acceptance:

- Shift-click routing handles player inventory, hotbar, armor slots, crafting
  result, furnace, and chest-like container slots without item loss or dupes.
- Hotbar number-key swap, offhand swap where applicable, and throw/drop clicks
  mutate only legal slots and resync rejected claims.
- Quick-craft/drag and pickup-all are either implemented for the common survival
  cases or explicitly rejected with verified resync behavior and tests.
- Client changed-slot hashes are never trusted as authority; impossible claims are
  rejected without corrupting server inventory state.
- Harness tests cover accepted clicks, stale/impossible clicks, and authoritative
  resync after rejection.

### M28.e - Single chest block entity

Add world-owned single chest contents with screen open, slot mapping,
persistence, and viewer synchronization.

Acceptance:

- Chest block-entity NBT shape is verified from a local 26.1.2 oracle and
  documented in this file.
- `mc-world` models single chest inventory state and round-trips it through real
  Anvil storage without dropping unknown unrelated block-entity data.
- Right-clicking a single chest opens the verified menu type with stable slot
  mapping between chest slots and player inventory slots.
- Chest clicks mutate world-owned contents, mark the chunk dirty, and synchronize
  all viewers with authoritative slot/content updates.
- Harness/storage tests cover open, insert, extract, restart persistence, and two
  concurrent viewers seeing the same mutation.

### M28.f - Double chest basics

Add adjacent chest merge/split behavior only after the single chest path is stable
and the packet/data facts are verified.

Acceptance:

- Placement/open rules identify a left/right adjacent chest pair without breaking
  the single chest path.
- Opening either half exposes one combined 54-slot container backed by both
  block entities or by a documented world-owned aggregate view.
- Breaking one half closes or resyncs viewers safely and leaves the surviving half
  with correct contents.
- If double-chest facts cannot be verified quickly, this slice is deferred in
  closeout rather than guessed.

### M28.g - Death and explicit item drops

Complete server-side inventory drops for death and explicit inventory drop
actions.

Acceptance:

- Player death drops inventory and carried-item contents server-side through the
  existing item-entity path, then clears authoritative inventory state.
- Explicit drop clicks from inventory/hotbar create item entities for the legal
  stack amount and resync rejected drop attempts.
- Keep-inventory, gamerules, and rich death behavior are documented as out of
  scope unless already represented in current config.
- Harness tests cover death inventory clear/drop, pickup after death, and explicit
  inventory throw/drop behavior.

### M28.h - Closeout

Update this document with final landed commits, validation, scope drift, and
remaining container follow-ups.

Acceptance:

- `cargo fmt --all -- --check` passes.
- `cargo test --workspace` passes.
- `cargo clippy --workspace --all-targets -- -D warnings` passes.
- Manual PrismLauncher gate is either recorded or explicitly deferred under the
  active owner instruction.
- Remaining container gaps are listed without claiming full vanilla parity.

## Risks / Pitfalls

- Container click behavior is dupe-prone. Reject unsupported modes with complete
  authoritative resync until each mode has a focused test.
- Client changed slots may look convenient but are not truth. Treat them as drift
  diagnostics, not mutation input.
- Chest ownership can split between connection-local state and world storage if
  the model is not explicit. M28 should make world/chunk state authoritative from
  the first chest slice.
- Double chests have orientation and pairing edge cases. If local oracles do not
  answer them quickly, ship single chest plus a documented follow-up.
- Stack limits and durability must not regress M25's data seam by adding new
  private item-name arrays in `mc-net`.
- Quick-craft and pickup-all can balloon into UI parity. Implement the narrow
  survival-visible path or reject cleanly with verified resync.

## Validation

Run before every code commit and at closeout:

```sh
cargo fmt --all -- --check
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
```

Focused gates to add/run during implementation:

```sh
cargo test -p mc-data item_components -- --nocapture
cargo test -p mc-world chest_block_entities -- --nocapture
cargo test -p mc-world anvil_chest_block_entities -- --nocapture
cargo test -p mc-net --lib inventory -- --nocapture
cargo test -p mc-test-harness --test block_edit container_clicks -- --nocapture
cargo test -p mc-test-harness --test block_edit chest_container -- --nocapture
cargo test -p mc-test-harness --test mob_presence death_drops_inventory -- --nocapture
```

## What Landed Where

| Sub-milestone | Status | Commit | Key artifact |
|---|---|---|---|
| M28.a - Plan and branch baseline | landed locally | pending | milestone plan |
| M28.b - Click-mode oracle audit | landed locally | pending | click-mode/resync oracle notes |
| M28.c - Data-backed stack rules | landed locally | pending | shared stack-limit path |
| M28.d - Click semantics expansion | landed locally | pending | authoritative click modes |
| M28.e - Single chest block entity | landed locally | pending | persistent shared chest storage |
| M28.f - Double chest basics | landed locally | pending | adjacent chest aggregate view |
| M28.g - Death and explicit item drops | landed locally | pending | inventory drop lifecycle |
| M28.h - Closeout | landed locally | pending | validation and follow-ups |

## Oracle Notes

M28.b checked the local 26.1.2 protocol dump and permitted metadata before click
implementation work:

- `ServerboundContainerClick` is game serverbound id `0x12`; field order is
  `containerId`, `stateId`, `slotNum`, `buttonNum`, `containerInput`,
  `changedSlots`, `carriedItem`. `containerId` and `stateId` are VarInts,
  `slotNum` is short, `buttonNum` is byte, and changed slots are capped at 128
  hashed-stack entries.
- `ContainerInput` ids are metadata-pinned as `0=PICKUP`, `1=QUICK_MOVE`,
  `2=SWAP`, `3=CLONE`, `4=THROW`, `5=QUICK_CRAFT`, and `6=PICKUP_ALL`.
- Quick-craft uses `button = header | (type << 2)`, with headers start `0`, add
  `1`, end `2`; type `2` is clone-only and therefore not valid for normal
  survival handling. Outside slot is `-999`.
- Relevant resync packets are already present in `mc-protocol`: `ContainerClose`
  id `0x11`, `ContainerSetContent` id `0x12`, `ContainerSetData` id `0x13`,
  `ContainerSetSlot` id `0x14`, `OpenScreen` id `0x3b`, `SetCursorItem` id
  `0x60`, and `SetPlayerInventory` id `0x6c`.
- Menu registry ids needed by M28: `generic_9x3 = 2`, `generic_9x6 = 5`,
  `crafting = 12`, and `furnace = 14`.

M28.c routes normal inventory/container stack limits through the M25
`ItemFactsTable` when component facts are available. The previous `mc-net`
item-name checks remain only as a fallback for missing component reports.

M28.d expands the authoritative click path for common survival actions: number-key
swap in player/crafting/furnace views, offhand swap in the player inventory view,
and Q/Ctrl-Q throw from legal player/menu slots. Quick-craft, clone, and
pickup-all still reject with full authoritative resync until a focused parity
slice handles their client-side drag/collection semantics.

M28.e models single chest block entities as 27-slot world-owned storage. The
verified Anvil shape matches the common block-entity item list: `id =
"minecraft:chest"`, absolute `x/y/z`, and `Items` entries with `Slot`, `id`, and
`count`. Solaris opens it as menu `generic_9x3 = 2`, maps slots `0..=26` to the
chest, `27..=53` to player main inventory, and `54..=62` to hotbar. Concurrent
viewers of the same chest receive authoritative slot updates from the world-owned
chest state.

M28.f opens two horizontally adjacent chest blocks as a documented world-owned
aggregate view using verified menu `generic_9x6 = 5`. The aggregate keeps each
half backed by its own 27-slot `ChestBlockEntity`; menu slots `0..=26` map to the
canonical lower-sorted block position, `27..=53` map to the other half,
`54..=80` map to player main inventory, and `81..=89` map to hotbar. Opening
either half uses the same canonical viewer key so concurrent viewers of the pair
receive shared authoritative slot updates. If one half is gone, the surviving
block opens through the single-chest path with its own contents.

M28.g clears and drops player inventory contents on the first transition to dead
survival state. Slots `1..=45` and the carried item are emitted through the
existing item-entity drop path, slot `0` remains a virtual crafting result, and
the client receives a full authoritative inventory resync after the clear.
Explicit inventory Q/Ctrl-Q drops continue to use the same item-entity path from
the M28.d click handling.

## Closeout Validation

Automated closeout gates passed locally:

```sh
cargo fmt --all -- --check
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
```

Additional focused gates run during closeout:

```sh
cargo test -p mc-net --lib -- --nocapture
cargo test -p mc-test-harness --test block_edit survival_double_chest_opens_combined_storage_and_mutates_second_half -- --nocapture
cargo test -p mc-test-harness --test block_edit dead_survival_player_cannot_mine_or_eat -- --nocapture
cargo test -p mc-test-harness --test block_edit dead_survival_player_can_respawn_and_act_again -- --nocapture
cargo clippy -p mc-net --lib -- -D warnings
cargo clippy -p mc-test-harness --test block_edit -- -D warnings
```

Manual PrismLauncher 26.1.2 gate was not run in-agent and is deferred for the
owner.

## Scope Drift / Follow-ups

- M28 is not full container parity. Barrels, hoppers, brewing stands, anvils,
  smithing, enchanting, shulkers, villagers/trading, and recipe-book UI parity
  remain future milestones.
- Quick-craft/drag, clone, and pickup-all are intentionally rejected with full
  authoritative resync until a focused parity slice handles their client-side
  semantics.
- Double chest placement uses the existing block placement path and opens any
  horizontal adjacent chest pair as a deterministic aggregate; exact vanilla
  left/right orientation conversion and neighbor state rewrites remain a follow-up.
- Breaking one half leaves the surviving block entity authoritative and it opens
  as a single chest. Rich viewer close/reopen behavior for already-open split
  pairs remains future polish.
- Keep-inventory gamerule and richer death behavior are out of scope; M28 always
  drops inventory on first transition to dead survival state.
