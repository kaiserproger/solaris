# M44 - Physics parity fixes

**Status:** plan on `main` from local M41 recovery head `8b85e97`
(`feat: M39-M41 manual recovery hardening`), refreshed on
`dev/M43-vanilla-physics-validation` with the M43 deferred backlog. M44 must
consume the M43 mismatch report; it should not start by guessing fixes.
**Owning crates:** `mc-physics` for movement/collision primitives, `mc-net` for
play-state behavior and packet dispatch, `mc-world` for block/fluid/scheduled tick
state, `mc-entity` for AI goals, and `mc-test-harness` for regression coverage.

## Goal

Fix the highest-priority vanilla physics mismatches proven by M43, with tests that
exercise the same real play/runtime paths that failed manually.

M44 is the implementation counterpart to M43. Every non-trivial fix must cite the
M43 oracle or manual capture that proves the mismatch.

## Current Foundation

- M40/M41 added partial water/drop/mob recovery work but explicitly did not close
  the manual gameplay gaps.
- M43 added oracle workflow/capture scaffolding, a deterministic physics fixture,
  scripted water-entry capture support, a Solaris water-motion observation, and a
  sugar-cane support-break water-replacement observation.
- M43 did not complete vanilla captures for every planned scenario; M44.a must
  finish or explicitly reject those captures before gameplay fixes.
- Existing systems are intentionally scoped: Solaris is not trying to be bit-perfect
  vanilla redstone/vehicle/projectile parity in this milestone.

## M43 Handoff Backlog

M44.a must turn this handoff into a ranked fix list before changing gameplay code.

The minimum triage matrix is:

| Category | M44 decision required |
|---|---|
| A. Collision | Player and entity AABB against ground, wall, selected step-up, correction behavior, entity-vs-block, entity-vs-fluid, and entity-vs-entity push/collision. |
| B. Water/fluid spread | Source/flowing spread, replacement on block break, water/lava interaction, scheduled fluid ticks. |
| C. Falling blocks | `sand`, `gravel`, `anvil`, and any selected support-sensitive falling blocks. |
| D. Player/entity falling | Gravity, landing, fall damage, water-entry suppression, mob/entity vertical motion. |
| E. Swimming | Entering water, sprint-swim, pitch-driven swimming, jump/shift, surfacing, leaving water. |

Priority 1 candidates for M44:

- Player water and swimming: compare vanilla captures for shallow entry, deep
  swimming, sprint-swim, looking up/down, jump/shift, surfacing, and leaving water
  against Solaris' current runtime `SetEntityMotion` observation. Fix only the
  proven mismatch boundary.
- Sugar cane support and block breaks: M43 proved support-cell replacement next
  to source water should become `minecraft:water[level=1]`, not source water;
  Solaris has been updated for that slice. M44 still needs vanilla captures for
  sugar cane cascade, drops, scheduled ticks, and persistence.
- Item drop and pickup timing: capture survival block drops, mob/death drops,
  creative/no-loot negatives, pickup delay/window, `TakeItemEntity`, removal, and
  inventory ordering. Avoid manufactured tests that bypass loot/items sidecars.

Priority 2 candidates for M44 if Priority 1 is bounded:

- Entity and aquatic movement: capture one land passive, one hostile, one aquatic
  fish-like entity, and item entities in/near water; separate AI goal choice from
  collision/fluid physics and packet dispatch.
- Collision and fall damage: capture flat ground, wall collision, selected step-up,
  landing fall damage, and water-entry fall-damage suppression; keep client
  prediction separate from server corrections.
- Mob collision: vanilla-capture mob-vs-block, mob-vs-fluid, and mob-vs-mob cases.
  Solaris currently has entity-vs-block/fluid primitives but no proven
  entity-vs-entity collision/push runtime; do not fake this with manufactured
  state.
- Fluid interaction parity: capture water/lava replacement, source/flowing states,
  underwater breaking, and scheduled fluid tick behavior.

Deferred beyond M44 unless the owner reprioritizes:

- Bit-perfect redstone, vehicles, boats, projectiles, and full vanilla movement
  engine parity.
- Broad low-priority mob pathfinding polish and exact despawn edge cases.
- Performance-only follow-up for any physics fix that regresses TPS/lock metrics;
  split that into a later hardening milestone if needed.

## Strategy

1. Fix only cases reproduced or captured in M43.
2. Land fixes in small slices with one behavior class per commit where practical.
3. Keep vanilla-oracle citations in tests or milestone notes, not in speculative
   comments.
4. Preserve replacement-server behavior already covered by M38-M41.
5. Prefer server-authoritative correctness over client-side visual hacks; use
   visual assists only when vanilla behavior is client-predicted and documented.

## Sub-Milestones

### M44.a - M43 backlog triage

Convert the M43 mismatch report into a fix order.

Acceptance:

- The M43 handoff backlog above is resolved into `fix now`, `capture first`, or
  `defer` entries.
- Each selected fix has severity, reproduction, oracle citation, impacted crates,
  and expected regression tests.
- Deferred mismatches are explicitly listed with why they are out of M44 scope.
- No gameplay code changes land before this triage is written.

### M44.b - Player water and swimming fixes

Fix the player water behavior proven wrong by M43.

Acceptance:

- Entering water, swimming horizontally, swimming up/down, surfacing, and leaving
  water match the M43 oracle within documented tolerance.
- The fix handles serverbound input/commands, pose metadata, motion/correction, and
  fall damage/trample side effects as required by the oracle.
- A vanilla client can manually swim in PrismLauncher 26.1.2 without bottom-walking
  as the only available movement.
- Existing movement, reach, mining, and multiplayer visibility tests still pass.

### M44.c - Drop visibility and pickup fixes

Fix item-drop behavior proven wrong by M43.

Acceptance:

- Survival block drops are visible before pickup and collect promptly.
- Creative/no-loot cases do not spawn drops.
- Mob drops and death drops keep correct spawn/data/take/remove/inventory ordering.
- Pickup delay is stored and advanced in a way that survives normal tick timing and
  does not require ad-hoc side effects at each spawn site.

### M44.d - Sugar cane and block support fixes

Fix support/break behavior for sugar cane and other M43-selected block support
cases.

Acceptance:

- Breaking support blocks updates dependent blocks and drops according to the M43
  vanilla oracle.
- Scheduled updates do not disappear when chunks unload/reload.
- Fluid adjacency/support rules use data-derived block facts where possible.
- Block edit and persistence harnesses cover the real play path.

### M44.e - Aquatic and land-mob water movement fixes

Fix mob movement cases proven wrong by M43.

Acceptance:

- Fish/squid-like entities move through water with sane vertical and horizontal
  motion and orientation.
- Land mobs in water float/swim/escape according to the scoped oracle behavior.
- Entity motion packets, relative moves, rotations, and visibility updates remain
  bounded and per-viewer correct.
- Existing combat and mob-drop tests remain green.

### M44.f - Collision and fall-damage fixes

Fix selected collision/fall-damage mismatches from M43.

Acceptance:

- Flat ground, wall collision, selected step-up behavior, water-entry fall damage,
  and landing fall damage match M43 expectations.
- Player and entity AABB changes do not break reach validation or mob melee tests.
- Any remaining mismatch is explicitly deferred.

### M44.g - Fluid interaction fixes

Fix selected water/lava/fluid-flow mismatches from M43.

Acceptance:

- Source/flowing states, bucket replacement, underwater breaking, water/lava solid
  formation, and scheduled fluid ticks match the selected oracle cases.
- Existing fluid persistence and scheduled tick tests remain green.
- Work remains bounded by simulation budgets.

### M44.h - Integrated manual replacement gate

Run the owner-facing scenario that originally failed M40/M41.

Acceptance:

- Player can swim acceptably in water.
- Broken survival blocks show visible collectible drops.
- Sugar cane support/break behavior is correct for scoped cases.
- Nearby mobs move, and fish/aquatic mobs no longer show the known sideways/bottom
  behavior from the manual report.
- TPS/lock metrics remain acceptable after M42, or any remaining pressure is
  documented as a separate performance follow-up.

### M44.i - Closeout

Record fixes, oracle citations, validation, manual-gate status, and deferred cases.

Acceptance:

- `cargo fmt --all -- --check` passes.
- `cargo test --workspace` passes.
- `cargo clippy --workspace --all-targets -- -D warnings` passes.
- Closeout maps each fixed behavior back to its M43 mismatch id.

## M44.a Triage

M43 did not land a complete mismatch report. It landed the oracle workflow, a
deterministic Solaris fixture, one Solaris shallow-water motion observation, and
one vanilla-backed sugar-cane support-cell replacement assertion. The only
vanilla-proven gameplay mismatch from M43 was the support-cell replacement next
to source water; that was already fixed in M43. M44 therefore starts with
capture-first work for the remaining Priority 1 gameplay gaps instead of
guessing constants or packet side effects.

### Category Decisions

| Category | M44 decision | Reason |
|---|---|---|
| A. Collision | capture first | M43 only prepared a ledge fixture. No vanilla flat-ground, wall, step-up, or entity-vs-entity oracle was committed. |
| B. Water/fluid spread | capture first | M43 proved one flowing-water replacement slice. Source/flowing spread, water/lava, underwater break, and scheduled ticks still lack captures. |
| C. Falling blocks | capture first | Fixture has sand/gravel/anvil targets, but no vanilla fall-start capture or Solaris comparison exists yet. |
| D. Player/entity falling | capture first | Fall damage and water-entry suppression are not vanilla-backed yet; do not change damage rules speculatively. |
| E. Swimming | capture first, then fix | M43 has a Solaris `SetEntityMotion` observation for shallow entry but no matching vanilla capture. This remains the highest manual pain point. |

### Ranked Fix Order

| Rank | Decision | Severity | Reproduction / oracle citation | Impacted crates | Expected regression tests |
|---|---|---|---|---|---|
| 1 | capture first, then fix player water/swimming | high | `docs/physics-oracles.md` scenarios `player-shallow-water-entry`, `player-deep-water-swim`, and `player-water-surface-exit`; M43 Solaris observation `shallow_water_entry_emits_player_motion_observation` | `mc-net`, `mc-physics`, `mc-test-harness` | Extend `physics_validation` with vanilla-backed motion/metadata/correction assertions; keep movement/reach/player presence tests green. |
| 2 | capture first, then fix visible drops/pickup | high | `docs/physics-oracles.md` scenario `item-water-drop-window`; M43 deferred `AddEntity`, metadata, pickup delay, `TakeItemEntity`, removal, and inventory ordering | `mc-net`, `mc-world`, `mc-test-harness` | Real survival block-break and mob-death paths assert visible `AddEntity` before pickup, delayed collection, `TakeItemEntity`, removal, and inventory slot updates. |
| 3 | capture first, then fix sugar-cane cascade/drops | high | M43 vanilla-backed support-cell assertion at `external_vanilla_sugar_cane_support_break_oracle`; cascade/drop/scheduled tick behavior still pending | `mc-net`, `mc-world`, `mc-test-harness` | `block_edit`/`physics_validation` cover creative and survival support breaks, cascade updates, drops, and persistence through reload. |
| 4 | capture first, then fix aquatic/land mob water motion | medium | `docs/physics-oracles.md` scenarios `entity-land-passive-motion`, `entity-hostile-motion`, and `entity-aquatic-motion`; M43 deferred all mob captures | `mc-entity`, `mc-net`, `mc-physics`, `mc-test-harness` | Mob presence/combat tests plus water-motion capture comparisons for one land passive, one hostile, and one aquatic entity. |
| 5 | capture first, then fix collision/fall damage | medium | `docs/physics-oracles.md` scenario `collision-wall-step-fall`; M43 fixture only | `mc-physics`, `mc-net`, `mc-test-harness` | Focused collision/fall tests for ground, wall, selected step-up, landing damage, and water-entry suppression. |
| 6 | capture first, then fix fluid interaction parity | medium | `docs/physics-oracles.md` scenario `water-lava-replacement`; M43 only proved support-cell flowing-water replacement | `mc-world`, `mc-net`, `mc-test-harness` | Fluid persistence/scheduled-tick tests plus water/lava replacement and underwater break runtime paths. |
| 7 | capture first, then fix falling blocks | low | `docs/physics-oracles.md` scenario `sand-gravel-fall-start`; M43 fixture targets only | `mc-world`, `mc-net`, `mc-test-harness` | Real scheduled/block-update path asserts sand/gravel/anvil start falling and persist/update correctly. |

### Deferred Beyond M44

- Bit-perfect vanilla movement, redstone, vehicles, boats, and projectiles remain
  out of scope for this physics-parity milestone.
- Broad mob pathfinding polish and exact despawn edge cases stay deferred unless
  they block the scoped M40/M41 manual replacement gate.
- Entity-vs-entity push/collision is not implemented in Solaris and needs a
  vanilla-backed design slice before code changes; M44 may capture it but should
  not expand into a full collision rewrite.
- Performance-only work caused by these fixes should split into a later
  hardening milestone unless a regression breaks the M44 acceptance gates.

## M44.b Capture Progress

The player-water capture tooling now scripts all three M44.b water scenarios and
prepares the vanilla fixture automatically when `--server-kind vanilla` is used.
Local vanilla 26.1.2 was started from `.analysis/vanilla-physics-server/` with
Java 25 because the default Java 17 runtime cannot load classfile 69.
`wire-probe` now annotates `SetEntityMotion`, `SetEntityData`, and
`SynchronizePlayerPosition` frames in captures to make the M44.b comparison
signals explicit.

Successful local captures, not committed:

- `.analysis/physics-oracles/player-shallow-water-entry.vanilla.capture`
- `.analysis/physics-oracles/player-deep-water-swim.vanilla.capture`
- `.analysis/physics-oracles/player-water-surface-exit.vanilla.capture`
- `.analysis/physics-oracles/player-shallow-water-entry.solaris.capture`
- `.analysis/physics-oracles/player-deep-water-swim.solaris.capture`
- `.analysis/physics-oracles/player-water-surface-exit.solaris.capture`

Capture commands used the opped offline profile `M43Oracle` against
`127.0.0.1:25566`, for example:

```sh
cargo run -p mc-test-harness --bin wire-probe -- \
  --addr 127.0.0.1:25566 \
  --name M43Oracle \
  --server-kind vanilla \
  --scenario player-deep-water-swim \
  --play-seconds 3 \
  --out .analysis/physics-oracles/player-deep-water-swim.vanilla.capture
```

M44.b comparison result:

| Scenario | Vanilla player entity | Solaris player entity | Post-input player correction/motion | M44 decision |
|---|---:|---:|---|---|
| `player-shallow-water-entry` | 64 | 1 | neither server emitted a player `SetEntityMotion` or extra `SynchronizePlayerPosition` after the scripted input | no gameplay fix proven by wire capture |
| `player-deep-water-swim` | 128 | 2 | neither server emitted a player `SetEntityMotion` or extra `SynchronizePlayerPosition` after the scripted input | no gameplay fix proven by wire capture |
| `player-water-surface-exit` | 188 | 3 | neither server emitted a player `SetEntityMotion` or extra `SynchronizePlayerPosition` after the scripted input | no gameplay fix proven by wire capture |

These captures prove the oracle workflow for M44.b is runnable and that Solaris
does not obviously reject the scripted water movement packets. They do not prove
visual swim parity: vanilla client swimming is mostly client-predicted in this
window, and the wire capture does not expose the local client pose/motion state.
M44.b should therefore keep player water gameplay code unchanged until a manual
PrismLauncher gate or a runtime-side state assertion proves a concrete mismatch.

## M44.c Capture Progress

`wire-probe` now has an `item-water-drop-window` scenario and typed annotations
for the packet sequence M43 wanted to prove before changing drop or pickup
runtime behavior: `AddEntity`, `SetEntityData`, `BlockUpdate`,
`BlockChangedAck`, `ClientboundContainerSetSlot`, `ClientboundTakeItemEntity`,
and `RemoveEntities`. The probe also gained `ServerboundSwing` from
`.analysis/protocol-dump.txt` (`SERVERBOUND_SWING`, game-SB index 63, wire id
`0x3F`, single `InteractionHand` field) because vanilla survival mining needs
the normal swing/tick cadence instead of only a start/stop destroy pair.

The first scripted vanilla attempts were invalid: they accepted the destroy
sequence but never produced a target drop. The missing lifecycle packet was
`ServerboundPlayerLoaded`; after adding it, the local vanilla 26.1.2 capture at
`.analysis/physics-oracles/item-water-drop-window.vanilla.capture` produced a
clean target break/drop/pickup window for dirt at `(0, 64, 1)`:

| Signal | Vanilla capture |
|---|---|
| loaded gate | `PlayerLoaded` before fixture commands and mining |
| target setup | `BlockUpdate: pos=(0, 64, 1) state_id=10` |
| start ack | `BlockChangedAck: sequence=201` |
| target drop | `AddEntity: entity_id=683 type_id=71 pos=(0.268478, 64.331120, 1.455045)` followed by `SetEntityData` item stack `item_id=28` |
| break completion | `BlockUpdate: pos=(0, 64, 1) state_id=0`, held shovel damage slot update, `BlockChangedAck: sequence=202` |
| pickup | `TakeItemEntity: item_entity_id=683 player_entity_id=636 amount=1`, inventory slot `37` receives `item_id=28`, then `RemoveEntities: entity_ids=[683]` |

No gameplay change was needed for this slice. Solaris already has the focused
regression `survival_break_drops_item_entity_and_picks_it_up`, which asserts the
same evidence-backed ordering: break update/ack before item spawn, item metadata
before pickup/removal, a visible window of at least 120 ms, `TakeItemEntity`,
inventory update, and `RemoveEntities`. The external Solaris `wire-probe`
comparison remains noisy against the default live terrain and should not replace
the deterministic harness test.

## M44.d Capture Progress

The M43 vanilla sugar-cane support oracle was expanded from a one-block cane
fixture to a three-block column at `(8, 64..=66, 0)`. Local vanilla 26.1.2 proves
that creative breaking the support dirt at `(8, 63, 0)` both replaces the support
cell with flowing water and cascades every cane block in the column to air. The
oracle is the external harness gate:

```sh
M43_VANILLA_ADDR=127.0.0.1:25566 \
  cargo test -p mc-test-harness --test physics_validation \
  external_vanilla_sugar_cane_support_break_oracle -- --nocapture
```

Solaris previously matched only the support-cell replacement; it did not emit
updates for `(8, 64..=66, 0)`. `mc-net` now extends the shared block-break edit
planner so a broken support or lower cane block clears the contiguous sugar-cane
column above it in the same real play-path edit batch. The focused Solaris gate
`sugar_cane_support_break_emits_real_block_edit_observation` now asserts the
same support replacement and cane-column cascade as the vanilla oracle.

Survival sugar-cane drop details remain capture-first. This slice intentionally
does not guess whether dependent cane drops should spawn alongside the support
block drop; it fixes the vanilla-proven support/cascade block updates only.

## M44.e Capture Progress

`wire-probe` now scripts the three deferred M43 entity-water scenarios:
`entity-land-passive-motion` summons a cow, `entity-hostile-motion` summons a
zombie, and `entity-aquatic-motion` summons a cod into a command-built water
pool. The probe also decodes `ClientboundMoveEntityPacket$Pos` (`wire id 0x35`)
so the captures can cite both `SetEntityMotion` and relative position packets.

Sequential local vanilla 26.1.2 captures were written to
`.analysis/physics-oracles/entity-*.vanilla.capture`. The world still contains
background persisted entities, so the target signal is the post-`summon` entity:
cow `entity_id=108`, zombie `entity_id=409`, and cod `entity_id=664` in the
current local captures. All three target windows show vanilla sending
`SetEntityMotion` alongside relative moves while the entity is in water.

Solaris already sent velocity packets for aquatic entities only. `mc-net` now
sends movement velocity for non-item, non-XP server-owned entities when their
physics velocity is non-zero, preserving item drop movement without an extra
velocity packet. Focused coverage asserts that a moving zombie relative move has
`send_velocity=true` while an item drop relative move keeps `send_velocity=false`.

## M44.f Collision/Fall Decision

M44.f remains capture-first and does not land gameplay changes. M43 prepared the
fixture geometry for walls, ledges, falling blocks, and farmland, but did not
capture vanilla flat-ground, wall-correction, selected step-up, landing-damage,
or water-entry fall-damage behavior. The existing Solaris coverage exercises the
current primitives (`mc-physics` wall collision, full-block non-step-up, water
drag/buoyancy, long-fall clamp; `mc-net` fall-damage threshold and survival
damage lifecycle), but those tests are not a vanilla parity oracle.

Changing collision or fall-damage constants from these tests would violate the
M44/M43 capture-first rule. The remaining work is explicitly deferred to a future
oracle slice that records `collision-wall-step-fall` against vanilla and compares
Solaris packet corrections/damage outcomes before touching gameplay.

## M44.g Fluid Decision

M44.g also remains capture-first beyond the M44.d support-cell water replacement
slice. Existing Solaris coverage already covers source/flowing classification,
bucket source resolution, water/lava solidification edits, scheduled fluid tick
delay/budgeting, and sideways/downward spread in unit tests, plus the M44.d
real-play flowing-water replacement next to a broken support block. No additional
vanilla capture was produced for underwater breaking, broad source/flowing spread,
or scheduled tick timing.

No new fluid gameplay change landed here. The safe closeout state is to keep the
existing tests green, keep the `water-lava-replacement`/underwater/scheduled-tick
oracle names documented, and require a vanilla capture before changing runtime
fluid spread or replacement behavior again.

## M44.h/M44.i Closeout

The integrated manual replacement gate is ready for the owner-run PrismLauncher
check against a debug `mc-server`, but it was not run by the agent. The scoped
server-side fixes that did land are traceable to local vanilla oracles or focused
Solaris regressions: water capture comparison bounded M44.b, item drop visibility
was already covered by the deterministic block-edit regression after a vanilla
drop-window capture, sugar-cane support cascade was fixed from the external
vanilla oracle, and mob water movement velocity packets were fixed from the three
entity-water captures.

Deferred after M44: visual swimming feel, survival sugar-cane drop details,
collision/fall-damage parity, broad fluid parity, falling-block runtime behavior,
and entity-vs-entity push/collision. Each requires a new vanilla-backed oracle
before gameplay changes.

Future milestone candidates:

| Candidate | Deferred finding | Required first artifact |
|---|---|---|
| M45 candidate - manual physics feel | Visual swimming feel and owner-facing M40/M41 water movement symptoms | PrismLauncher 26.1.2 manual gate notes plus any runtime-side player-state assertion needed to make the symptom testable |
| M45 candidate - support/drop parity | Survival sugar-cane dependent drops after support breaks | Vanilla survival cane-support drop capture before changing loot/drop runtime |
| M46 candidate - collision/fall parity | Flat-ground, wall, step-up, landing fall damage, and water-entry fall-damage suppression | `collision-wall-step-fall` vanilla capture and Solaris comparison |
| M47 candidate - broad fluid/falling-block parity | Underwater breaking, water/lava/source/flowing spread timing, scheduled fluid ticks, sand/gravel/anvil fall-start | `water-lava-replacement` and `sand-gravel-fall-start` vanilla captures plus Solaris real-play regressions |
| Later hardening | Entity-vs-entity push/collision | Separate vanilla-backed design slice; not a drive-by M44 fix |

## Validation

Baseline gates:

```sh
cargo fmt --all -- --check
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
```

Focused gates expected during implementation:

```sh
cargo test -p mc-test-harness --test physics_validation -- --nocapture
cargo test -p mc-physics --lib -- --nocapture
cargo test -p mc-net water pickup physics -- --nocapture
cargo test -p mc-test-harness --test block_edit -- --nocapture
cargo test -p mc-test-harness --test mob_presence -- --nocapture
cargo test -p mc-test-harness --test player_presence -- --nocapture
cargo test -p mc-test-harness --test persistence_inventory -- --nocapture
```

Manual gate:

```sh
RUST_LOG=mc_server=info,mc_net::server=debug,mc_net::lock_metrics=warn \
  cargo run --bin mc-server -- --config example.toml
```

Then connect PrismLauncher 26.1.2 and rerun the M40/M41 owner scenario plus every
M43 high-priority mismatch scenario.

## Risks / Pitfalls

- Fixes can accidentally chase visual symptoms instead of the server-side mismatch
  M43 identified.
- Player movement changes can fight vanilla client prediction and feel worse even
  if tests pass.
- Block support fixes can create cascading updates that break tick budgets.
- Mob movement fixes can regress combat, pickup, despawn, or visibility ordering.
- M44 must not become an unbounded physics rewrite; defer low-priority parity.

## What Landed Where

| Sub-milestone | Status | Commit | Key artifact |
|---|---|---|---|
| M44.a - M43 backlog triage | drafted | pending | ranked capture-first fix order |
| M44.b - Player water and swimming fixes | capture comparison bounded | pending | water probe scripts + vanilla/Solaris captures |
| M44.c - Drop visibility and pickup fixes | oracle captured; existing regression passed | pending | vanilla drop-window capture + `survival_break_drops_item_entity_and_picks_it_up` |
| M44.d - Sugar cane and block support fixes | support cascade fixed; drops still capture-first | pending | vanilla-backed three-block cane cascade |
| M44.e - Aquatic and land-mob water movement fixes | vanilla motion captured; mob velocity packets fixed | pending | entity-water captures + non-item velocity dispatch |
| M44.f - Collision and fall-damage fixes | deferred capture-first | pending | no vanilla collision/fall oracle captured |
| M44.g - Fluid interaction fixes | deferred capture-first | pending | existing fluid tests green; no new vanilla fluid oracle captured |
| M44.h - Integrated manual replacement gate | ready for owner-run manual gate | pending | debug server command documented |
| M44.i - Closeout | drafted | pending | fixed/deferred behavior mapped to oracles |
