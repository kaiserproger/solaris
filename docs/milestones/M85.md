# M85 - Entity Ecosystem And Pathing Baseline

**Status:** Draft implementation in progress. Quality label: `draft`.

## Agent Start Prompt

Read M77-M84, M32-M33, M50, and entity harness tests. Run preflight.
Improve entities only where ledger-ranked blockers remain.

## Goal

Move entities from simple presence/combat toward a stable scoped survival
ecosystem.

## Scope

- Spawn/despawn caps and predicates for scoped passive/hostile mobs.
- Loaded-terrain bounded pathing: flat movement, step-up, simple detour,
  stuck recovery, no chunk generation from AI ticks.
- Passive behavior: wander and panic/flee if scoped.
- Hostile behavior: target acquire/forget, melee cadence/reach.
- Villagers/trading are implemented only if M77 made them M100 blockers;
  otherwise classify as non-goal/deferred.

## Acceptance

- Entity coverage table by type/family: spawn, sync, AI, damage,
  drops/XP, despawn, persistence, two-client evidence, accepted gaps.
- Harness and two-client evidence cover scoped behavior.
- AI budgets and lock behavior are measured or marked degraded.

## Implementer 8/20 Slice

Quality label: `draft`.

This slice intentionally stayed narrow: loaded-terrain pathing now has a
bounded per-entity candidate budget, and the play loop refuses direct hostile
movement into chunks that are not currently loaded by any session. It does not
attempt full vanilla pathfinding, chunk generation from AI, broad spawn/despawn
policy, or entity-family parity.

### What Landed

| File | Change |
|---|---|
| `crates/mc-entity/src/lib.rs` | Added `PathingProbe`, `PathingProbeResult`, `PathingBudget`, bounded `FollowPosition` pathing, and goal tick stats for pathing move/blocked/unloaded decisions. |
| `crates/mc-net/src/play.rs` | Imported the pathing types for play-loop integration. |
| `crates/mc-net/src/play/session.rs` | Routed entity goal ticks through a loaded-chunk pathing probe so hostile AI does not directly step toward unloaded target chunks. |
| `crates/mc-net/src/play/tests/spawning_and_world.rs` | Added a runtime regression for hostile pathing refusal at an unloaded chunk boundary. |
| `docs/VALIDATION_LEDGER.md` | Updated N1 with M85 draft pathing evidence and remaining debt. |
| `docs/milestones/M85.md` | Recorded this draft slice, evidence, and gaps. |

### Evidence Matrix

| Gate | Result |
|---|---|
| Cargo baseline | Final gates run after code changes: `cargo test -p mc-entity --lib`, `cargo test -p mc-net --lib hostile_`, `cargo test -p mc-net --lib`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo test --workspace`. An earlier `cargo fmt --all -- --check` found one formatting diff before `cargo fmt --all`; the final check passed. |
| Focused tests | `mc-entity` unit/runtime tests exercise the actual `EntityStore::tick_goals_with_pathing` path for flat movement, one-block step-up, simple detour, blocked terrain, and unloaded terrain. `mc-net` hostile tests exercise the play-loop `tick_entities_and_collect_physics_queries` path, including unloaded target chunk refusal. These are not pass-by-construction because they drive the same goal ticking and query collection used by runtime sessions. |
| Vanilla oracle | Not run. No packet IDs, layouts, or vanilla AI timing claims were changed. |
| Client/manual gate | Not run. No PrismLauncher/real-client evidence was collected. |
| Performance | Degraded/not measured beyond bounded implementation review. Candidate scanning is capped by `PathingBudget::max_candidates_per_entity` with default 8; no TPS or lock metrics were measured. |
| Concurrency | Reviewed for this slice: pathing probe reads the already-collected active chunk set under the existing session lock and does not lock world storage, spawn chunks, or perform async/blocking work. Broader M90 lock review remains required. |
| Data/protocol facts | Not changed. ADR 0001/0002 remain in force. |
| Persistence/storage | Not touched. Entity goal persistence remains existing behavior. |
| Dependencies | No new dependencies and no intended `Cargo.lock` changes. |
| Known gaps | No terrain-material collision predicates in the play-loop pathing probe; passive panic/flee is not implemented; spawn/despawn caps were not changed; entity-family coverage table is still missing; no two-client/client/manual/oracle evidence; no AI budget metrics beyond static bounded candidate count. |
