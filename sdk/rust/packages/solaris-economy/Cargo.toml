[package]
name = "solaris-economy-plugin"
description = "Solaris Economy WebAssembly component source."
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
solaris-plugin-sdk = { path = "../../solaris-plugin-sdk" }
toml = "0.8"
wit-bindgen = "0.62"
