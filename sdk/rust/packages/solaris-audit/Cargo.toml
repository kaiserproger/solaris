[package]
name = "solaris-audit-plugin"
description = "Solaris Audit WebAssembly component source."
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
solaris-plugin-sdk = { path = "../../solaris-plugin-sdk" }
wit-bindgen.workspace = true
toml.workspace = true
