/target/
/.gradle/
**/.gradle/
**/build/
**/*.rs.bk
*.pdb

.idea/
.vscode/
*.iml
.DS_Store
*.swp
*.swo

# Local oracle/scratch dir. Everything inside is local-only by default;
# only Markdown files (engineering notes, observations) are tracked.
/.analysis/*
!/.analysis/*.md

# Vanilla Mojang data files (per ADR 0001) are not redistributed via git.
# They are repopulated locally from .analysis/server.jar by
# tools/extract-vanilla-data.sh. The README and any future schema docs
# stay tracked.
/data/vanilla/data/
/data/vanilla/version.json
/data/vanilla/reports/

# Side-effect of running vanilla-server-derived helpers and client launchers.
# Local-only runtime state must never enter source control.
/logs/
/client-mod/solaris-client-agent/*/logs/
/client-mod/solaris-client-agent/*/.analysis/
/client-mod/solaris-client-agent/*/run*/

# Local Claude / session-scratch dirs (per-session prompt drafts,
# downloaded manifests, harness settings). Not part of the project.
/.claude/
/work/

# Local CodeGraph index. Derived from source; regenerate with `codegraph init`.
/.codegraph/

# Agent/runtime scratch and generated local launch state.
/.ai-bridge/
/.codex/
/run/
/run-mcp/
/run-far-travel*/
/docs/spark-team/
**/__pycache__/
**/r.*.*.mca

# Local decompile/oracle and repository snapshots.
/net/
/patches/
/repomix-*.md
/repomix-output.md
