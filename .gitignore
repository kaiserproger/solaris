/target/
**/*.rs.bk
*.pdb

.idea/
.vscode/
*.iml
.DS_Store
*.swp
*.swo

# Local oracle/scratch dir. Everything inside is local-only by default;
# only Markdown files (engineering notes, observations) are tracked.
/.analysis/*
!/.analysis/*.md

# Vanilla Mojang data files (per ADR 0001) are not redistributed via git.
# They are repopulated locally from .analysis/server.jar by
# tools/extract-vanilla-data.sh. The README and any future schema docs
# stay tracked.
/data/vanilla/data/
/data/vanilla/version.json
